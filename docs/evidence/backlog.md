# Backlog — task-pipeline

> **The board.** Mutable: priority is re-derived, state changes, rows close. A row may
> never vanish silently — it leaves into *Closed* with its commit.
>
> Seeded 2026-08-09 from **twenty-four open rows across eight carry-over ledgers** — sixteen across six by the first count, which read the status column by position and was wrong in half the corpus, which is
> what the project had committed to remembering with nowhere to rank it. Three of them
> were the same finding written three times; the board is where that collapses.
>
> `prio = sev × blast + age_bonus` — inputs in the row, formula here, so the ranking can
> be checked rather than trusted. Doctrine: the skill's `references/backlog.md`.
>
> **Age and Prio re-derived 2026-08-31 against each row's Source date.** Every open
> row had sat at its seed value — rows filed 08-17 and 08-20 still said Age 0 on
> 08-29 — under this header's own claim that priority is re-derived. `Age` counts
> days since the Source date; `age_bonus` is 1 past 14 days, 2 past 30. A loop
> iteration that reads this board re-derives before ranking, or its ranking is the
> seed order wearing a formula.

| id | What | Source | Size | Sev | Blast | Age | Prio | State | Home |
|---|---|---|---|---|---|---|---|---|---|
| B-118 | **On the cheapest tier, checkup/setup/prod-check phrasings route to project-audit instead of task-pipeline's own modes.** Measured 2026-08-31 in the blind trigger probes (evals/RESULTS.md → the haiku run): «что у нас накопилось непроверенного?» (TRIG-05, the checkup trigger), «проверь документацию проекта перед тем как что-то строить» (TRIG-06, setup) and «проверь, всё ли живо в проде после вчерашнего релиза» (TRIG-09, production check) all answered `project-audit`; sonnet and opus routed all three correctly. The two skills now genuinely share the what-is-unverified ground and only the fuller descriptions separate them. Fix shape: name the seam in both descriptions — project-audit's Not-for gains "what shipped unverified is the pipeline's checkup", task-pipeline's checkup clause names its own two modes harder — and keep TRIG-05/06/09 as the eval anchors that must stay green. | blind eval run, 2026-08-31 | S | 2 | 2 | 0 | **4** | open | — |
| B-114 | **The run receipt records no cost, no retries and no human-corrections count** (extends B-096). Anthropic publishes per-phase cost tables for its own long-running harness — planner $0.46, build $113.85, QA $10.39 (*Harness design for long-running agents*, anthropic.com/engineering, read 2026-08-30) — and the harness-engineering digest's Change Receipt carries `cost_usd`, `retries` and `human_corrections` per change. This pipeline records none of the three: grep finds only a qualitative "token cost" (certification.md, *What this costs*). Fix shape: optional computed fields on the run ledger / hand-back — cost per stage where the harness exposes it, retry count from the `touch:` lines it already keeps, human corrections from the manual-gate returns — disclosures, never targets. | harness-engineering articles, 2026-08-31 | M | 1 | 2 | 0 | **2** | open | — |
| B-115 | **Stage 6's evidence channels are code- and UI-shaped, and research/data deliverables have no named channel** (extends B-074). The gate doctrine names tests for code and the browser look for UI; a RESEARCH deliverable (a market summary, a doc synthesis) and a DATA deliverable (a migration, a dataset) reach stage 6 with no stated channel, so their verification is whatever the run improvises. External framing agrees: the digest's per-artifact-class table is CODE→tests, UI→render+screenshot, RESEARCH→sources+contradiction check, DATA→schema+range+freshness (read 2026-08-30). Fix shape: a per-artifact-class evidence-channel table in gates.md, and verification.md rows naming the channel used. | harness-engineering articles, 2026-08-31 | M | 2 | 2 | 0 | **4** | open | — |
| B-116 | **Nothing computes which harness clauses were born under which model generation.** The doctrine now says to re-test compensating controls when the confirmed tier moves a generation (model-tiering.md → *A harness clause encodes a model's weakness*, shipped v1.80.0), and no mechanism lists the clauses or their birth generations, so the re-test is a memory. Anthropic removed a whole construct from its own harness when a newer model stopped needing it (read 2026-08-30). Fix shape: a dated `born-under` note on caps and ceilings (fix-loop 5, certify ceiling 3, the context-budget flush point), and a checkup line naming clauses older than the current generation. | harness-engineering articles, 2026-08-31 | S | 1 | 2 | 0 | **2** | open | — |
| B-117 | **The failure mode where a model prematurely wraps up as the window fills has no name in the continuity doctrine.** Anthropic names it "context anxiety" — the model starts summarising and closing early when it believes the window is nearly spent, even when it is not (read 2026-08-30). continuity.md's Part 2 already forbids declaring the context exhausted without a signal, which is the same failure from the other side; naming it beside the boundary doctrine gives run reviews a word for what they are seeing. Fix shape: one paragraph in continuity.md → Part 2 (and the hand-back's NOT VERIFIED note where a run wrapped early), citing the dated source. | harness-engineering articles, 2026-08-31 | XS | 1 | 1 | 0 | **1** | open | — |
| B-110 | **Three surfaces state three different rules for what the retro archive receives.** `references/retrospective.md` says the Recent log is **uncapped by design** and the run stamps are capped at ten with rotation at the eleventh. `cursor/rules/task-pipeline.mdc` says *«Entries older than the last five run stamps move to an append-only archive»*, and `docs/evidence/retro/2026-Q3.md` says its entries were rotated *«once they fell outside the last five run stamps»* — a window for log entries that the doctrine does not have and nothing enforces. Found 2026-08-20 while collapsing `B-060`/`B-069`: fixing the live heading exposed that the false bound also lives in a self-contained Cursor rule and in the archive's own header, and choosing between *uncapped* and *five stamps* is a doctrine decision, not an edit — so it was filed rather than taken. Fix shape: decide which unit governs log entries, state it in `retrospective.md` alone, and have the other two derive from it; the heading guard added today only refuses a bound in the heading, not one in the prose beneath it. | this session, 2026-08-20 | S | 1 | 3 | 11 | **3** | open | — |
| B-106 | **Fourteen consecutive releases carry no run stamp, and the cold-retirement trigger has had nothing to read across all of them.** Measured 2026-08-20 by tag range rather than by grepping a tag's own commit — a stamp names the commit the *run* ended on, never the release commit, which is why the receipt in `retro.md` reported every tag as unstamped as written: `v1.60.1` through `v1.72.0`, 14 of 117 tags, and 84 unstamped overall because the register began at `v1.41.0`. The gap is now recorded and a guard requires it to be, so the next release either carries a stamp or lands in a declared range. What is NOT closed: the releases themselves ran outside the pipeline, so the standing instructions' *has not fired in the last five run stamps, or in the last sixty days* trigger has been readable only through its sixty-day half since 2026-08-16, which is doing all the work. Fix shape: run the pipeline on this repository again, or state a policy for guard-only releases and give the cold trigger a second countable unit that such a release moves. | this session, 2026-08-20 | M | 2 | 2 | 11 | **4** | open | — |
| B-107 | **Two of the registered claim classes match their noun anywhere, so they only worked while the corpus never narrated.** `negative self-tests` matches any `N guards` and `gates.md's own axes` any `N axes`. Bringing the registers into the corpus on 2026-08-20 refused 26 statements and every one was narration — "Two guards caught the change as it landed", "1, 2, 3 and 4 guards not firing", "two axes of four". The dated-record exemption removed most of them and the rest were reworded with a qualifier between the number and the noun, which is the device the `rotation axes` class already relies on. It is a **writing rule with no mechanism**, and it was broken twice within this session's own edits — once in `B-091`'s closing note, once in `B-095`'s own axes. Both were caught by the gate rather than by review, which is the arrangement working and also the measure of how often the rule will be broken. Measured alternative, and why it was not taken: requiring a totaliser (`the N guards`, `all N axes`) disarms both classes on the surfaces their original incidents happened on, including the Cursor rule's bare *Gates: three axes*. Fix shape: split each class into a total form and a subset form so the subset can be recognised rather than avoided. | this session, 2026-08-20 | S | 1 | 3 | 11 | **3** | open | — |
| B-108 | **The widened reference-file class reads three phrasings, and a fourth was in the bundle.** `references/progress.md` said *«over 34 files nobody checked»* against a directory of 35 — found by grep while fixing the three surfaces the class does catch, not by the class. The pattern now covers `N reference files`, `N doctrine files` and ``N files under `references/` ``; a bare `N files` is deliberately outside it, because in this corpus that phrase counts guards, surfaces, plants and fixtures far more often than reference files, and a class that fires on all of them is a class somebody switches off. Fix shape: give the bundle's size one name the doctrine actually uses and check that instead of enumerating phrasings — the same move `_axis_keys()` made for the rotation axes. | this session, 2026-08-20 | XS | 1 | 2 | 11 | **2** | open | — |
| B-109 | **The citation-currency guard reads open rows only, so every closed row's `file:N-M` is unchecked forever.** 9 spans are checked and 6 single-line citations are disclosed as unanchored; the 5 ranges in closed rows are neither. That is deliberate — a closed row states what was true at closing, and rewriting it edits a record — but it means the `## Closed` list and every closed row's evidence can rot into pointing at other text with nothing saying so. Fix shape: resolve a closed row's citation against the commit its State cell names rather than against `HEAD`, which is the only comparison that is both checkable and honest. | this session, 2026-08-20 | S | 1 | 2 | 11 | **2** | open | — |
| B-080 | **A node cannot say how it will be closed, and shipped doctrine tells an agent to read that field.** `graph.schema.json`'s node carries `id, title, owner, status, blocked_by, serves, evidence, parked_reason` — there is no `check`. Meanwhile `agents/verifier.md` instructs the verifier to *«run the checks the task named. Not a check you invented, and not `npm test` alone if the task named something narrower»*. Nothing writes what the task named, so the instruction points at an absence and the verifier's only honest options are to invent a check or to run everything — the two things the paragraph forbids. Filed by the manifesto audit, 2026-08-17; the contradiction is between two files shipped the same day. Fix shape: `check` on the node (the command or test name that closes it), required for a node the verifier will close, and the verifier runs that and reports its output as the evidence row. | manifesto audit, 2026-08-17 | S | 3 | 2 | 0 | **6** | **closed 2026-08-19** — `check` is a node property, **required on every node except a `parked` one**: the node the verifier will close says how it will be closed, and the parked node is the one nobody will close, where *n/a — parked* would be the confidence-without-correctness this schema refuses everywhere else. One string, never a list — the requirement is singular throughout, and a node needing two unrelated checks is a node doing two jobs. `agents/verifier.md` step 2 now reads **the node's `check`**, runs it, and its output is the first `evidence` row; a new step 7 requires every `replan.add` entry to name its own, refused in `verdict_violations()` so the key is named BEFORE the close writes anything rather than aborting a close whose node was already marked done. `add --check` is required like `--why`. The rule is stated twice on purpose — schema and `violations()` — because the schema never runs against a live graph, and both are RUN against a planted graph: `validate` exits 1 naming the node for a `check` absent, blank, whitespace or carrying a line break, and exits 0 for the parked exemption. The two homes are also compared directly: whatever `agents/verifier.md` tells the verifier to read off the node must be a property `graph.schema.json` declares — the comparison nobody made when the contradiction shipped in two files on one day. Eight planted defects refused, including both shapes that beat earlier guards here (an `if` made unsatisfiable by an extra `required`, and a `^.*$` pattern) plus a disarm run against `graph_test.py`, so the fixtures are not decoration. One guard was defeated first, in the propagation rather than in the code: invariant 56's `*(guard: …)*` citation nested backticks, so the citation check extracted `' and '` as a cited literal and passed on fragments — a citation resolving for the wrong reason, found by printing the extraction rather than by the guard. `graph_test.py` 114 → 129; guards 376 → 384. | — |
| B-081 | **Verification never expires: a row verified at commit A stays `verified` after commit B.** `docs/evidence/verification.md` tracks rows that were *never* verified — the exposure line counts them — and has no notion of a row whose verification has been **overtaken** by a later change. Nothing maps a diff to the claims it puts back in doubt, so a ledger can read fully green over a tree where every check ran against code that has since moved. These are the same failure from two ends and only one end is instrumented. Fix shape: each verification row records the commit it observed and what would invalidate it (code, dependency, environment, policy); a check computes which rows a diff has overtaken, and the exposure line prints that count beside the `never` count. Invalidation is not deletion — the historical row stays true about what it saw. **Sharpened 2026-08-17:** the mechanism already exists in this pack for one artifact — `references/knowledge-graph.md:100-129` gives the code graph a `built_at_commit`, computes commits and days behind `HEAD`, names three signal states and stamps every non-current one *«not trusted for reach until refreshed»*. This is a port of a tested contract, not new design, and the part most implementations miss is that state zero is printed out loud. Two halves are still missing from the row as written: invalidation is not deletion, and an invalidated claim reopens the node that served it; and the **policy** under which evidence was accepted is unversioned (B-091), so the check has a tree to compare against and no rule. | manifesto audit, 2026-08-17 | M | 3 | 3 | 0 | **9** | **closed 2026-08-17** — `templates/verification.md` gains `Observed at` (the commit the check ran against) and the doctrine of the four invalidators; `templates/exposure.sh` reports **current · behind · unresolvable · unanchored** as a disclosure with no floor and no target, each non-current state carrying *not trusted until re-observed*; `references/verification.md` carries it as the other end of `never`. A port of the freshness contract `references/knowledge-graph.md` already sets for the code graph, as the row's own sharpening said. `test/validate.py` EXECUTES the section over a real repository with exactly one row in each state, plus state zero. Placement was itself a defect: the first version sat after the check-list, which exits early when nothing is unverified — invisible in the state where the counts matter most. Eight planted defects, three of which defeated the guard first: a substring match on `staleness`, a marker checked once instead of per state, and nothing asserting the SHIPPED template carried the column. Guards 366 → 367. | — |
| B-082 | **`references/audit.md`'s pass table ends on a reversal it never explains, and it is now publicly cited.** The self-inflicted share runs 42% → 53% → 77% → **21%**, the last row being pass 7 with 19 findings of which 4 were self-inflicted. The prose above stops at pass six and is therefore not wrong, but the last row argues against the decay the section teaches, and the vertical pass is described separately at line 62 so it cannot be credited for the drop. An external document now permalinks straight to this table, so a reader following the receipt lands on the anomaly. Fix shape: state what pass 7 did differently, or end the series at pass 6 and keep the seventh row in a footnote with its explanation. **Sharpened 2026-08-17:** the citing sentence and the cited range disagreed — the manifesto's prose stopped at pass six while its permalink covered the table including the unexplained seventh row. | manifesto audit, 2026-08-17 | XS | 1 | 2 | 0 | **2** | **closed 2026-08-20** — footnoted, not truncated. `/Users/sshlg/DATA/pod-manifesto/manifesto.md:356` now names the seventh pass, says *«the record does not say what that pass did differently»*, and refuses to end the series at six because *«deleting a measured row to protect a claim is the opposite of what this document asks for»* — so the member's own record says the same. The table keeps all four rows, gains the computed self-inflicted share (42% → 53% → 77% → 21%) and a note stating that the trend is measured over passes four to six, that pass seven is not part of it, and that the vertical pass cannot be credited for the drop because it is a separate pass. The measurement survives; the claim is scoped to what it covers. | — |
| B-083 | **`R-005`'s independent reader is never measured, so we cannot say what its independence is worth.** The standing instruction requires a reader who did not write the check; in practice that reader shares the model, the instructions and the repository, and differs only in context. It works — the 2026-08-17 read of T-3 returned 14 findings against work its author had gated green, two critical — but *how often it disagrees* has never been counted, and a judge never observed disagreeing is the agentic form of a green nobody watched turn red. Fix shape: record each reader's finding count and confirmed rate in the verification ledger, and state in `R-005` what an isolated context does and does not buy — it is not a deterministic runner, a contract at another boundary, or an external system. **Sharpened 2026-08-17:** the defect is not only that independence is unmeasured but that **the reader ranking is inverted**. `references/review.md:138-146` orders readers — *«A subagent this run dispatches»* first — subagent → bot → person and justifies it by observability, which is a different axis: a dispatched subagent shares the model, the instructions, the repository and the rubric with the author and differs only in context, making it the least independent of the three and placing it first. The file names that blind spot two paragraphs later about the probes and never draws the same conclusion about the reader. Meanwhile the pack's one genuine second evidence path — the `gate:` line written by `hooks/gate-observer.sh` against the agent's own `stage:` claim — is not named in `review.md` at all. First deliverable is a sentence stating what an isolated context does and does not buy; the count is second. | manifesto audit, 2026-08-17 | S | 2 | 2 | 14 | **4** | open | — |
| B-084 | **`graph.py add` manufactures a fake edge every time it runs, and the payload rule cannot fire at runtime.** The graph stores a dependency in two unlinked places: `blocked_by`, which is what `frontier()` obeys, and `edges`, which carries the required `payload` and which no execution path reads beyond a from/to existence check. `add` writes `blocked_by` and never touches `edges`, so every node added mid-run creates a scheduling dependency whose payload is unnamed **by construction** — measured: adding a node to `graph.example.json` gives 5 nodes, 2 edges, `validate` exit 0. Worse, `violations()` never inspects `payload` at all, and the schema that does is never applied to a live graph, so an edge with no payload passes while `jsonschema` rejects the same file. This is the manifesto's named teeth — *«an edge that carries no named artifact is chronology drawn as architecture»* — and our own mutation verb draws chronology. Distinct from B-077, which is about `serves` not resolving. Fix shape, three parts together: `violations()` checks `edges[].payload`, `title` and `serves` non-empty (all three are schema-only today, and `serves: ""` passes runtime while the schema forbids it); `violations()` requires a payload-bearing edge for every `blocked_by` pair in both directions; `add` takes `--carries` and writes the edge with the node. Add `--why` in the same change — `park` demands a reason and `add` demands nothing, so half the graph's revision surface is silent. | manifesto audit, 2026-08-17 | M | 3 | 3 | 0 | **9** | **closed 2026-08-17** — four parts, together: `violations()` refuses an edge whose `payload` is missing or blank and a `blocked_by` with no payload-bearing edge in the blocker→blocked direction; `title` and `serves` must be non-empty at runtime, where the schema never ran; `add --carries` pairs one payload per `--blocked-by` and writes the edge **with** the node, refusing a count mismatch by naming both counts; and `add --why` plus a `revisions` log both verbs append to, `why` required and bound to a non-whitespace pattern. Watched: four planted schema defects refused, including a `^.*$` pattern and a nullable `why`. Tightening the rule broke the fixtures that had relied on it being loose — the helper now derives an edge per `blocked_by`, and the one fixture needing a dependency without an edge asks for it. `graph_test.py` 62 → 75; guards 361 → 362. | — |
| B-085 | **Nothing computes the coverage relation; done is still a status read off a table.** `references/acceptance.md:44-48` defines the path exactly — decision → spec section → contract and its failure behaviour → task → change → executed test → surface and docs — and an agent walks it by hand, one REQ at a time. No artifact holds the path and no script computes it: the only seeded coverage script computes **stage** coverage, the bidirectional REQ↔ledger refusal is prose addressed to the agent, and `graph.py` has no `coverage` verb. A broken path is therefore invisible unless the walker notices, which the pipeline's own doctrine calls the failure mode of a rule that should be a mechanism. This is the superset of B-077 — that row's `coverage` verb printing two directions is step one, and the sharper framing is that there are four: REQs with no node, nodes serving nothing, nodes done with no evidence row, evidence rows closing no REQ. Closing it also answers the manifesto's *«requirements with no evidence path»* measurement, which today is enforced per run and counted never. | manifesto audit, 2026-08-17 | L | 3 | 3 | 0 | **9** | **closed 2026-08-17** — the graph carries `requirements` (required, non-empty) plus optional `goal_clauses`, so `serves` resolves; `add` refuses to invent a requirement and names what the brief froze; `graph.py coverage` computes three directions — no node serves it, every node serving it is parked, and each requirement with its nodes and statuses — and **names the fourth as unread**, because it lives in the ledger. Two guards were defeated first: a source scan for `cmd_coverage` passed a renamed `_cmd_coverage_disabled`, and running coverage only against the covered example passed a `return 0` that had stopped refusing. Both behavioural now, with the example supplying the failing control and a derived copy the passing one; the subparsers are built from the dispatch table so an unwired verb cannot exist. Seven planted defects watched refused. `graph_test.py` 75 → 85; guards 362 → 364. | — |
| B-086 | **No proof records what produced it.** Every artifact records what was done, what proved it and whether a person looked. None records the model, the runtime, the version of the doctrine the run executed under, the tools and permissions it held, or a trace id — not the coverage table, not `templates/verification.md`, not a graph node. `references/documentation.md:46-48` already states the rule this needs: where the subject is non-deterministic the address **is** a trace id and the assertion that ran against it. The rule is stated and no artifact can hold its object. Concrete consequence: two runs six months apart, one under v1.40 doctrine and one under v1.69, produce indistinguishable coverage tables, so a defect traced to a doctrine change cannot be scoped to the runs that carried it. Fix shape: a `producer:` block written once per run into the acceptance artifact and echoed into each verification row — actor, model resolved at runtime (never a hardcoded id, per `references/model-tiering.md`), the skill version from `plugin.json`, the pipeline config's hash, and the harness trace id where one exists; written by a hook where the harness exposes the value and marked `unavailable: <why>` where it does not, never omitted. | manifesto audit, 2026-08-17 | M | 3 | 3 | 0 | **9** | **closed 2026-08-17** — `graph.py producer` prints seven fields and needs no graph: skill version from the plugin manifest, a digest of the project's `pipeline.json`, and `git rev-parse HEAD` resolve from the tree; `actor`, `model`, `runtime` and `trace` are read from named environment variables a project wires once. Every unresolved field **prints anyway and says why** — an omitted field is indistinguishable from one checked and found empty — and `model` is never inferred. `templates/verification.md` carries the block with the command that computes it. The guard RUNS the verb; five planted defects watched refused, and the fifth needed its own fixture because the guard cannot observe the no-manifest branch in a repository that always has one — a version guessed as `task-pipeline@unknown` had passed. `graph_test.py` 85 → 93; guards 364 → 365. Two harness defects fixed on the way: a non-assertion exception used to abort the whole suite, hiding every case after it. | — |
| B-087 | **Release acceptance proves the pointer, not the path.** Stage 10 requires `git submodule status` with no `+` and every repo clean and pushed. That is a statement about **commits** — the parent points at the child's newest one. It does not prove anything works at those two versions together. A parent can point at a green submodule whose contract the parent calls with the previous signature, and every check passes: the submodule's suite ran against the submodule, the parent's against the parent, and no check ran across the pointer. `pipeline.schema.json`'s `release.verify` is the only release-time verification and is explicitly one component. Fix shape: a stage-10 criterion that, for a change spanning components, names one cross-component path, states the exact versions at which it was observed (tag or commit or pin per component), and records the observation as evidence to the same standard a single REQ already meets. The umbrella's own `test/check_pins.py` and release smoke test prove it is buildable; the doctrine that makes every project do it does not exist. | manifesto audit, 2026-08-17 | M | 3 | 3 | 0 | **9** | **closed 2026-08-17** — `templates/convergence.sh` ships plus the stage-10 criterion in `references/acceptance.md`. It checks the pointers mechanically, checks the one thing `git submodule status` cannot see (whether the pinned commit is **published** — the failure that broke the v0.80.0 tag at checkout), and requires a recorded cross-component observation naming the exact version **the parent pins**, fired only where a pointer moved in the accepted range. `test/validate.py` EXECUTES it over four shapes built from real git repositories; five planted defects watched refused. On its first live run it found a real defect in the umbrella — the parent's pointer and the child's HEAD disagree — and one in itself: the published-pin section read the child's HEAD where it needed the parent's pointer, which are the same fact only while they agree. Guards 365 → 366. | — |
| B-088 | **The work graph does not survive the run that walked it.** `graph.schema.json` declares `.task-pipeline/graph.json` a run artifact never committed, and `.gitignore` excludes the directory; stage 10 copies out pass counts and loop-guard rulings and nothing else. So the shape the run actually walked cannot be reconstructed afterwards — which is precisely the condition the manifesto names as making a completion claim unfalsifiable, and precisely what `references/planning.md:83-100` argues against for the stage list (*«a graph that decides its own shape while running produces a shape nobody drew»*). The graph is the one artifact carrying `serves`, `payload` and `parked_reason`, the three fields that make a claim checkable. Fix shape: stage 10 writes the final graph, or a rendered digest, into the committed acceptance artifact beside the coverage table, and records each re-plan the verifier caused as a revision line with its reason. | manifesto audit, 2026-08-17 | S | 3 | 2 | 14 | **6** | open | — |
| B-089 | **The pipeline can prove it built the thing and cannot represent whether it worked.** `hypothesis`, `success signal` and `product proof` return zero hits across `SKILL.md`, every `references/*.md`, every template and both schemas. Intake opens at *what to build*; the four acceptance statuses are all delivery-side; `verification.md`'s `Human` column records that a **person looked**, not that an outcome moved. So there is no legal way to record *«released, hypothesis not yet observable»* — the honest state of most work on the day it ships — and `unknown` fails the gate, which means the only route is to promote it to `verified`. Fix shape: three optional lines at the top of the brief (problem, hypothesis, and a success signal naming its measurement and the earliest date it can be read, with `none — internal change` a legal and common answer); a product column in `templates/verification.md` whose values are `unobserved` / observed-with-a-date / `contradicted`; and — exactly like the `Human` column's `never` — no floor, no direction and never a target, because the failure mode of an outcome column is people filling it in. | manifesto audit, 2026-08-17 | M | 2 | 3 | 14 | **6** | open | — |
| B-090 | **`verified` never records that its own check was watched failing.** Three places make *«a green from a check nobody watched fail is not evidence»* binding, and all three are **run-level gate criteria**. Nothing writes the fact against the claim: a coverage row, a verification row and a graph node's `evidence` array look identical whether the check behind them was probed or has never been observed in any state but green. This is the same defect the pack already fixed one level over — a `verified by` cell naming a check that does not exist is refused, and a cell naming a check nobody has watched fail is not. Fix shape: each evidence cell carries a probe reference — the plant label from `test/probe.py`, a mutation run id, or the literal `unprobed`; `unprobed` is legal and counts into `abstained`; stage 10's criterion 3 becomes computable instead of asserted. | manifesto audit, 2026-08-17 | M | 3 | 2 | 14 | **6** | open | — |
| B-091 | **The acceptance policy has no owner, no version, and its two rules contradict each other.** `references/gates.md:57-59` says *«the framework fixes no stage count and no gate assignment»* — gate assignment is the operator's decision; `references/acceptance.md:390-392` says stage 10 is *«Manual by design»* and criterion 10 requires the operator every run. Both cannot be true, and the second wins today: a one-line doc fix still requires a sign-off ceremony, which is the pressure that teaches an operator to route around the pipeline on small work. Separately, nothing names who owns the rules (which evidence types count, which gates are manual, what independence means at each risk level, how long evidence stays valid, who may change any of it), there is **no acceptance template** at all, and the coverage table's shape has already drifted across ten runs. A table accepted under v1.20 doctrine is indistinguishable from one accepted under v1.70, forever — in a pack that makes every SHA in the retro resolve. Fix shape: an `acceptance` block in `pipeline.schema.json` with a policy id, version and risk predicate; a `policy:` line stamped into the acceptance artifact; `references/acceptance.md` states manual is the **default policy** rather than the design, and any auto-close stamps the policy version that permitted it. **The doctrine half closed 2026-08-20:** the `## GATE` section opens with **`AP-1`** — a policy id, an owner (*the operator of the project running this pipeline*), an in-force date and what it supersedes — and states that an amended criterion moves the version to `AP-2` with a row in the decision register. `references/gates.md` now scopes its *«the framework fixes no stage count and no gate assignment»* sentence to the pipeline's **shape**, and says explicitly that where a project keeps stage 10 manual the criteria are `AP-1` — the two rules stood side by side unscoped for seventy releases and a reader could take either as the whole rule. Three new guards, three plants watched failing: the id (anchored on the declaration, because the block's own *moves to `AP-2`* sentence satisfied a bare `AP-\d` search and was watched doing so), the owner, and the scoping sentence in `gates.md`. **Still open:** the mechanical half — an `acceptance` block in `pipeline.schema.json` carrying policy id, version and risk predicate, a `policy:` line stamped into the acceptance artifact, and an acceptance template. Until those land, a table still cannot say which version of the standard admitted it. | manifesto audit, 2026-08-17 | M | 3 | 2 | 14 | **6** | open — doctrine half closed 2026-08-20 | — |
| B-092 | **The report an operator actually reads carries neither scope nor what was skipped.** The pipeline computes, at every gate, exactly the numbers a not-verified field needs — `abstained` and `unlooked`, no floor, no target. But the hand-back has TASK / PROGRESS / DONE / SURFACED plus decisions and ambiguities, and its ledger trace records `done`, `surfaced`, `decisions`, `amb`. Neither carries what scope the claim covers nor what was never looked at. So a run can hand back a report that is honest sentence by sentence and still cannot be told apart from a run whose checks never looked — the precise failure this pack's own doctrine names three separate times. Fix shape: `SCOPE` (commit, environment, REQ ids, surfaces covered) and `NOT VERIFIED` on the hand-back block and on the `hand:` ledger shape; `NOT VERIFIED` populated from the disclosures the gates already computed; the literal `none within the stated scope` legal and an empty field not; a guard counting `hand:` lines that carry both, with a planted defect in CI. | manifesto audit, 2026-08-17 | M | 2 | 3 | 0 | **6** | **closed 2026-08-17** — `SCOPE` and `NOT VERIFIED` are in the hand-back block and in the `hand:` ledger shape beside it (the block is transient, the ledger survives a compaction, so a field in one and not the other is lost when it is needed). `NOT VERIFIED` is populated from the `abstained`/`unlooked` sets the gates already compute rather than composed, and the literal `none within the stated scope` is required for the empty case — canon 9a one artifact over. Three of five plants defeated the guard first, all by one hole: either side satisfying a check meant for both — a file-wide word search passed a block carrying neither, and a search across `hand:` lines was satisfied by the example while the shape had lost it. Each is checked against its own continuation now. Guards 373 → 374. | — |
| B-093 | **The frontier can dispatch two agents onto one mutable target.** `references/planning.md:74-81` states the rule with the right teeth — *«distinct is not the same as independent; the check is what they touch, never what they are called»* — and it lives entirely in the markdown plan. The role-agent design replaces the plan with `graph.json` as the thing that decides what runs next, and the node schema is `additionalProperties: false` with no field for what a node touches. So `frontier()` ranks by `blocked_by` alone and can return two runnable nodes that write the same file, the same register or the same fixed scratch path — the exact false parallelism the layer above refuses — and nothing can even report it. Fix shape: a `touches` array on the node (paths, register names, remote resource ids); `violations()` reports any pair of mutually unblocked nodes sharing a member; `next` withholds the second or prints the collision **on stderr**, because the frontier rows are the one line paid for on every iteration. | manifesto audit, 2026-08-17 | M | 3 | 2 | 0 | **6** | **closed 2026-08-17** — `touches` ships on the node (paths, registers, resource ids) and `next` names any pair of **simultaneously-runnable** nodes sharing one, plus the shared target. Only simultaneously: a sequential pair never holds it at once, and warning about it is how a warning becomes noise. Both reports go to **stderr** — the frontier rows are parsed one per node and paid for every iteration, so a warning among them reads as a node. The third state is the load-bearing one: a frontier where nobody declared `touches` produces no pairs and looks checked, so `next` prints how many said nothing. Three existing width fixtures went red correctly — they read stdout and stderr merged; `run_out`/`run_at_out` read the stream the contract is about. Six planted defects refused. `graph_test.py` 93 → 101; guards 371 → 372. | — |
| B-094 | **The one checker the manifesto names by name has never been watched failing.** `references/gates.md:196-199` makes *«the check has been probed»* a precondition on running any check, and this repo mechanises it for its own validator with 361 planted defects. The group convergence check is exempt in practice: it is a five-item prose checklist an agent runs over a set of diffs, and its gate requires only that it ran and logged a line. No fixture, no planted defect, no recorded historical failure it caught. Each of its five items is stated to have been a real defect, so the raw material for the fixture is already in this repository's history. Fix shape: two-to-four synthetic branch reports per item, one clean and one carrying the defect, and a requirement that a project adopting the check runs them once and records the rejection. Add *carries its evidence* as a sixth item in the same change — the manifesto lists it and the five do not cover it. This closes the same class as B-083 from the other side: that row is the unmeasured independent reader, this is the unmeasured group checker, and instrumenting one leaves the other reading as covered. **Citation re-pointed 2026-08-20.** It cited "`gates.md:135-137`"; the obligation is at the address this row now carries. The address resolved and pointed at other text, which is the failure a resolving address cannot report on its own; the row now quotes the phrase it points at, and `test/validate.py` resolves every `file:N-M` in an **open** row against that quote. The finding itself is open: the group convergence check still has no fixture and no planted defect. | manifesto audit, 2026-08-17 | M | 2 | 2 | 14 | **4** | open | — |
| B-095 | **Proof depth follows risk on two proof-depth axes of four, and there is no route below eleven stages.** `references/gates.md:474-480` — *«Scale the check to what breaking costs»* — scales the check to what breaking costs, covering impact and, in one row, irreversibility. **Uncertainty** (how much of the behaviour cannot be checked deterministically) and **coordination** (how many agents, repositories, services and owners meet at the change) are absent as inputs — and both are where this pipeline's own measured failures came from: a REQ closed on judgement demands the same proof as one closed on an executed assertion, and multi-repo work demands the same proof as single-repo work despite B-087. Separately, `references/gates.md:497-500` states *«the floor is not proportionate to anything»* and `SKILL.md` offers only a binary opt-out, so the manifesto's cheapest tier — a narrow check, a diff inspection, a short honest report — has no route through this pipeline at all. That absence is the pressure that produces the *«quick»* escape being used on work that should not have taken it. Fix shape: name all four proof-depth axes in the `Cost:` line that stages 3 and 4 already require, and define one abbreviated route whose floor is stated rather than implied. **Citation re-pointed 2026-08-20.** It cited "`gates.md:465-474`" and "`gates.md:488-493`"; both passages are at the addresses this row now carries. The address resolved and pointed at other text, which is the failure a resolving address cannot report on its own; the row now quotes the phrase it points at, and `test/validate.py` resolves every `file:N-M` in an **open** row against that quote. The row's own axes are now written as **proof-depth axes** — a third sense of the word in this corpus, and the bare form is reserved for gates.md's Axis A/B/C, which is what the claim registry counts. The finding is open: uncertainty and coordination are still not inputs, and there is still no route below eleven stages. **Confidence raised 2026-08-31 by external evidence that graduated harness depth is market-recognised:** the harness-engineering digest ships a Level 0–3 depth ladder; Anthropic states an evaluator is worth its cost only when the task sits beyond what the model does reliably solo, and that the harness should be smaller than the failure surface it controls (both read 2026-08-30). Three independent sources now say proof depth scales with risk — the absence of a route below eleven stages is a gap the market has already named. | manifesto audit, 2026-08-17 | M | 2 | 3 | 14 | **6** | open | — |
| B-096 | **Five of the eight useful measurements are not computed, and every one is two greps from data already on disk.** Only `unlooked`/`abstained` is genuinely computed. **Reopened gates**: `templates/run.md` writes a timestamped `stage:` line on every gate return and `templates/stage-coverage.sh` throws them away by design, keeping only the last verdict. **Time from implementation-complete to accepted**: same lines, same timestamps, no consumer reads them. **False-green incidents**: the class is named with five known shapes and counted nowhere, in a pack whose thesis is that green must be earned. **Manual proof fields still needing automation**: `review` is deliberately excluded from every per-run bucket because it would print the same standing number — correct per run, and exactly why the manifesto lists it as a **system** measurement; the population is already enumerable from the propagation matrix's `review` cells. **Requirements with no evidence path**: enforced per run as a gate and never counted. Fix shape: extend `/task-pipeline checkup`, which already exists to answer questions no single run can ask, with these five — each computed from a file the pipeline already keeps, each printed with no floor and no target. Invalidated proofs is B-081; do not duplicate. | manifesto audit, 2026-08-17 | M | 2 | 2 | 14 | **4** | open | — |
| B-097 | **The walking skeleton is one unfalsifiable sentence on a path most work never takes.** `references/decomposition.md:57-60` calls the first module *«the thinnest end-to-end slice that proves the architecture»* and gates it with an escape written in. Two problems. Decomposition runs only when stage 2 judges the brief a **platform** rather than a change, so ordinary multi-part feature work — the majority — never meets the rule. And the sentence names none of the six elements the manifesto lists, so a slice with a stubbed datastore and no executed end-to-end assertion satisfies it verbatim: nothing requires **real** storage or integration, and nothing requires the **one end-to-end observation** that is the whole difference between a skeleton and inventory. Fix shape: enumerate the six as a checklist on the gate item, and hoist the rule out of `decomposition.md` into stage 4's plan gate so any plan with more than one task declares which task is the skeleton and what its single end-to-end observation is. | manifesto audit, 2026-08-17 | S | 2 | 2 | 14 | **4** | open | — |
| B-098 | **The enforcement ladder tops out at a control that fails open, and has no rung for a capability boundary.** `references/gates.md:128-132` ranks five mechanisms — doctrine line, review question, script check, CI step, hook, the fourth promoted when *«it must hold for people who never run it locally»* — and the top rung is a Claude-Code-only hook that the same file records as **failing open** on a crash. The manifesto's strongest control is of a different kind: not a check that decides, but a capability the run does not hold. Everything outward this pack protects — deploy, publish, opening a PR, creating a shared design file — is guarded by prose plus a manual gate, and both are exactly what a context loss removes, which is the case the manifesto's example is chosen to survive. Fix shape: a sixth rung, *capability boundary*, defined as «the action is impossible because the credential, scope or network path is absent», plus a stage-0 autonomy row asking which credentials this run's environment actually holds and which the operator wants withheld; promote to it when a rule protects an irreversible outward act. This is also the honest answer to why the grill needs a hard floor at all — a floor is what you build when the fence is prose. **Citation re-pointed 2026-08-20.** It cited "`gates.md:61-78`", which is the judgment-gate section; the ladder is at the address this row now carries. The address resolved and pointed at other text, which is the failure a resolving address cannot report on its own; the row now quotes the phrase it points at, and `test/validate.py` resolves every `file:N-M` in an **open** row against that quote. The finding is open: there is still no capability-boundary rung and no stage-0 autonomy row. | manifesto audit, 2026-08-17 | M | 2 | 3 | 14 | **6** | open | — |
| B-099 | **No evidence row records where or when the check was observed.** The coverage table's columns are REQ, Requirement, Status, Evidence; the verification ledger carries `Shipped in` and nothing about the environment the check ran in or the time it was observed. A smoke test against a preview URL therefore enters the record in a shape indistinguishable from one against production, and a suite that passed on a laptop with accumulated state is indistinguishable from one that passed on a clean runner. This pack already owns the incident the field exists for: `references/learned.md:136` records a suite green on every author's machine and 1039 failures on a runner that started clean. The lesson was written down and the field was never added. Combined with B-086, the pipeline instruments two of the five members of a proof's validity domain — commit-ish and, partially, time — and silently drops the rest. Fix shape: environment as a required cell on every evidence row, drawn from the vocabulary the project's own runbook declares; stage 8 refuses to write a verification row whose observation names a non-production target while the REQ claims production behaviour. **The environment half closed 2026-08-20:** `Environment` is a required cell on every row of `templates/verification.md` and of this project's own ledger, with the vocabulary declared in the template — `production` · `preview` · `ci` · `local` · `—` — and read from there by `test/validate.py`, never listed in the checker. `—` is a **recorded absence**, the same shape `Observed at` was introduced with, and every row predating the column carries it; an **omitted cell** is refused, because a row that forgot the question is not a row answering it. Three plants watched failing: a row with the cell deleted, a value outside the declared vocabulary, and the shipped template losing the column — the last because a template without it seeds a dormant section into every host project. **Still open:** the stage-8 refusal of a non-production observation against a production REQ, which needs the REQ to declare that it is about production. | manifesto audit, 2026-08-17 | S | 2 | 2 | 14 | **4** | open — environment half closed 2026-08-20 | — |
| B-102 | **A member can ship a coordination config whose generated snapshot is stale, and only the PARENT's CI notices.** Observed 2026-08-17 while releasing: B-065 added `SKILL-CARD.md` and `docs/evidence/specs/*carryover*.md` to `guardedFiles`, `docs/AGENT_SYNC.md` was not regenerated, and **`npm test` here stayed green through the whole release** — v1.69.0 was tagged, published to npm and smoke-tested with the snapshot already stale. What caught it was the umbrella's `validate` step *Coordination configs are checked, in every repository that declares one*, which runs `agent_sync.py check` in each submodule; it failed and `publish`/`release` were correctly skipped, so the umbrella published nothing. The asymmetry is the defect: the check exists once, in the parent, for a config that lives in the member — so a member's own gate cannot refuse a change the member made. `CLAUDE.md` already states the rule (*if the two disagree the tool is right and the file is stale, regenerate rather than edit*) and nothing here enforces it. Fix shape: `test/validate.py` runs `agent_sync.py check` where `.claude/agent-sync.json` exists, disclosing a skip when the tool is absent rather than passing silently — the parent's copy then becomes a second opinion instead of the only one. | stage 7 of v1.69.0, 2026-08-17 | S | 3 | 2 | 14 | **6** | open | — |
| B-103 | **A release checks out the tag to pin it, and the next commit lands on a detached HEAD.** Done here 2026-08-17: pinning the umbrella at the tag's own tree required `git checkout v1.69.0` in the submodule, and the coordination fix committed after it went onto a commit whose parent was the **tag**, not `main` — orphaning it from the two rows already on the branch. Nothing was lost (a cherry-pick moved it), but nothing warned either: `git status` says *HEAD detached from v1.69.0* and no gate reads that. The shape is worth a mechanism because the sequence that causes it is the CORRECT one — pinning the tag's tree rather than the branch tip is what `skills.json`'s version claim requires, so this will recur every release. Fix shape: the parent's release procedure returns the submodule to its branch immediately after staging the pointer (`git -C <dir> checkout main`), because the pointer is already recorded in the index and the working checkout no longer matters; and a member-side guard refuses a commit made on a detached HEAD unless the message says why. | stage 7 of v1.69.0, 2026-08-17 | S | 2 | 2 | 14 | **4** | open | — |
| B-104 | **The guard-count claim has no home between a tag and the next version bump.** `test/validate.py` reads the count from the newest `## vX.Y.Z` section of `CHANGELOG.md` and requires it to equal the workflow's — by its own comment, *"the TOP CHANGELOG section describes the release being cut"*. After a tag that section describes a release already cut, and the version-sync invariant forbids opening a new `## vX.Y.Z` heading without moving all five version surfaces. So work landing guards between a tag and the next bump has two options and both are wrong: rewrite a released sentence, or let the count go stale and the gate go red. Measured 2026-08-19 closing B-080 — eight plants took the workflow to 384 while the v1.71.1 section said 376 — and the remedy taken was a dated note INSIDE the released section, which is correct and is not where a claim about *now* belongs. Fix shape: read the claim from an `## Unreleased` section when one exists and fall back to the newest version section, so the number has a home that needs no version bump; the version-sync check keeps owning the heading. | closing B-080, 2026-08-19 | S | 2 | 2 | 0 | **4** | **closed 2026-08-20** — the check reads the TOPMOST `## ` section, `## Unreleased` or a version heading, and `## Unreleased` is required to sit **above** every version heading, because under a release it reads as a claim about the past and the check would never reach it. The version-sync check is untouched: it still finds the newest `## vX.Y.Z` anywhere in the file. Found live on the first guard this session added — the CHANGELOG said 390 against a workflow defining 392 with nowhere to write the new number, which is the exact bind the row describes. Two plants: a stale count inside `## Unreleased` (proving the section is read, not merely tolerated) and an `## Unreleased` block parked under a release. | — |
| B-100 | **The guard suite's cost is linear in plants × tree size, and it is already 38 minutes.** Measured 2026-08-17 from this repository's own run history: `17ef1a6` 19m · `b5c0ad6` 24m · `a6464f1` 23m · `bf264bd` 30m · `26ac6dd` **38m**. **366 of the 376 plants run `cp -R .`** (384 plants as of 2026-08-19, B-080) — a full copy of the tree, once per plant — so both factors only grow, and the ~25 plants added in v1.69.0 map exactly onto the 30m → 38m step. What breaks is not the CI budget (GitHub's ceiling is six hours) but the habit: a 38-minute gate stops being run locally before a push, and a gate nobody runs locally catches things one round trip later. This session ran `test:all` twice in eight hours and both runs found something — a rotted plant and a suite in no script at all — so the local run is not ceremony. Fix shapes by descending value: (1) copy once per job and reset between plants with `git checkout -- . && git clean -fd`, milliseconds against seconds; (2) shard 376 independent plants across a matrix, no change to any plant; (3) `cp -Rl` where a plant edits one file, cheap but silently wrong for a plant that edits in place. **Whatever is done, record the wall clock before and after on the same commit** — a change that should be faster and was never timed is the estimate this repository refuses to print as a measurement. | stage 7 of v1.69.0, 2026-08-17 | M | 2 | 3 | 14 | **6** | open | — |
| B-101 | **The same commit is validated twice per release, and the second run carries no new information.** `validate.yml` declares `on: push:` with no branch or tag filter, so a tag push re-triggers it on a SHA that has already passed. Observed 2026-08-17 while releasing v1.69.0: `validate` succeeded on `92fc3ea` at 14:15, and the `v1.69.0` tag push started a second `validate` on **the same `92fc3ea`** eleven minutes later, alongside `release`. At the measured 38 minutes a run (B-100), that is 38 minutes of CI spent re-deriving a verdict already in hand. A third run of the same tree happens inside `release` itself as *the suite this release claims* — that one is defensible, because a release should prove its own tree rather than trust a branch's history, but the tag-triggered `validate` proves nothing the branch run did not. Fix shape: `on: push: branches: [main]` plus `tags-ignore: ['**']`, or a `github.ref_type != 'tag'` condition on the job. Two lines. **Watch for the trap:** a `paths-ignore` filter would be the wrong instrument — this is not about which files changed, it is about the same commit being asked the same question twice. Distinct from B-100, which is about one run's cost rather than the number of runs. | stage 7 of v1.69.0, 2026-08-17 | XS | 1 | 2 | 0 | **2** | **closed 2026-08-20** — `validate.yml` declares `on: push: tags-ignore: ['**']`, which is the instrument the row named and not `paths-ignore`: the question was never which files changed, it was the same commit being asked the same question twice. `pull_request` is untouched, and `release.yml` still runs the suite on its own tree — that third run is the one that proves what shipped. | — |
| B-079 | **The graph-schema check refuses a valid schema that puts its rules behind a `$ref`.** `_conditionals()` recurses into `allOf` but does not dereference, so moving the two conditionals into `#/definitions/rules` and referencing them keeps the schema **fully binding** — a parked node with no reason is still rejected by `jsonschema` — while `test/validate.py` exits 1. It fails safe, which is why this is low and not high: nobody ships a broken schema on it. But it is a false refusal aimed at the one refactor a growing schema invites, and the author who hits it will be told the rule is missing when it is present. Measured by the `R-005` read of T-3, 2026-08-17. Fix shape: resolve local `$ref`s against the document before walking, and add the `$ref` form to the plants so the resolution itself is watched. | reader `R-005`, run `2026-08-17-t3` | S | 1 | 1 | 0 | **1** | **closed 2026-08-20** — `_conditionals()` dereferences through `_gderef` (the same bounded follower every other field check uses) on both the subschema and the `if`, with an identity set so a self-referential `allOf` cannot spin. **Proved by moving them, not by a fixture:** the shipped `graph.schema.json` now states all three node rules in `definitions` — `rule_check_unless_parked`, `rule_done_needs_evidence`, `rule_parked_needs_reason` — and `node.allOf` is three `$ref`s, so the deref branch is exercised by every run of the gate rather than by one test. `npm test` and `graph_test.py` (129 cases) both green on the moved schema. One plant removes the done-rule from behind its `$ref` and is refused; one property check asserts the schema keeps all three behind `#/definitions/`, so nobody can inline them and leave the branch untested again. | — |
| B-078 | **The manifesto's canonical URL does not resolve yet, so it is deliberately not shipped.** `https://podmanifesto.org` is the intended home of *Proof of Done*, and the operator asked for it in the README and the run's sign-off. Measured 2026-08-17: `curl` returns `Could not resolve host`, and `nslookup` against **both** `1.1.1.1` and `8.8.8.8` returns no answer — there is no A record. Shipping it would put a dead link in a README that goes to npm and GitHub, which is the one thing the manifesto's own documentation rule names by name: *a reference that does not resolve is a claim with its receipt removed*. The in-repo `MANIFESTO.md` is linked instead and resolves today. Fix shape: when DNS is live, re-run the two lookups, then switch both surfaces (umbrella `README.md`, `commands/task-pipeline.md` sign-off) in one change — the in-repo copy stays as the fallback. | operator request + measurement, 2026-08-17 | XS | 1 | 2 | 0 | **2** | **closed 2026-08-17** — DNS is live and the link was switched. Measured before switching, and by two resolvers because one answer is one resolver's opinion: `1.1.1.1` → `104.21.63.123`, `8.8.8.8` → `172.67.170.172`, and `curl -L https://podmanifesto.org` returns **HTTP 200**. The canonical URL now leads in the README with the repository named after it, and the in-repo `MANIFESTO.md` pointer stays as the fallback. The row existed because shipping a dead link into a README that reaches npm is the one failure the manifesto's own declaration names — so it waited on a measurement rather than on a plan, and closed the hour the measurement changed. | — |
| B-076 | **A judgement is recorded in the slot reserved for facts a machine established.** `pipeline.schema.json` enumerates exactly two gate types, `auto` and `manual`, and `references/gates.md` calls `auto` *«verify the check yourself»*. But a reviewer's ruling, a UX check that scenarios are coherent, and a UI check that a mockup is good have **no deterministic check** — they are judgement, and today they ride in `auto` indistinguishable from an exit code. Consequence: a coverage table cannot tell a measured row from an opinion, and the role-agent programme multiplies the problem (`ux`, `ui`, `reviewer`, `market-analyst` all produce judgement by design). Fix shape: a third type `judgment` that must name its judge and record its verdict AS judgement; `auto` then means only what a machine established. Guard + CI plant, like every other gate rule. | manifesto review, 2026-08-17 | S | 3 | 3 | 0 | **9** | **closed 2026-08-17** — `pipeline.schema.json` enumerates `auto · judgment · manual`, and a draft-07 conditional requires a non-blank `judge` on a judgment gate; `references/gates.md` carries the Axis A row and a section with three obligations — name the judge, record the verdict AS judgement in the artifact that quotes it, and never stand in for a `manual` authorisation. It generalises the rule `templates/verification.md` already applied to one column (a `review` verdict becomes `none` in `Auto`). Which of this pipeline's own gates are judgement is deliberately left to the operator, because the doctrine says gate assignment is theirs. The guard RUNS the schema over four probes; eight planted defects refused **on the first attempt**, including both rename shapes that had beaten every earlier guard this session. Guards 367 → 368. | — |
| B-077 | **`serves` does not resolve, and T-2's own DoD says it does.** The plan for T-2 promised *«every `serves` resolves»*; `graph.py violations()` checks the owner, dangling `blocked_by` and cycles — not this. `graph.schema.json` requires `serves` to be a non-empty string and nothing more, so `serves: "REQ-999"` and `serves: "asdf"` both pass every gate. That is the one edge joining the intent graph to the execution graph, and it is unchecked: a node can serve a requirement that was never frozen, and a frozen REQ can have no node at all, with nothing reporting either. A stated-but-unbuilt check is worse than an absent one — it reads as covered. Fix shape: the graph carries the frozen REQ ids (and the goal's clauses), `serves` must be one of them, and a `coverage` verb prints both directions — REQs with no node, nodes serving nothing. | manifesto review, 2026-08-17; T-2 DoD vs `graph.py:90` | M | 3 | 2 | 0 | **6** | **closed 2026-08-17 by B-085** — `requirements` on the graph is the resolution target T-2's DoD promised, `violations()` refuses an unresolvable `serves` with a near-miss hint, and the `coverage` verb prints both directions this row asked for plus a third. Watched: `serves: "REQ-999"` against a graph declaring `REQ-001` exits 1 and names REQ-999. | — |
| B-068 | **Three narrow guard defects the reviewer found after the release content was complete.** (1) `negatives.py`'s aggregate PASS sizes its claim off every test attempted, with no subtraction for those that resolved to `SKIP` — *all N provably reject* is then larger than what ran. (2) The release-suite guard wraps its YAML parse in a bare `except Exception` and falls back to a raw substring, so a malformed workflow silently returns to the weaker check it was written to replace. (3) `B-045` was marked a duplicate of `B-041` with the two still reporting "10" and "9"; the row stated both rather than reconciling, which defers a measurement instead of taking it. **(3) closed 2026-08-20** — re-measured at 9, B-041 kept the true figure, B-045 closed as the duplicate, and the count is now a registered claim class so neither row can restate it. (1) and (2) are open and were re-measured the same day: `negatives.py` still sizes its aggregate PASS off `len(tests)` with no subtraction for skips (`test/negatives.py` → the `all N guards provably reject` line), and the release-suite guard still falls back on a bare `except Exception`. | PR #38 review, 2026-08-12 | S | 2 | 2 | 19 | **5** | open — (3) closed 2026-08-20 | — |
| B-063 | **The working language of a run is not enforced anywhere.** Everything a run authors should be English — brief, REQ, spec, commits, docs, board rows, ledgers, issues — with non-English legitimate only as **data**: routing vocabulary, a quoted operator request, a test fixture, product copy. Measured 2026-08-12: 22 of 29 files carrying Cyrillic hold it only as data, so the distinction is computable, and a naive ban would break the Russian routing that measured 9/10 against 7/10 without it. One genuine leak exists — a historical spec in Russian prose, under a superseded banner, which the rule should exclude by name rather than silently. | stage 10 of `v1.48.0`, 2026-08-12 | M | 2 | 3 | 19 | **7** | open | — |
| B-064 | **A worked example is the executable half of doctrine, and nothing checks one.** Three times in one release a rule moved and its own example did not: GATE 5 held a container while GATE 6 claimed no container tooling; both stage-10 verdicts predated the `holds:` line they now mandate; teardown examples ignored the ledger grammar declared one file over. An agent copies the example literally and paraphrases the prose, so the example is what ships. | stage 10 of `v1.48.0`, 2026-08-12 | M | 3 | 3 | 0 | **9** | **closed 2026-08-17** — every `GATE <n> <name>: PASS\|FAIL` block in `references/` and `templates/` is read as a BLOCK (its line plus the indented continuation) and required to carry the `holds:` line `gates.md` mandates; three of seven did not and do now; and the page stating a mandate must carry a conforming example of its own. The block unit is the finding, not a detail: measuring by line said five lacked it, by block three did — and the line-wise measurement led me to append `holds: 0` to two blocks already saying `holds: 10`, which this check caught ten minutes later. Two values for one disclosure is worse than none, so the guard now refuses a repeated disclosure as well as a missing one — a rule learned from being broken by the change that introduced it. Six planted defects refused. Guards 368 → 369. | — |
| B-065 | **Two agents, one checkout, no lease — measured cost, not a hypothetical.** In one session: four version collisions (1.45.0, 1.45.1, 1.46.0, 1.47.0 each taken by other work mid-branch), a `files[]` entry silently dropped by a merge and caught only by the validator, and a red harness self-test caused by a probe copying the tree during another agent's write. `.claude/agent-sync.json` was added by v1.48.0 with six registers under lease; what stays open is the habit — a lease taken before the edit rather than after the collision, and the same config in the other projects this skill runs in. | stage 10 of `v1.48.0`, 2026-08-12 | M | 3 | 3 | 0 | **9** | **closed 2026-08-17** — all three halves measured. **Stale:** `idRegisters` is deliberately empty (the `fs` backend cannot reserve safely), and all **eight** family repositories carry the config. **Open and now closed:** the version-sync invariant names five surfaces and four were guarded — `SKILL-CARD.md`, whose omission had already surfaced on a release bump, plus the carry-over ledgers, are guarded now, and a validator check **discovers** every file declaring the current version and requires each to match a `guardedFiles` glob (watched catching a `registry.json` created for the test). **Not mechanisable:** the habit is `R-009` in `docs/evidence/retro.md` with its retirement condition — whether a write is *about to* happen is not a state a script can read, and the guard refuses an unleased edit at the edit, which is already too late to have avoided the race. Five planted defects refused. Guards 370 → 371. | — |
| B-066 | **A probe reports about its subject what actually happened to its environment.** Three times in one session: `timeout` absent on macOS returned exit 0 without running; a clone taken from a git worktree failed 93 probes because `.git` is a pointer file there; a probe said *the guard does not actually fire* when the guard had correctly stayed silent. Each message named the doctrine while describing the host. `res8` now has a three-branch form — fired, could not look, or neither — and it is the shape the rest need. | stage 10 of `v1.48.0`, 2026-08-12 | M | 2 | 3 | 19 | **7** | open | — |
| B-067 | **B-062's needle sweep reads 71 of 304 probes.** It extracts a literal `n = ` binding, so a probe whose needle is an `assert` or a regex is invisible to it — which is how `cn3` reached a 15-minute run to report a needle stale since another session's line rewrap. State the coverage beside the row, or the sweep reads as complete. | stage 10 of `v1.48.0`, 2026-08-12 | S | 2 | 2 | 19 | **5** | open | — |
| B-062 | **A probe's needle goes stale silently, and only a 15-minute run says so.** When doctrine is reworded, any probe whose needle quoted the old text stops landing. The probe itself is honest — it asserts the plant landed and reports BROKEN rather than passing — but the discovery costs a full negatives run. Measured 2026-08-11: a one-line reword of acceptance.md criterion 13 broke `res5`, found after ~15 minutes; a 20-line script that extracts every probe's literal needle and checks it against its target file answered the same question for **all 67 simple-needle probes in two seconds**. Make it a validator guard so `npm test` says it. | stage 10 of `residue-and-honesty`, 2026-08-11 | S | 2 | 3 | 20 | **7** | open | — |
| B-061 | **Nothing measures which doctrine a run actually read, and the bundle is 127k tokens.** Measured 2026-08-11: `SKILL.md` 7 912 tok plus 32 reference files at 118 842 — `stages.md` alone 13 668. The routing surface fits in 1008 characters; the doctrine behind it does not fit in a context window. There is a reading floor naming what is mandatory, and `unlooked: N` for checks that did not look — but no equivalent for doctrine a run skipped. A skipped file and a read one are indistinguishable, which is the same class every guard in this repo exists to catch. | stage 10 of `six-lessons`, 2026-08-11 | M | 3 | 3 | 0 | **9** | **closed 2026-08-17** — a `PostToolUse` hook on `Read` appends `read: references/<file>.md` to the run ledger, deduplicated and always exiting 0; `scripts/graph.py doctrine` reports the count and names every unread file. The load-bearing part is what it **refuses** to print: with no ledger or no `read:` lines it says `unmeasured` and why, because the hook being absent and the run reading nothing are opposite facts the ledger cannot separate — `0 of 34` would be the reassuring answer to a question nobody asked. A disclosure with no floor and no target. **No per-file reading floor was invented**: stage 0's mandatory items are the floor that exists and they are not per-file, and declaring one inside a measurement would smuggle a doctrine decision in as a count. Two existing guards caught the change as it landed — a relative link in a seeded template, and a ledger shape with no reader. Seven planted defects refused. Guards 369 → 370. | — |
| B-058 | **A claim that leaves the repository has no guard behind it.** Every check here reads the working tree; an issue comment, a release note or the npm description is written at stage 7 and no validator can see it. Proven the same day: the comment closing the issue that taught *delete the hand-written figure* quoted a probe count from memory and was wrong, twenty minutes after the lesson shipped. Either outward text names what measures each number, or it carries none. | stage 10 of `six-lessons`, 2026-08-11 | S | 2 | 3 | 20 | **7** | open | — |
| B-059 | **The standing-instructions `Last fired` column is derived, and nothing derives it.** Measured: four of five rows were stale — the stamps recorded R-002, R-003, R-005 and R-006 firing on 2026-08-11 while their cells said 08-08 and 08-10. The retirement trigger reads the stamps, so nothing retired wrongly, but a reader trusts the cell and at the cap of ten the coldest-looking row is the one that goes. Compute it from the stamps or delete it; two homes for one fact is the defect. | stage 10 of `six-lessons`, 2026-08-11 | S | 2 | 2 | 20 | **5** | open | — |
| B-060 | **The Recent log's heading claims a bound the section does not have.** It read *entries from the last five run stamps* and held considerably more. The doctrine above it is correct — the log is queried, not read, and is deliberately uncapped — so the heading is what was false. It was a heading like this that made the stage-0 reading floor overstate what was bounded. **`B-069` is this row's second filing** and is collapsed into it: same heading, same window, filed three days apart by two runs, and the file itself disclosed the defect in its own honest-gaps section — three records of one finding and no fix. | stage 10 of `six-lessons`, 2026-08-11 · re-filed as `B-069`, 2026-08-14 | S | 1 | 2 | 0 | **2** | **closed 2026-08-20** — the heading now reads *narrative entries, uncapped and queried rather than read*, in the live retro **and** in `templates/retro.md`, which seeded the false bound into every host project; `references/retrospective.md`'s source row says uncapped by design and records what it said before. The rename was deferred once as *renaming a section four guards may key on is its own change* — measured: no guard keys on the heading's wording, one keys on `## Recent log` and it still matches. A guard now refuses a numeric bound in that heading, in digits or in words, in both files; the plant was watched failing in both. Counted at the close: 25 entries under a heading that claimed five. | — |
| B-001 | **The description's headroom is the budget every future addition pays from.** Re-measured 2026-08-31: **897 of 1024**, 127 spare — the row said "1008 of 1024, 16 spare" until this check, then 962 after the findings classes were named. It stopped being a hand-written number in the same pass: the claim registry has a **description budget** class, so `N of 1024` and `N spare` are computed from `SKILL.md`'s own frontmatter on every `npm test` and this row cannot go stale again. The shipped doctrine is ~113k tokens over 38 reference files; the next companion must displace text, not append it | `08-08-audit-followup` #2 · re-measured `2026-08-31` | M | 2 | 3 | 23 | **7** | open | — |
| B-002 | **Zero blind eval runs on zero of three models.** The skill's behavioural evidence is one self-check by its author; `evals/RESULTS.md` states the split honestly, which is reporting the gap, not closing it | `08-08-audit-followup` #6 · `08-03-default-routing` #C-1 · `08-03-setup-and-autonomy` #C-1 | L | 2 | 3 | 23 | **7** | **closed 2026-08-31** — three blind trigger runs recorded, one per model (haiku 39/42, sonnet 40/42, opus 42/42), 147 fresh-subagent probes over three suites; method and limits in `evals/RESULTS.md` → the 2026-08-31 headings, ratchet re-derived to 3-of-3 models blind. The INSTR/scenario half stays honest: recorded as not reproducible from this harness, so instruction-following remains observed-only — that residue lives in the RESULTS files, not in a reopened row | — |
| B-006 | `npm run test:negatives` was **flaky**: failed 20, then 4, then passed 114 twice — never diagnosed. **Observed 2026-08-20: one run of the full suite, 390/390 first try, no retry.** That is recorded and it closes nothing — a single green run against an intermittent failure is the observation this row exists to distrust, and treating it as a diagnosis is the same substitution as reading a passing gate as a probed one. The row keeps its age bonus. What would close it: a named cause, or a run count large enough to bound the rate, with the bound stated. | `08-06-ci-run-verified` #3 | M | 2 | 2 | 25 | 5 | open | — |
| B-008 | Promote the graph-staleness measurement from doctrine (rung 2) to a script (rung 3) | `08-06-graph-staleness` #2 | S | 1 | 2 | 25 | 3 | open | — |
| B-010 | The validator re-reads the same files from disk — **25 reads of `audit.md`, 668 `.md` opens per run**, measured by instrumentation against an estimate of "3+" | `08-08-audit-followup` #12 | S | 1 | 2 | 23 | 3 | open | — |
| B-011 | `grill.md` has no *"is this worth doing at all"* question, and the run that noticed deliberately did not add one | `08-05-spec-plan-quality` #C-002 | S | 1 | 2 | 26 | 3 | open | — |
| B-015 | Nothing tests the routing rule **as a rule** — that a task of the shape the rule describes actually reaches this pipeline | `08-03-setup-and-autonomy` #C-2 | M | 1 | 3 | 28 | 4 | open | — |
| B-024 | **The stage-10 coverage table has no template and its shape has already drifted.** Ten acceptance files in this repo, and the first REQ-bearing table differs in almost every one — ladder walks and coverage tables share a file with different columns. `acceptance.md` defines the shape in prose only, which is precisely how the carry-over ledger reached six header shapes | `2026-08-09-planning-system` / N2 stage 0 | M | 2 | 3 | 22 | **7** | open | — |
| B-024b | **A string substitution that matches nothing says nothing.** Three times this session an edit targeted a shape the file did not have — an import written on one line, a doctrine phrase worded differently, a probe pattern for rows that had changed — and each no-op was invisible until something downstream failed. The class is not the typo; it is that `replace` has no failure mode | `2026-08-09-planning-system` / N3 stage 5 | M | 2 | 3 | 1 | **6** | open | — |
| B-022 | **A guard read a table column by position and was wrong in half the corpus.** Ten ledgers, six header shapes, five with two status-ish columns. The class is wider than one guard: any check that locates data by column index over a hand-written corpus has the same blind spot, and it fails by passing | `2026-08-09-planning-system` / N1 stage 7 | M | 2 | 3 | 22 | **7** | open | — |
| B-016 | The term-index check has a false-positive budget that was never measured over a real corpus — `learned.md` rule 10 says measure a detector before trusting it | `08-03-setup-and-autonomy` #C-3 | S | 1 | 2 | 28 | 3 | open | — |
| B-043 | **`stages.md` is 13 161 tokens and is the gate list every run must satisfy.** Splitting it into a spine (gates + criteria) and per-stage detail loaded on entry is the remaining half of the reading-floor work | `2026-08-10-skill-audit` | M | 1 | 3 | 21 | 4 | open | — |
| B-044 | **Duplicate of B-040 — the same finding recorded twice; collapse at next triage.** **340 `never`s across 77 230 words — one per 227 words — with no ranking function.** Reserving the absolute form for acts that lose work or ship a false claim needs a sweep of 33 files and a re-measurement, not a search-and-replace | `2026-08-10-skill-audit` | L | 1 | 3 | 21 | 4 | open | — |
| B-045 | **Duplicate of B-041 — the same finding recorded twice, and the two disagreed on the number: this row said "10 registered claim classes", B-041 said "9".** Re-measured 2026-08-20 at the collapse: the registry held **9** classes, so B-041's figure was the true one and this row's had been false since it was written — the row about hand-written counts, carrying a hand-written count that was wrong. | `2026-08-10-skill-audit` | M | 1 | 2 | 0 | 2 | **closed 2026-08-20 as a duplicate of B-041** — collapsed, not deleted: the finding lives in B-041 with the number that was right, and the disagreement is recorded here because a duplicate resolved in silence is how the wrong number survives the merge. The registry has since grown (three classes added the same day: **description budget**, **description headroom**, and **registered claim classes** itself), and neither row restates the size any more: it is computed and printed by `npm test` beside the verdict. | B-041 |
| B-040 | **One `never` per 227 words across 77,230 words of doctrine.** 340 absolutes with no ranking function: an agent cannot tell which prohibition is load-bearing, so selective compliance looks arbitrary from outside | `2026-08-10-skill-audit` | M | 1 | 3 | 21 | 4 | open | — |
| B-046 | **The trigger vocabulary is named but not tested — half closed.** v1.39.0 took implicit `should_trigger` coverage from **two** cases to **eight** (TRIG-03/04 plus the four findings classes), so the claim this row was filed with no longer holds. What remains: six of the eight BUILD nouns still have no eval reaching them without the invocation, and `AMB-01` is still filed `ambiguous` on purpose — the suite admitting it does not know how *"clean up the error handling"* should route. Blocked behind `B-002` for the behavioural half: vocabulary is not behaviour | `2026-08-10-routing-taxonomy` · re-measured `2026-08-10` | M | 2 | 2 | 21 | 5 | open | — |
| B-047 | **Findings-shaped work now has an entry, and three classes of four reach it.** v1.39.0 restated the boundary as *what a request ends in*. Measured on fresh agents (`evals/routing/`): bug hunt and PR review moved `none` → routed in **both** after-runs, and the audit's *reason* stopped resting on a stretched `hardening`. **Not fixed: the production check never routed, 0 of 2** — in the second run the agent said the request has *'no repository change and no matching trigger'* and reached for `checkup` before rejecting it. Adding `/проверь прод` was necessary and not sufficient, so the class is **named and still unreachable**; R-006 says that is reporting, not a fix | `2026-08-10-routing-taxonomy` · `2026-08-10-findings-entry` | S | 2 | 2 | 21 | 5 | open | — |
| B-048 | **"Decide, don't build" has no contract.** «продумай фичу» wants stages 0–4 and a stop; the bundle has the stages (brainstorm at 2, spec at 3, plan at 4) and no way to ask for them without the six that follow. The short-path triage (`stages.md:143`) shrinks a run, it does not truncate one, and it is proposed for *small* work — the opposite case. Absent this, the request either becomes a full run nobody asked for or falls out of the pipeline into an ungated conversation | `2026-08-10-routing-taxonomy` | M | 2 | 2 | 21 | 5 | open | — |
| B-049 | **Runtime observability is not a harvest source.** `knowledge-sources.md` ledgers ten sources and all ten are written artefacts; logs, traces, error trackers and dashboards appear nowhere. The only runtime reads in the bundle are `artifacts.md:144` (stage-8 notes, explicitly *not a committed file*) and `deploy-targets.md:78,114` (a per-platform log command inside the runbook). So «сходи посмотри логи» has no row to be: a run cannot cite what it saw in production, date it, or mark it stale — the contract every other source is held to | `2026-08-10-routing-taxonomy` | M | 2 | 2 | 21 | 5 | open | — |
| B-050 | **Pull requests exist only as steps inside stage 7, never as requests.** Opening one is a line in `stages.md:400` and an outward-authorization rule in `conventions.md:125`; reviewing one is `review.md`, bound to a run that is already at stage 7. Neither «сделай PR» nor «проверь PR #24» reaches anything, and the second is the more expensive miss: `review.md` carries the three reader states and the `NO READER` rule that v1.38.0 shipped, and a standalone review request gets none of it | `2026-08-10-routing-taxonomy` | S | 2 | 2 | 21 | 5 | open | — |
| B-051 | **Companions are bound to stages, never to request classes.** `companion-skills.md` answers *"I am at stage 3, what do I reach for?"* — super-ux and sheleg-design at 3, context7 at 1, obsidian-wiki at 0 and 9. It never answers the question an operator opens with: *"I want an audit / a bug hunt / a PR review — what runs, and what does it pull in?"* Nothing binds «проверь ошибки» to `systematic-debugging`, «сделай аудит» to the L0→L7 ladder, or «проверь PR» to the reviewer contract. Depends on `B-047`: a class with no entry cannot have a companion bound to it | `2026-08-10-routing-taxonomy` | M | 2 | 3 | 21 | **7** | open | — |
| B-052 | **The routing harness cannot separate an effect from noise — one agent per query.** Three runs of the same ten queries scored 7, 9 and 8; one control flipped on a clause the edit never touched, with reasoning that does not parse. Had the run stopped at the second, `RESULTS.md` would have reported a clean 9/10 resting on single samples exactly like the one that moved. Needs N≥3 per cell majority-scored, controls scored apart from targets, and the *reason* recorded — every real finding in that file came from why an agent chose, not what | `2026-08-10-findings-entry` · `RESULTS.md` §3 | M | 2 | 3 | 21 | **7** | open | — |
| B-053 | **A production check needs a mode, not a synonym.** The vocabulary fix did not move it (`B-047`, 0 of 2). The refusals name the reason: the request has no repository change and no artefact to point at, and `checkup` — the nearest thing — reports what shipped unverified, not whether the running system is alive. Shape to design: a no-task mode that reads the CI verdict, the platform's own log command (`deploy-targets.md:114`) and the verification ledger, and writes rows rather than an answer. Blocked by `B-049`: a production check has nowhere to cite from until runtime observability is a harvest source | `2026-08-10-routing-taxonomy` · `2026-08-10-findings-entry` | M | 2 | 3 | 21 | **7** | open | — |
| B-054 | **The loop has a contract now; nothing in the skill still STARTS it.** v1.40.0 shipped the queue (stage 2's module map), `run.loop.arm`, `mode: dynamic` and the goal re-read between items, all guarded. What remains is the half the occasion was about: a run that should loop still depends on the operator invoking `/loop`, or on the agent choosing to — `arm` says *where* a recorded mode is armed, not who arms it, and Part 1a deliberately refused to make arming unconditional because that would override `Default off`. The open question is whether a run may arm its own harness primitive when the mode IS recorded, and what it prints when it cannot | `2026-08-10-findings-entry` · v1.40.0 | M | 2 | 3 | 21 | **7** | open | — |
| B-056 | **Playwright MCP / agent-cli as a second browser path, beside chrome-devtools.** The bundle already names chrome-devtools MCP for stages 5–6 and 8 — *verify the rendered surface rather than the diff* — and nothing else. Operator supplied the Playwright MCP docs, the CI intro, the agent-cli intro, the `class-playwright` API and both `microsoft/playwright-mcp` and `ChromeDevTools/chrome-devtools-mcp` tool references (2026-08-10). To decide: whether Playwright is a **second** recommended companion or **the** one for projects that already run it in CI; what stage 6's *checked in a browser, not in the diff* means when the check is a Playwright spec that CI runs rather than an agent driving a page; and whether structured accessibility snapshots change what the rendered-surface claim is worth. **Not started; a pasted note of ~6 lines did not reach the agent and may hold requirements** **Closed 2026-08-14 → `DEC-0004`, v1.55.0.** All three sub-questions answered: a second channel ranked by nothing; a browser test suite is the coverage half and never the look; accessibility snapshots change the cost of a look, not its worth. The pasted note never arrived and the questions were re-asked instead of guessed. | operator, 2026-08-10 | M | 2 | 2 | 0 | 4 | closed | — |
| B-057 | **A check answered by text that is not its subject — the silent half.** v1.42.0 shipped the **neighbour probe** (`gates.md`): break the subject, plant the guard's own evidence next door, require it to still fail. Three shipped, one per guard that fell this way, and the third caught its guard on its first run. **Correction to this row as filed:** half (a) — a probe keyed to a stale literal — was already mechanised: `negatives.py` runs `differs_from_repo` on every planted copy, so those fail loudly, which is what all four stale needles this session actually did. What remains: **only "three guards of 253" have a neighbour probe** — a citation of the 2026-08-11 measurement, not a count of now; `DEC-0002` carries it with its date, and the suite has grown since — and nothing computes which of the rest read a scoped span — that list cannot be discovered from the code as written, which is the real work **Work-list, 2026-08-11 (`DEC-0002`):** the premise *cannot be computed* is withdrawn — 55 scoping sites are statically detectable (10 named helper · 4 inline slice · 8 `re.split` · 27 split-then-index · 6 search-offset) against 350 `fail()` calls. They are the **population to sample from, not a defect count**; published as debt the number manufactures busywork. | this session, 2026-08-10 · v1.42.0 | M | 2 | 3 | 21 | **7** | open | — |
| B-041 | **92 hand-written counts in prose against 15 registered claim classes.** Most are true today; none of the 83 is guarded, in the repository whose loudest canon is compute-never-restate. The row said "9" until 2026-08-20, when B-045 was collapsed into it and the count itself became a registered class — so this figure is now computed on every `npm test` rather than restated here. Three of the twelve were added that day: **description budget**, **description headroom** and this one | `2026-08-10-skill-audit` | M | 1 | 3 | 21 | 4 | open | — |
| B-069 | **Duplicate of B-060 — the same heading, the same window, filed three days later by a different run.** It read the drift the other way round (rotate the excess) where B-060 read it as a false heading; the doctrine settles it — the log is uncapped **by design**, so there is no excess to rotate and the heading was the defect. Recorded rather than deleted: two runs and one self-disclosure produced three records of one finding and no fix, which is the shape the board exists to collapse. | run `2026-08-14-playwright-browser-channel` | S | 2 | 1 | 0 | 2 | **closed 2026-08-20 as a duplicate of B-060** | B-060 |
| B-070 | **A matrix row whose first cell is not literally `**bold**` is invisible to every check over that table.** The reader `R-005` dispatched planted a row naming a fictional browser companion with the link brackets OUTSIDE the bold rather than inside, and the validator passed: the row vanished from the matrix→stages check, from **both** directions of the preflight cross-check, and from the new cell-count guard at once. A row with no bold at all does the same. The file already carries three rows written bold-wrapping-link, so the inversion is a plausible slip that silently removes a companion from every check. The fix is one anchored pattern shared by the three readers, not a fourth regex. | reader `R-005`, run `2026-08-14-playwright-browser-channel` | M | 2 | 3 | 17 | 7 | open | — |
| B-071 | **The table's readers parse across newlines and the cell-count guard cannot.** The cell pattern matches a newline, so both readers happily consume a wrapped row, while the new guard iterates `splitlines()` and sees only the first physical line. A bolded row wrapped over two lines with the stray pipe on the continuation line passed the validator and derived no stages — reproduced by the `R-005` reader. Either the guard joins continuation lines the way the readers do, or the readers stop at a newline; the two must agree about what a row is. | reader `R-005`, run `2026-08-14-playwright-browser-channel` | S | 2 | 2 | 17 | 5 | open | — |
| B-072 | **Nothing compares the browser doctrine across the surfaces that mirror it.** The matrix→stages check reads `references/stages.md` only. The `R-005` reader rewrote `SKILL.md`'s stage-6 gate row to name a single channel, and deleted the `playwright` channel from `cursor/rules/task-pipeline.mdc` — the validator passed both times. `README.md` and `SKILL-CARD.md` are equally unguarded. v1.55.0 doubled what these four surfaces have to keep in step, which is what makes the gap worth closing now rather than at the next channel. | reader `R-005`, run `2026-08-14-playwright-browser-channel` | M | 2 | 3 | 17 | 7 | open | — |
| B-073 | **No check separates a recipe from an anti-recipe in the same section.** The mechanism guard proves `browser.md` carries the whole look in one runnable fence, in the section the stages point at. The reader `R-005` dispatched then wrote *"the commands below are the ones this file tells you never to run"* above a fence holding all four, and it passed — three drafts of the pattern all did. The gap is real and is not regex-shaped: it is prose contradicting its own example, which is what a reader catches and a needle cannot. Either an assertion the doctrine states positively somewhere a check can read, or accept that this class ships with R-005 as its only detector and say so at the gate rather than in a code comment. | reader `R-005`, run `2026-08-15-browser-mechanism` | M | 2 | 2 | 16 | 5 | open | — |
| B-074 | **A stage can demand the look without naming a channel, and no guard looks at it.** The mechanism check only examines stage sections that mention `playwright` or `chrome-devtools`. The `R-005` reader planted, in this repo's own house phrasing, *"the preview build is checked in a browser, not in the diff"* into stage 7 — no channel named, no pointer, validator green. Stages 5, 6 and 8 are covered only because the matrix→stages check independently forces the channel names into them. Content before the first stage heading is invisible for the same reason. | reader `R-005`, run `2026-08-15-browser-mechanism` | S | 2 | 2 | 16 | 5 | open | — |
| B-075 | **Two concurrent runs of `test/negatives.py` corrupt each other's results, and the corruption reads as a doctrine failure.** Every step copies the repo to a **fixed** `/tmp` name, so a second suite started while the first is running overwrites scratch mid-copy: measured 2026-08-15 across four overlapping runs which reported 1, 2, 3 and 4 guards *not firing* plus 0, 0, 2 and 4 property checks silent — four different answers about an unchanged tree, none of them about the tree. The runner's own comment already says *"collisions only happen between two SUITE runs, which is a different row"*; this is that row. Two hypotheses were tested and disproved before the cause was found (325 steps, 325 distinct paths, 0 duplicates; 0 parent/child nestings), which is the cost of not having it filed. Fix shape: derive each step's scratch path from a per-run token, or take a lock. **Sharpened 2026-08-17:** this and B-080 together are the manifesto's *«requirements with no evidence path»* measurement made impossible at the node level, and the proposed `coverage` verb is step one of B-085 rather than a separate build — one fix closes both obligations. **Sharpened 2026-08-17:** the precedent already exists in miniature — `templates/verification.md:27-31` turns a coverage verdict of `review` into `none` in the `Auto` column, because that column records what a machine established. Generalise that rather than invent it. A `judgment` type is also the mechanism an acceptance policy needs to say what independence means at each risk level (B-091), and the input the missing **uncertainty** axis needs (B-095). | 2026-08-15 graph audit, F-8's baseline | S | 2 | 2 | 0 | **4** | **closed 2026-08-16** — the runner scopes every `/tmp/...` path in a step's script to its own pid before executing, so the copy, the heredoc inside it and the sweep all agree. The fix is in `negatives.py` and not in the 344 workflow steps: the steps are the CI contract, one runner per job, and they were never the ones colliding. **Watched both ways under real overlap** — without it, two concurrent runs of the same selector returned `1 did not fire, 7 broken` and `8 did not fire`; with it, both returned `all 8 guards provably reject their planted defect`, exit 0. | — |
| B-105 | **The record behind the manifesto's opening story framed two observations as one instant.** `references/residue.md`'s first section — the source of the Proof of Done manifesto's opening claim, permalinked from it — said *“One minute later the harness task inventory was queried”* directly above a `ps` line whose `etime` reads `03:12`, and nothing outside the fenced block reconciled them. Neither number was wrong: both anchor to the moment the monitor was armed, about two minutes apart. But the record that teaches *prove no more than you observed* was the one asking to be interpreted, and a reader following the receipt landed on the discrepancy. `git log --follow` returns four commits and the block is byte-identical in all of them back to `298abb6` — there is no earlier variant to recover, the framing shipped this way at birth. Fixed by stating both moments, the interval, and what the record never carried (the wall-clock times, not recoverable from anything it kept). Nothing it claims changed. Same pass: the neighbouring E4 measurement said *“they have been up for three days”* over a block reading *“oldest 3 days”* — now *“the oldest of them”*. | TP-02 · M-40, manifesto conformance, 2026-08-19 | XS | 2 | 2 | 0 | **4** | **closed 2026-08-19** — both records now agree with their own receipts, and three guards say so: the `etime` in the `ps` block must be accounted for by a sentence outside it, the two-moments framing and the wall-clock limit are guarded as sentences, and the containers count in the block must equal the count in the prose that carries it outward. Five plants watched refused. | `f0402c2` |
| B-111 | **A node was closed by one reading, and a whole class of defect is invisible to one context.** A verifier reads the diff it was handed — so a change that is correct where it was made and wrong one level out closes green: a caller's contract that moved, a second implementation of the same rule that did not get the fix, a documented behaviour now false, another feature reaching the same path. Closed 2026-08-20 in v1.74.0: `graph.py certify` requires one report each from `unit`, `seam` and `product`, dispatched blind and in parallel, all three passing, and assembles the seven-key verdict `close` already consumed — `close`'s contract unchanged. A tier cannot pass on an empty `scope` or while carrying a `breaks` finding; a failing round records itself and leaves the node open; the ceiling names the tier failing every round rather than halting. 20 fixtures, and `npm run test:certify` disables each of the 16 rules and requires its own fixture to notice — 16 of 16. | operator request, 2026-08-20 | L | 3 | 4 | 0 | **12** | **closed 2026-08-20** — the three tiers ship as agents beside `verifier.md`, doctrine in `references/certification.md` | `certify` |
| B-112 | **The three tiers are dispatched by instruction, and nothing checks that a run actually dispatched three.** `certify` refuses a certification that arrives with fewer than three reports, which is the half that can be mechanised — but a run may still close a node by writing one verdict by hand and calling `close --verdict` directly, which is the path `verifier.md` deliberately still documents for a verdict already in flight. So the gate is complete and the OBLIGATION to use it is doctrine. What would close this: `close` refusing a verdict for a node whose `certification.round` is absent, with a named escape for the in-flight case. Filed rather than done because that flag is a migration for any graph mid-run when it lands, and this release already changes how every node closes. | B-111 close, 2026-08-20 | S | 2 | 3 | 11 | **6** | open | `graph.py close` |
| B-119 | **`certification.md` decoupled the three readers from each other and never decoupled the fixer from the grader.** Tier blindness is horizontal — no tier reads another's report — and nothing said the agent that produced a change may not be the agent that certifies it. The saving that removes it is real (the fixer already holds the context) and it converts certification into self-assessment, invisibly: every report it writes stays sincere. Separately, the *two severities and no third* rule was stated without its scope, and binary pass/fail measures **0% detection** of regressions in a non-deterministic workflow where trace fingerprinting reaches **86%**. | 2026-08-31 harness-engineering harvest, queue positions 9-10 (both ARCH) | S | 3 | 3 | 0 | **9** | closed | `references/certification.md` |
| B-120 | **`task-pipeline`'s SKILL.md body sits at 4749 of the 4750-token working limit — one token — and nothing in this repository's gate measures it.** The second member found in that state today; `agent-stack` filed the same as its B-129 and the auditor's own remedy is *a split, not a trim*. Found by running `make-skill`'s auditor by hand during an unrelated release, which is the only way it surfaces here. | 2026-08-31, observed while shipping B-114 | M | 2 | 2 | 0 | **4** | open | `test/validate.py` and `plugins/task-pipeline/skills/task-pipeline/SKILL.md` |
| B-121 | **The validator accepts a guard-count form the CI plant cannot land on, so a release passes locally and fails in CI on formatting.** `validate.py` reads `Guards: N → M` with the second number optionally bold; the negative self-test in `validate.yml` first probes that same loose pattern to decide whether to INJECT a count, then mutates with a strict one requiring `→ **M**`. A count written without the bold satisfies the loose probe — so nothing is injected — and then fails the strict search with `PLANT DID NOT LAND: no guard count in the TOPMOST section`. Observed 2026-09-01 on `fix/b119-dupids`: `npm test` → `rc=0` locally, CI red. The class is this repository's own recurring one — the check that ran was not the check that mattered — and here the two checks are a local gate and its own CI plant disagreeing about a format neither documents. Wanted: one pattern, defined once and read by both, or a `validate.py` refusal when the count is present in a form the plant cannot mutate. | 2026-09-01 CI, this run | S | 2 | 2 | 0 | **4** | open | `test/validate.py` (the reader) and `.github/workflows/validate.yml` (the plant) |
| B-134 | **A release here is two merges whose ORDER is load-bearing, and nothing checks it until the tag exists — three tags burned in one day.** A run stamp must cite a commit inside the new tag's OWN range, and that commit does not exist until the payload merges, because the ruleset rebase-merges and mints every SHA at merge time. So the protocol is: payload lands as `X`, a SECOND change stamps the run citing `X` and lands as `Y`, tag `Y`. Cut the tag on `X` and the release is unstamped. `validate.yml` ignores tag pushes and `release.yml` is the first thing to run on the tag's own tree, so the branch is green either way and the cost is always a burned version — the ruleset forbids deleting a tag. Paid three times on 2026-09-01: `v1.82.0` (stamp written in the sibling repo's bullet shape into a section this gate reads as a table), `v1.82.1` and `v1.82.2` (tag cut on the payload commit, no stamp for the release's own run) — the third by the run that had just written the rule into the CHANGELOG. Wanted: something that refuses BEFORE the tag — a `release:preflight` script that resolves the range the tag would create and asserts a stamp falls inside it, run by the release checklist and by `validate.yml` against the version in `package.json`. **A fourth mechanism, 2026-09-01:** `v1.82.3`'s stamp was correct and in the right order, and the release still failed — on a negative self-test added in the same programme. `test/negatives.py` locates a plant's scratch directory by searching its script for a literal `cp -R . <dir>`, then diffs that copy against the repository to tell a real defect from a no-op; the duplicate-id plant copied ONE file and mutated the live tree, so the harness could not confirm it landed and reported the guard as one that did not fire. `npm test` cannot see this, because `negatives.py` is a CI step and not part of the suite. So the preflight this row asks for must run the PLANT HARNESS as well, not only `npm test` — four dead tags, four different mechanisms, one shared shape: a green local gate that does not cover what the release runs. **A fifth instance, 2026-09-02 — caught before the tag was pushed, and the first one that cost nothing.** The repair for `v1.82.3` was green on all three branch checks and would have burned `v1.82.4` on the release-gap check: it declared no stamp for the release it was itself creating. A PR repairing burn N is STRUCTURALLY blind to burn N+1, because the check lives on a tree the branch never has. What caught it was cutting the tag locally and running `npm run test:all` in a worktree at the tag, then deleting the tag on the failure — so the preflight this row asks for already exists as `test:all`, and what is missing is not a new check but running the existing one on the tag's own tree. `npm test` is three of its thirteen. | 2026-09-01, three failed releases | S | 2 | 3 | 0 | **6** | open | `.github/workflows/release.yml`, `test/validate.py`, and the stage-10 checklist |

## Closed

Rows leave the table above only into this list, one line each, with the commit.

- **B-113 · Negative-test anchors are pinned to literals that the releases they guard move** — the class, not the four instances. `test/anchors.py` reads all 422 negative self-tests out of the workflow and answers the row's own question from the AST: a needle is only what a plant reads out of a file *before it writes that path*, so provenance is tracked from `open(...).read()` through derived names, read-backs are tracked **per path** (a plant-wide "after the first write" rule exempts `pf1`'s second file, which is a genuine anchor), regex shape is stripped, and a grep over the validator's own output is assertion 3 rather than a needle. **Counted, not estimated: 26 of the then-419 plants pinned a value a release can move.** The row's own figure of 27 came from a grep that both over-reported (quantifiers, `\uXXXX` escapes) and under-reported — it missed `bd5`'s single-digit `| 1 | **6** |` entirely. 25 of the 26 derive their anchor at run time now; one declares instead (`res15`'s `^13\.`, because the criterion number is the *guard's* contract and deriving it would prove some other criterion's guard fired). Three were pinned to LIVE board rows — `bd10` on `B-008`, `tbl`/`tbl2` on `B-015` — and would have stopped landing the day those rows closed. **Dormant stopped reading as a pass:** `test/negatives.py` folded every `SKIP` into *all N guards provably reject*, and now names them in their own block and reports *N of M*; all four skip-capable plants declare `# dormant-when:`. The class is refused in the **cheap** gate rather than found by the 35-minute suite a release later, and four checks watch it fail (`anc1` a re-pinned needle, `anc2` a removed dormancy declaration, `anc3` a declaration that resolves to nothing, plus a property check that the census prints its disclosure) beside 47 whole-workflow fixtures in `test/anchors_test.py`, 27 of them watched firing — and the 18 that assert silence are all retractions of the detector's own drafts.  **THE LOOP GUARD TRIPPED ON THIS ROW, and the trip is the finding.**
`build.md:417` says to stop before the round cap when *the same file returns for the same
reason*, name the two shapes and escalate to the layer that owns the conflict. This closure
line returned twice with the same stale fixture count — round two's finding #1 and round
three's C-1 — while narrating that exact defect as the row's lesson. Touch log:
`touch: docs/evidence/backlog.md — round 2 — reason: stale fixture count`,
`touch: docs/evidence/backlog.md — round 3 — reason: stale fixture count`. The two shapes:
**(a)** a document that teaches a lesson about a restated count *by restating one*, so the
sentence a reader trusts most is the sentence most likely to be wrong; and **(b)** a fix
applied by APPENDING a corrected sentence rather than correcting the one already there,
which leaves both in the file and lets the older one survive every later round. The layer
that owns the conflict is not this document — it is canon 8, and the escalation is
mechanical: **`anchor-census fixtures` and `anchor-census fixtures watched firing` are
registered claim classes now**, computed from `test/anchors_test.py`, so a fifth occurrence
is a failing gate rather than a reader's patience. Registered classes 13 → 15.
**The lesson this row taught inside its own fix, stated because it is the row:** the
count moved four times before it was true — 27 (the row's grep) → 21 (a census blind to
`enumerate`, `pathlib` and `re.compile`) → 24 (the reader's figure) → **26** — and the
fixture count was written 18/8 in one sentence and 35/20 in the next, then left at 18/8
in this very paragraph's own document for one more round, and the fixture total moved
35 → 44 → 47 as three reader rounds added cases. A detector is a
claim about a corpus, and its author is the last person who can measure its reach; R-005
is why any of those numbers is right. **What this does NOT close, named rather than
implied:** a needle that goes stale because prose was *reworded* carries no digits and this census cannot see it — that is `B-062`, and the extractor it asked for now exists (every needle, with its target path, in two seconds) with one thing left to decide, which `B-067` should record: a needle inside `if not re.search(...)` is *expected* not to match, because the plant seeds the precondition when it is absent, so resolution needs the asserted/conditional distinction before it can be gated. Five further blind spots are stated rather than closed, each with a reason: a needle built by `%`-formatting or an f-string over an **int** (the digits never exist as a string); a needle passed through a helper function (parameters carry no provenance); a plant that touches its target before reading it, which exempts every later needle on that path; a single digit outside a table cell or bold emphasis, because widening to any digit floods the corpus; and a needle held in a shell variable. **Derived plants still narrow to one candidate by ORDER** — the first match of a shape, the middle rule, the last row — and order is a value a release moves too, in a currency this census cannot read. No count is given here on purpose: two definitions of *narrowing by order* gave 11 and 26, and a figure whose definition is not written beside it is the defect this row is about. The largest single gap is the helper function: provenance stops at a `def`, which costs six plants every needle they have and is why they sit in the no-needle bucket. One **latent false positive** is accepted rather than closed and the trade is written here: `_bind` clears provenance on an assignment but not on a for-target rebinding, so `for l in <file text>: pass` followed by `for l in <a local list>:` refuses a look at the second `l`. One live plant reuses a for-target name across two loops and is not refused today. Reverting the scope was still right — it closed two real false negatives for one latent false positive — but a trade nobody wrote down is a trade nobody can revisit. Shipped in v1.81.1.

- **B-009 · `docs/DOCMAP.md` records no register for open questions** — it recorded `none` while the skill it ships tells every host project to keep one, and `templates/open-questions.md` had shipped all along. Seeded with three real questions this session raised, and the DOCMAP row now names it — which also gives the hand-back's first ambiguity source a number instead of `— no register`. **Behaviour**, not reporting. Closed 2026-08-11 in v1.43.0.

- **B-055 · `retro.md`'s run-stamp table is read in full and has no cap** — capped at ten with rotation into the quarterly archive, taught on all four surfaces (doctrine, the live file, the scaffold, the archive template) and owned by stage 10's prune. Stamp section 2 099 → 1 088 tok, read portion 3 333 → 2 335, stage-0 floor ~36 300 → ~35 300. **Behaviour**, not reporting. Closed 2026-08-10 in v1.41.0.

- **B-003 · The independent reviewer is not in the stage list** — it is now: `stages.md:469` *"The independent reader is dispatched by this stage, and read by its output"*, with `NO READER` as a printed state. Shipped in v1.38.0 and left open on the board for two days; found by the goal-check `continuity.md` Part 1a added, on its first run. Closed 2026-08-10.

- **B-007 · The code graph's semantic layer is stale; its structural layer is current** — the blocker this row named was a credential, and the alternative it named was taken instead: four dispatched subagents re-read all 49 changed files. Graph **1070 → 1257 nodes, 1217 → 1506 edges, 168 communities**; `built_at_commit` `cf501f2`, `git rev-list --count cf501f2..HEAD` → **0**; health check clean; divergence check finds no ghost source and no unindexed `references/*.md`. One template stays out — `hooks.example.json` extracts to zero nodes, as four of its JSON siblings do. Closed 2026-08-10, after v1.38.0.

- **B-033 · The command body is one 1281-word paragraph with 25 obligations, and it is the first text an age** — eight headed sections; longest paragraph 1281 → 295 words, longest line 4115 → 261 chars, nothing removed. Closed in v1.38.0.
- **B-034 · `setup` and `checkup` are invisible in every browsable surface — absent from the SKILL descripti** — both modes named in the SKILL description, the command description and both manifests. Separate command files were refused — `exposure.md` says a mode, not a new command. Closed in v1.38.0.
- **B-035 · The description is at 1015/1024 chars and ~40% of it is mechanism prose that cannot affect routi** — the four mechanism sentences cut; 1015 → 956 chars with both no-task modes named and 68 chars of headroom. Closed in v1.38.0.
- **B-036 · Both manifests describe the product as it stood at v1.30 — no board, ledger, exposure line, prog** — both manifests now name the board, the ledger, the exposure line, the progress rail, the review ceiling, the three tracks and the two modes. Closed in v1.38.0.
- **B-037 · R-005's reader is a third party with no contract** — stage 7 dispatches the reader and reads its **output**, recording one of three states; `NO READER` is printed, never omitted. Closed in v1.38.0.
- **B-038 · R-001's probe harness was written into its retirement condition in 2026-08-03 and never built** — `test/probe.py` — three assertions per plant, the third being that the guard which fired is the guard under test. It caught two of this release's own guards being too loose on first use. Closed in v1.38.0.
- **B-039 · The stage-0 reading floor is ~48k tokens before the first grill question — `retro.md` ~14.9k and** — the retro's uncapped narrative log is queried rather than read in full; stage-0 floor ~47 750 → ~36 950 tokens. Splitting `stages.md` into a spine plus per-stage detail remains open as B-043. Closed in part, v1.38.0.
- **B-042 · The eval suite is frozen at the v1.9 feature set — zero cases for backlog, verification, exposur** — the suite covers the last seven releases — 15 → 21 cases including both no-task modes, the three tracks, the rail and the reader — and the preflight now states that no blind run exists. **Running them blind is still open as B-002.** Closed in part, v1.38.0.
- **B-025 · Stage 7's review loop has no declared cap** — the cap is a decision point, not a stop — at it the run prints new findings against self-inflicted ones per round, because the ten-round runs were still finding real defects on round nine. `loop-guard.md` + stage 7 + `run.review.maxRounds`. Closed in v1.35.0.
- **B-026 · `.task-pipeline/run.md` had never been written by any run** — seeded at stage 0 from `templates/run.md`, named in that stage's gate, and read by two mechanisms — the churn detector and the progress rail. Closed in v1.34.0.
- **B-027 · Nothing in the pipeline owned how the product sounds or looks** — stage 3 runs three tracks with boundaries in both directions; `sheleg-design` joins the matrix and the preflight. Closed in v1.36.0.
- **B-028 · No stage prints which pipeline, which module or which iteration is running** — the header block at task start and the one-line iteration close, the rail computed from the project's own `pipeline.json`, every number borrowed from the gate that measured it. Closed in v1.34.0.
- **B-029 · Every lesson the pipeline learns dies in the repository that learned it** — `retro.publish`, off when absent; the body printed in full before it is sent, five numbered redaction rules. Closed in v1.37.0.
- **B-030 · Eleven stages and four manual gates for any change, including a one-paragraph edit** — stage 0's three-question triage proposes the short path and marks the skipped stages `⊘` with the triage answer — proposed, never taken silently. Closed in v1.35.0.
- **B-031 · The companion matrix is stale about super-ux's own surface** — super-ux's row names its copy half; a guard now compares the matrix's `needed for stage N` cell against what that stage says, and found `chrome-devtools` pointed at stage 5 with stage 5 silent. Closed in v1.36.0.
- **B-032 · `exposure.md`'s worked example disagreed with its own print** — the example carries no digits and its vocabulary is computed from the print statement. Closed in v1.35.0.
- **B-012 · The `v1.15.0` tag and its npm release** — `08-06-graph-staleness` #9; superseded: the project has shipped through `v1.30.0` since, and the release path is exercised every module. Closed by measurement on 2026-08-09.
- **B-013 · `gh` CLI unusable, token went invalid mid-run** — `08-06-graph-staleness` #10; environmental and no longer reproducing: `gh` has served every PR and merge of the last two programmes. Closed by measurement on 2026-08-09.
- **B-004 · A blank line silently splits a markdown table** — `08-05-artifact-hygiene` #C-007; the class hit this very board mid-build, splitting three rows off into prose, and a reader caught it. Promoted to a check the same day rather than to a third ledger row. Closed on 2026-08-09.
- **B-005 · Fixed `/tmp/<name>-copy` scratch paths** — the runner snapshots the tree once and runs every test against the snapshot; parallel execution means a second concurrent suite is rarely needed. Reproduced twice on 2026-08-09 before it was fixed. Closed 2026-08-10.
- **B-021 · The suite copied the repository per test, 13+ minutes** — eight-way parallel run over one snapshot: **5m34s**, measured. Closed 2026-08-10.
- **B-023 · Editing the working tree mid-run corrupted copies** — same change: tests read a pristine snapshot, never the live tree. Closed 2026-08-10.
- **B-017 · The `~/DATA/agent-sync` checkout was behind origin during the 2026-08-03 run** — `08-03-default-routing` #C-3; a run-local observation about another repository's working copy, not project work. Closed as `dropped` on 2026-08-09.
- **B-018 · The wiki was not synced** — `08-03-setup-and-autonomy` #C-4; synced repeatedly since, three times on 2026-08-09 alone. Closed by measurement.
- **B-019 · Ten consecutive releases shipped without a run stamp** — `08-08-audit-followup` #1; addressed in M3, which gave the cold-retirement trigger a second unit precisely because the stamp counter stops when the pipeline is not used. Closed on 2026-08-09.
- **B-020 · The M8 graph refresh dropped nine nodes to id collisions** — `08-08-audit-followup` #9; the row's own content already recorded **0 collisions** after M2's refresh while its status cell still read `open`. Closed by the row's own measurement.
- **B-014 · A run parked at its stage-0 gate** — `08-05-spec-plan-quality` #C-004; not work, a state note about a run that has since finished. Closed as `dropped` on 2026-08-09.

## How rows arrive

1. **Stage 0** seeds this file when absent, reads it when present, and quotes its open
   count in the brief.
2. **Any stage, mid-run** — the moment something is said aloud and not done.
3. **Stage 10** — every carry-over row still `open` arrives here with an id, and the
   ledger row is updated to name it. An `open` ledger row with no board id is the
   dangling pointer this file exists to resolve.
