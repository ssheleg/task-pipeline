name: validate

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # the tag-ancestry check below needs history, not a shallow clone

      # A release that lives only on a tag is invisible from inside either record.
      # v1.4.4 was tagged, released and published with a manifest field that `main`
      # never received; every later branch was cut from a tree that had not seen it,
      # and v1.6.0 silently shipped without it. main was consistent with itself, the
      # tag was consistent with itself, CI was green on both — the disagreement only
      # existed BETWEEN them, which is the same shape as the parent/submodule pointer
      # this project's own stage-10 doctrine guards against. So: every released tag
      # must be reachable from main.
      - name: Every v* tag must be contained in main
        run: |
          set -eu
          # --force, and it is not defensive noise. On a TAG build, checkout has
          # already created refs/tags/<tag> locally; re-fetching an ANNOTATED tag
          # then aborts with "would clobber existing tag" and git exits 1 — before
          # this step prints a single line, so `set -e` kills it with no output and
          # the log shows only the echoed script. That is exactly how it read.
          # It failed on every tag from v1.6.1 (the first one cut with `git tag -a`)
          # while `release` went out green beside it, so nobody saw a thing.
          git fetch --tags --force --quiet || echo "note: tag fetch degraded, continuing"
          git fetch --quiet origin main:refs/remotes/origin/main || true
          # If origin/main cannot be resolved, EVERY tag looks like an orphan and the
          # step reports a catastrophe that is really a missing ref. Say which it is.
          if ! git rev-parse --verify --quiet refs/remotes/origin/main >/dev/null; then
            echo "ERROR: refs/remotes/origin/main could not be fetched — the ancestry"
            echo "check cannot run, and reporting every tag as an orphan would be a lie."
            exit 1
          fi
          echo "checking $(git tag --list 'v*' | wc -l | tr -d ' ') tag(s) against $(git rev-parse --short refs/remotes/origin/main)"
          orphans=""
          for t in $(git tag --list 'v*'); do
            if ! git merge-base --is-ancestor "$t" origin/main 2>/dev/null; then
              orphans="$orphans $t"
            fi
          done
          if [ -n "$orphans" ]; then
            echo "ERROR: released tag(s) not contained in main:$orphans"
            echo "A tag outside main is a release nobody can branch from — its changes"
            echo "vanish from the next version while the registry still serves them."
            echo "Fix: merge or cherry-pick the tagged commit into main, then re-tag."
            exit 1
          fi
          echo "OK: every v* tag is an ancestor of main"

      - name: Install jsonschema (enables full pipeline.example.json conformance pass)
        run: python3 -m pip install --quiet jsonschema

      - name: Structural validator
        run: python3 test/validate.py

      - name: Stage-7 release gate (the hook this skill ships)
        run: python3 test/release_gate_test.py

      - name: Negative self-test (the release gate must be able to block)
        run: |
          set -eu
          rm -rf /tmp/gate-hook-copy && cp -R . /tmp/gate-hook-copy
          # Disarm the gate the way it was actually broken once: make the decision
          # unreachable, so every act classifies as "not a release". Anchored on the
          # env handoff — a structural marker — rather than on any prose around it.
          python3 - <<'EOF'
          import pathlib
          p = pathlib.Path('/tmp/gate-hook-copy/plugins/task-pipeline/hooks/release-gate.sh')
          s = p.read_text()
          needle = 'HOOK_INPUT="$input" python3 -'
          assert needle in s, "PLANT NO-OP: the payload handoff moved — re-anchor this plant"
          p.write_text(s.replace(needle, 'HOOK_INPUT="" python3 -'))
          EOF
          if python3 /tmp/gate-hook-copy/test/release_gate_test.py; then
            echo "ERROR: the fixtures passed against a gate that blocks nothing"; exit 1
          fi
          echo "OK: the release-gate fixtures detect a gate that fails open"

      - name: Negative self-test (validator must be able to fail)
        run: |
          set -eu
          rm -rf /tmp/corrupt-copy && cp -R . /tmp/corrupt-copy
          # corrupt the version so marketplace/plugin/CHANGELOG drift
          python3 - <<'EOF'
          import re
          p = "/tmp/corrupt-copy/plugins/task-pipeline/.claude-plugin/plugin.json"
          _orig_bytes = open(p, "rb").read()
          t = open(p, encoding="utf-8").read()
          open(p, "w", encoding="utf-8").write(
              re.sub(r'"version": "[0-9.]*"', '"version": "0.0.0-corrupt"', t))
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: validator-must-be-able-to-fail"
          EOF
          if python3 /tmp/corrupt-copy/test/validate.py; then
            echo "ERROR: validator passed on a corrupted copy — it cannot fail"
            exit 1
          else
            echo "OK: validator correctly FAILs on corrupted copy"
          fi

      - name: Negative self-test (bad gate type in pipeline.example.json must fail)
        run: |
          set -eu
          rm -rf /tmp/gate-copy && cp -R . /tmp/gate-copy
          python3 - <<'EOF'
          p = "/tmp/gate-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.example.json"
          _orig_bytes = open(p, "rb").read()
          t = open(p, encoding="utf-8").read()
          open(p, "w", encoding="utf-8").write(t.replace('"type": "auto"', '"type": "sometimes"'))
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: bad-gate-type-in-pipeline-example-js"
          EOF
          if python3 /tmp/gate-copy/test/validate.py; then
            echo "ERROR: validator passed an invalid gate.type"
            exit 1
          else
            echo "OK: validator rejects an invalid gate.type"
          fi

      - name: Negative self-test (release.enabled true but workflow missing must fail)
        run: |
          set -eu
          rm -rf /tmp/rel-copy && cp -R . /tmp/rel-copy
          rm -f /tmp/rel-copy/.github/workflows/release.yml
          if python3 /tmp/rel-copy/test/validate.py; then
            echo "ERROR: validator passed with release.enabled true but no release.yml"
            exit 1
          else
            echo "OK: validator requires the release workflow when release is enabled"
          fi

      - name: Negative self-test (hardcoded vendor model id must fail)
        run: |
          set -eu
          rm -rf /tmp/model-copy && cp -R . /tmp/model-copy
          printf '\nSwitch with `/model claude-opus-5` first.\n' \
            >> /tmp/model-copy/plugins/task-pipeline/skills/task-pipeline/SKILL.md
          if python3 /tmp/model-copy/test/validate.py; then
            echo "ERROR: validator passed a hardcoded model id — ids go stale, the tier is the contract"
            exit 1
          else
            echo "OK: validator rejects a hardcoded model id"
          fi

      - name: Negative self-test (non-token stage model must fail)
        run: |
          set -eu
          rm -rf /tmp/token-copy && cp -R . /tmp/token-copy
          python3 - <<'EOF'
          import json, collections
          p = "/tmp/token-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.example.json"
          _orig_bytes = open(p, "rb").read()
          d = json.load(open(p), object_pairs_hook=collections.OrderedDict)
          d["stages"][0]["model"] = "some-vendor-model"
          json.dump(d, open(p, "w"), indent=2, ensure_ascii=False)
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: non-token-stage-model-must-fail"
          EOF
          if python3 /tmp/token-copy/test/validate.py; then
            echo "ERROR: validator passed a stage model that is not a provider-agnostic token"
            exit 1
          else
            echo "OK: validator requires provider-agnostic stage model tokens"
          fi

      - name: Negative self-test (intake grill must stay mandatory + manual)
        run: |
          set -eu
          rm -rf /tmp/grill-copy && cp -R . /tmp/grill-copy
          python3 - <<'EOF'
          import json, collections
          p = "/tmp/grill-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.example.json"
          _orig_bytes = open(p, "rb").read()
          d = json.load(open(p), object_pairs_hook=collections.OrderedDict)
          d["stages"][0]["gate"]["type"] = "auto"
          json.dump(d, open(p, "w"), indent=2, ensure_ascii=False)
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: intake-grill-must-stay-mandatory-man"
          EOF
          if python3 /tmp/grill-copy/test/validate.py; then
            echo "ERROR: validator passed a non-manual intake-grill gate"
            exit 1
          else
            echo "OK: validator keeps the intake grill mandatory and operator-confirmed"
          fi

      - name: Negative self-test (brief template must keep the autonomy sweep)
        run: |
          set -eu
          rm -rf /tmp/brief-copy && cp -R . /tmp/brief-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/brief-copy/plugins/task-pipeline/skills/task-pipeline/templates/brief.md"
          _orig_bytes = open(p, "rb").read()
          t = open(p, encoding="utf-8").read()
          open(p, "w", encoding="utf-8").write(re.sub(r"^## Autonomy.*", "## Notes", t, flags=re.M))
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: brief-template-must-keep-the-autonom"
          EOF
          if python3 /tmp/brief-copy/test/validate.py; then
            echo "ERROR: validator passed a brief template with no autonomy sweep"
            exit 1
          else
            echo "OK: validator requires the autonomy sweep in the brief template"
          fi

      - name: Negative self-test (the grill doctrine must ship with the skill)
        run: |
          set -eu
          rm -rf /tmp/doctrine-copy && cp -R . /tmp/doctrine-copy
          rm -f /tmp/doctrine-copy/plugins/task-pipeline/skills/task-pipeline/references/grill.md
          if python3 /tmp/doctrine-copy/test/validate.py; then
            echo "ERROR: validator passed without the built-in grill doctrine"
            exit 1
          else
            echo "OK: validator requires references/grill.md — the grill is built in, not a dependency"
          fi

      - name: Negative self-test (a real broken link still fails past the fence stripper)
        run: |
          set -eu
          rm -rf /tmp/link-copy && cp -R . /tmp/link-copy
          printf '\n[gone](./does-not-exist.md)\n' >> /tmp/link-copy/README.md
          if python3 /tmp/link-copy/test/validate.py; then
            echo "ERROR: validator missed a broken relative link outside a code fence"
            exit 1
          else
            echo "OK: validator still catches broken links outside fences"
          fi

      - name: Negative self-test (the REQ spine must stay in the brief template)
        run: |
          set -eu
          rm -rf /tmp/req-copy && cp -R . /tmp/req-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/req-copy/plugins/task-pipeline/skills/task-pipeline/templates/brief.md"
          _orig_bytes = open(p, "rb").read()
          t = open(p, encoding="utf-8").read()
          open(p, "w", encoding="utf-8").write(re.sub(r"^## Requirements.*", "## Notes", t, flags=re.M))
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: the-req-spine-must-stay-in-the-brief"
          EOF
          if python3 /tmp/req-copy/test/validate.py; then
            echo "ERROR: validator passed a brief template with no REQ spine"
            exit 1
          else
            echo "OK: validator requires the REQ spine in the brief template"
          fi

      - name: Negative self-test (REQ rows must name how they are verified)
        run: |
          set -eu
          rm -rf /tmp/verify-copy && cp -R . /tmp/verify-copy
          python3 - <<'EOF'
          p = "/tmp/verify-copy/plugins/task-pipeline/skills/task-pipeline/templates/brief.md"
          _orig_bytes = open(p, "rb").read()
          t = open(p, encoding="utf-8").read()
          open(p, "w", encoding="utf-8").write(t.replace("How it's verified", "Notes"))
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: req-rows-must-name-how-they-are-veri"
          EOF
          if python3 /tmp/verify-copy/test/validate.py; then
            echo "ERROR: validator passed a REQ table with no verification column"
            exit 1
          else
            echo "OK: validator requires a named check per requirement"
          fi

      - name: Negative self-test (acceptance must close the flow, manually, with evidence)
        run: |
          set -eu
          rm -rf /tmp/acc-copy && cp -R . /tmp/acc-copy
          python3 - <<'EOF'
          import json, collections
          p = "/tmp/acc-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.example.json"
          _orig_bytes = open(p, "rb").read()
          d = json.load(open(p), object_pairs_hook=collections.OrderedDict)
          d["stages"][-1]["gate"]["type"] = "auto"
          json.dump(d, open(p, "w"), indent=2, ensure_ascii=False)
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: acceptance-must-close-the-flow-manua"
          EOF
          if python3 /tmp/acc-copy/test/validate.py; then
            echo "ERROR: validator passed a non-manual acceptance gate"
            exit 1
          else
            echo "OK: validator keeps acceptance manual — only the asker can confirm"
          fi

      - name: Negative self-test (the plan gate must be a set comparison)
        run: |
          set -eu
          rm -rf /tmp/seam-copy && cp -R . /tmp/seam-copy
          python3 - <<'EOF'
          import json, collections
          p = "/tmp/seam-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.example.json"
          _orig_bytes = open(p, "rb").read()
          d = json.load(open(p), object_pairs_hook=collections.OrderedDict)
          for st in d["stages"]:
              if st.get("state") == "plan":
                  st["gate"]["check"] = "every spec requirement maps to a task"
          json.dump(d, open(p, "w"), indent=2, ensure_ascii=False)
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: the-plan-gate-must-be-a-set-comparis"
          EOF
          if python3 /tmp/seam-copy/test/validate.py; then
            echo "ERROR: validator passed a plan gate with no REQ set comparison"
            exit 1
          else
            echo "OK: validator requires set equality at the brief-to-plan seam"
          fi

      - name: Negative self-test (the brief must keep the knowledge-source ledger)
        run: |
          set -eu
          rm -rf /tmp/ledger-copy && cp -R . /tmp/ledger-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/ledger-copy/plugins/task-pipeline/skills/task-pipeline/templates/brief.md"
          _orig_bytes = open(p, "rb").read()
          t = open(p, encoding="utf-8").read()
          open(p, "w", encoding="utf-8").write(re.sub(r"^## Knowledge sources.*", "## Notes", t, flags=re.M))
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: the-brief-must-keep-the-knowledge-so"
          EOF
          if python3 /tmp/ledger-copy/test/validate.py; then
            echo "ERROR: validator passed a brief template with nowhere to record the harvest"
            exit 1
          else
            echo "OK: validator requires the stage-0 source ledger in the brief template"
          fi

      - name: Negative self-test (stage 0 must harvest before it interviews)
        run: |
          set -eu
          rm -rf /tmp/harvest-copy && cp -R . /tmp/harvest-copy
          python3 - <<'EOF'
          import json, collections
          p = "/tmp/harvest-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.example.json"
          _orig_bytes = open(p, "rb").read()
          d = json.load(open(p), object_pairs_hook=collections.OrderedDict)
          d["stages"][0]["gate"]["check"] = "MANDATORY stage — never skipped. One question at a time; the brief is confirmed."
          json.dump(d, open(p, "w"), indent=2, ensure_ascii=False)
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: stage-0-must-harvest-before-it-inter"
          EOF
          if python3 /tmp/harvest-copy/test/validate.py; then
            echo "ERROR: validator passed a stage-0 gate with no knowledge harvest"
            exit 1
          else
            echo "OK: validator requires the phase-1 harvest and its ledger before the interview"
          fi

      - name: Negative self-test (stage 9 must close the loop on the ledger)
        run: |
          set -eu
          rm -rf /tmp/loopback-copy && cp -R . /tmp/loopback-copy
          python3 - <<'EOF'
          import json, collections
          p = "/tmp/loopback-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.example.json"
          _orig_bytes = open(p, "rb").read()
          d = json.load(open(p), object_pairs_hook=collections.OrderedDict)
          for s in d["stages"]:
              if s.get("state") == "docs-wiki":
                  s["gate"]["check"] = "docs in sync with code; wiki synced; dangling links fixed"
          json.dump(d, open(p, "w"), indent=2, ensure_ascii=False)
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: stage-9-must-close-the-loop-on-the-l"
          EOF
          if python3 /tmp/loopback-copy/test/validate.py; then
            echo "ERROR: validator passed a docs stage that never updates what stage 0 read"
            exit 1
          else
            echo "OK: validator keeps the harvest→update loop closed"
          fi

      - name: Negative self-test (a description that drops the final stage must fail)
        run: |
          set -eu
          rm -rf /tmp/blurb-copy && cp -R . /tmp/blurb-copy
          python3 - <<'EOF'
          import json
          p = "/tmp/blurb-copy/package.json"
          _orig_bytes = open(p, "rb").read()
          d = json.load(open(p))
          d["description"] = d["description"].replace(", acceptance)", ")")
          json.dump(d, open(p, "w"), indent=2, ensure_ascii=False)
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: a-description-that-drops-the-final-s"
          EOF
          if python3 /tmp/blurb-copy/test/validate.py; then
            echo "ERROR: validator passed a description that stops one stage short"
            exit 1
          else
            echo "OK: validator requires every description to name the flow's final stage"
          fi

      - name: Negative self-test (verdict count must not drift from the dev gate)
        run: |
          set -eu
          rm -rf /tmp/verdict-copy && cp -R . /tmp/verdict-copy
          python3 - <<'EOF'
          p = "/tmp/verdict-copy/plugins/task-pipeline/skills/task-pipeline/references/build.md"
          _orig_bytes = open(p, "rb").read()
          t = open(p, encoding="utf-8").read()
          open(p, "w", encoding="utf-8").write(t.replace("with **all three** verdicts", "with **both** verdicts"))
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: verdict-count-must-not-drift-from-th"
          EOF
          if python3 /tmp/verdict-copy/test/validate.py; then
            echo "ERROR: validator passed 'both verdicts' while the dev gate declares three"
            exit 1
          else
            echo "OK: validator keeps the review verdict count consistent across surfaces"
          fi

      - name: Negative self-test (a doctrine file's gate type must match the config)
        run: |
          set -eu
          rm -rf /tmp/doctrinegate-copy && cp -R . /tmp/doctrinegate-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/doctrinegate-copy/plugins/task-pipeline/skills/task-pipeline/references/acceptance.md"
          _orig_bytes = open(p, "rb").read()
          t = open(p, encoding="utf-8").read()
          open(p, "w", encoding="utf-8").write(re.sub(r"^## GATE \(manual\)", "## GATE (auto)", t, flags=re.M))
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: a-doctrine-file-s-gate-type-must-mat"
          EOF
          if python3 /tmp/doctrinegate-copy/test/validate.py; then
            echo "ERROR: validator passed a doctrine gate type contradicting pipeline.example.json"
            exit 1
          else
            echo "OK: validator keeps each stage doctrine's gate type in sync with the config"
          fi

      - name: Negative self-test (a stage-10 close-out rule must reach the surfaces that enforce it)
        run: |
          set -eu
          rm -rf /tmp/closeout-copy && cp -R . /tmp/closeout-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/closeout-copy/plugins/task-pipeline/skills/task-pipeline/references/acceptance.md"
          _orig_bytes = open(p, "rb").read()
          t = open(p, encoding="utf-8").read()
          open(p, "w", encoding="utf-8").write(re.sub(r"submodule", "sub-module", t, flags=re.I))
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: a-stage-10-close-out-rule-must-reach"
          EOF
          if python3 /tmp/closeout-copy/test/validate.py; then
            echo "ERROR: validator passed a close-out rule declared in SKILL.md but absent from the gate that enforces it"
            exit 1
          else
            echo "OK: validator keeps the stage-10 close-out on every surface that defines it"
          fi

      - name: Negative self-test (intake must settle the Figma design destination)
        run: |
          set -eu
          rm -rf /tmp/dest-copy && cp -R . /tmp/dest-copy
          python3 - <<'EOF'
          import json, collections, re
          p = "/tmp/dest-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.example.json"
          _orig_bytes = open(p, "rb").read()
          d = json.load(open(p), object_pairs_hook=collections.OrderedDict)
          c = d["stages"][0]["gate"]["check"]
          d["stages"][0]["gate"]["check"] = re.sub(
              r"; plus, with Figma on, the DESIGN DESTINATION.*?replacement\)", ")", c, flags=re.S)
          json.dump(d, open(p, "w"), indent=2, ensure_ascii=False)
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: intake-must-settle-the-figma-design"
          EOF
          if python3 /tmp/dest-copy/test/validate.py; then
            echo "ERROR: validator passed an intake gate that never settles which Figma file, in which team"
            exit 1
          else
            echo "OK: validator requires the design destination at intake — a duplicate design file is silent"
          fi

      - name: Negative self-test (the autonomy sweep must not drift between its two homes)
        run: |
          set -eu
          rm -rf /tmp/sweep-copy && cp -R . /tmp/sweep-copy
          python3 - <<'EOF'
          p = "/tmp/sweep-copy/plugins/task-pipeline/skills/task-pipeline/templates/brief.md"
          _orig_bytes = open(p, "rb").read()
          t = open(p, encoding="utf-8").read()
          t = "\n".join(l for l in t.splitlines() if not l.startswith("| 8 Post-deploy"))
          open(p, "w", encoding="utf-8").write(t + "\n")
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: the-autonomy-sweep-must-not-drift-be"
          EOF
          if python3 /tmp/sweep-copy/test/validate.py; then
            echo "ERROR: validator passed a brief template whose sweep drops a stage the grill asks about"
            exit 1
          else
            echo "OK: validator keeps grill.md's sweep and brief.md's sweep covering the same stages"
          fi

      - name: Negative self-test (acceptance must walk the ladder before the table)
        run: |
          set -eu
          rm -rf /tmp/ladder-copy && cp -R . /tmp/ladder-copy
          python3 - <<'EOF'
          import json, collections
          p = "/tmp/ladder-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.example.json"
          _orig_bytes = open(p, "rb").read()
          d = json.load(open(p), object_pairs_hook=collections.OrderedDict)
          d["stages"][-1]["gate"]["check"] = (
              "every REQ has a status; every verified carries evidence; the operator signs off"
          )
          json.dump(d, open(p, "w"), indent=2, ensure_ascii=False)
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: acceptance-must-walk-the-ladder-befo"
          EOF
          if python3 /tmp/ladder-copy/test/validate.py; then
            echo "ERROR: validator passed an acceptance gate that only compares the REQ list — an absence has one side"
            exit 1
          else
            echo "OK: validator requires the ladder walk before the coverage table"
          fi

      - name: Negative self-test (the audit doctrine must ship with the skill)
        run: |
          set -eu
          rm -rf /tmp/audit-copy && cp -R . /tmp/audit-copy
          rm -f /tmp/audit-copy/plugins/task-pipeline/skills/task-pipeline/references/audit.md
          if python3 /tmp/audit-copy/test/validate.py; then
            echo "ERROR: validator passed without the built-in audit doctrine"
            exit 1
          else
            echo "OK: validator requires references/audit.md"
          fi

      - name: Negative self-test (the open-source surface must stay present)
        run: |
          set -eu
          rm -rf /tmp/oss-copy && cp -R . /tmp/oss-copy
          rm -f /tmp/oss-copy/CONTRIBUTING.md
          if python3 /tmp/oss-copy/test/validate.py; then
            echo "ERROR: validator passed a repo with no CONTRIBUTING.md"
            exit 1
          else
            echo "OK: validator requires the open-source root files"
          fi

      - name: Negative self-test (npm test must run the validator)
        run: |
          set -eu
          rm -rf /tmp/script-copy && cp -R . /tmp/script-copy
          python3 - <<'EOF'
          import json
          p = "/tmp/script-copy/package.json"
          _orig_bytes = open(p, "rb").read()
          d = json.load(open(p))
          d["scripts"] = {"test": "echo ok"}
          json.dump(d, open(p, "w"), indent=2, ensure_ascii=False)
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: npm-test-must-run-the-validator"
          EOF
          if python3 /tmp/script-copy/test/validate.py; then
            echo "ERROR: validator passed a package.json whose test script skips the validator"
            exit 1
          else
            echo "OK: validator requires npm test to run test/validate.py"
          fi

      - name: Negative self-test (the negative self-tests must stay runnable off CI)
        run: |
          set -eu
          rm -rf /tmp/portable-copy && cp -R . /tmp/portable-copy
          python3 - <<'EOF'
          # build the forbidden token at runtime — spelling it literally here would
          # make this very file trip the guard it is testing
          bad = "s" + "ed -i"
          p = "/tmp/portable-copy/.github/workflows/validate.yml"
          _orig_bytes = open(p, "rb").read()
          t = open(p, encoding="utf-8").read()
          open(p, "w", encoding="utf-8").write(
              t + f"\n      - name: stray\n        run: |\n          {bad} 's/a/b/' x\n")
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: the-negative-self-tests-must-stay-ru"
          EOF
          if python3 /tmp/portable-copy/test/validate.py; then
            echo "ERROR: validator passed a workflow with non-portable in-place edits — that breaks the local runner on BSD"
            exit 1
          else
            echo "OK: validator keeps the negative self-tests portable"
          fi

      - name: Negative self-test (a shipped code-graph doctrine must reach the stage-9 gate)
        run: |
          set -eu
          rm -rf /tmp/graph-copy && cp -R . /tmp/graph-copy
          python3 - <<'EOF'
          import json, re
          p = "/tmp/graph-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.example.json"
          _orig_bytes = open(p, "rb").read()
          d = json.load(open(p, encoding="utf-8"))
          for s in d["stages"]:
              if s.get("state") == "docs-wiki":
                  s["gate"]["check"] = re.sub(r"graph", "doc", s["gate"]["check"], flags=re.I)
          json.dump(d, open(p, "w", encoding="utf-8"), indent=2, ensure_ascii=False)
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: a-shipped-code-graph-doctrine-must-r"
          EOF
          if python3 /tmp/graph-copy/test/validate.py; then
            echo "ERROR: validator passed a stage-9 gate that never mentions the graph — the refresh would be declared where it is not enforced"
            exit 1
          else
            echo "OK: validator requires the stage-9 gate to enforce the code-graph refresh"
          fi

      - name: Negative self-test (the stage-9 detail must name the code-graph refresh too)
        run: |
          set -eu
          rm -rf /tmp/graph-doc-copy && cp -R . /tmp/graph-doc-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/graph-doc-copy/plugins/task-pipeline/skills/task-pipeline/references/stages.md"
          _orig_bytes = open(p, "rb").read()
          t = open(p, encoding="utf-8").read()
          m = re.search(r"^## 9 — .*?(?=^## |\Z)", t, re.M | re.S)
          open(p, "w", encoding="utf-8").write(
              t[:m.start()] + re.sub(r"graph", "doc", m.group(0), flags=re.I) + t[m.end():])
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: the-stage-9-detail-must-name-the-cod"
          EOF
          if python3 /tmp/graph-doc-copy/test/validate.py; then
            echo "ERROR: validator passed a stage-9 section that never mentions the graph — an agent reading the stage detail is never told to refresh it"
            exit 1
          else
            echo "OK: validator requires stages.md stage 9 to name the code-graph refresh"
          fi

      - name: Negative self-test (the retrospective must reach the surfaces that enforce stage 10)
        run: |
          set -eu
          rm -rf /tmp/retro-anchor-copy && cp -R . /tmp/retro-anchor-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/retro-anchor-copy/plugins/task-pipeline/skills/task-pipeline/references/acceptance.md"
          _orig_bytes = open(p, "rb").read()
          t = open(p, encoding="utf-8").read()
          open(p, "w", encoding="utf-8").write(re.sub(r"retro", "post", t, flags=re.I))
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: the-retrospective-must-reach-the-sur"
          EOF
          if python3 /tmp/retro-anchor-copy/test/validate.py; then
            echo "ERROR: validator passed a close-out doctrine that never mentions the retro — the rule would be declared in SKILL.md and enforced nowhere"
            exit 1
          else
            echo "OK: validator requires the retro to reach the stage-10 surfaces"
          fi

      - name: Negative self-test (the retro template must ship)
        run: |
          set -eu
          rm -rf /tmp/retro-tpl-copy && cp -R . /tmp/retro-tpl-copy
          rm -f /tmp/retro-tpl-copy/plugins/task-pipeline/skills/task-pipeline/templates/retro.md
          if python3 /tmp/retro-tpl-copy/test/validate.py; then
            echo "ERROR: validator passed with no retro template — the run has nowhere to write its lessons"
            exit 1
          else
            echo "OK: validator requires the retro template"
          fi

      - name: Negative self-test (a standing instruction must carry its retirement trigger)
        run: |
          set -eu
          rm -rf /tmp/retro-prune-copy && cp -R . /tmp/retro-prune-copy
          python3 - <<'EOF'
          p = "/tmp/retro-prune-copy/plugins/task-pipeline/skills/task-pipeline/templates/retro.md"
          _orig_bytes = open(p, "rb").read()
          t = open(p, encoding="utf-8").read()
          open(p, "w", encoding="utf-8").write(t.replace("Retire when", "Notes"))
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: a-standing-instruction-must-carry-it"
          EOF
          if python3 /tmp/retro-prune-copy/test/validate.py; then
            echo "ERROR: validator passed a retro template with no retirement trigger — the prune becomes an argument and the list grows until nobody reads it"
            exit 1
          else
            echo "OK: validator requires every standing instruction to carry its retire-when trigger"
          fi

      - name: Negative self-test (a task-pipeline:<name> skill must resolve to its doctrine file)
        run: |
          set -eu
          rm -rf /tmp/doctrine-ptr-copy && cp -R . /tmp/doctrine-ptr-copy
          python3 - <<'EOF'
          p = "/tmp/doctrine-ptr-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.example.json"
          _orig_bytes = open(p, "rb").read()
          t = open(p, encoding="utf-8").read()
          open(p, "w", encoding="utf-8").write(
              t.replace('"task-pipeline:grill"', '"task-pipeline:interrogation"'))
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: a-task-pipeline-name-skill-must-reso"
          EOF
          if python3 /tmp/doctrine-ptr-copy/test/validate.py; then
            echo "ERROR: validator passed a stage pointed at doctrine that does not exist"
            exit 1
          else
            echo "OK: validator requires every task-pipeline:<name> to resolve to references/<name>.md"
          fi

      - name: Negative self-test (an unresolved merge conflict must fail)
        run: |
          set -eu
          rm -rf /tmp/conflict-copy && cp -R . /tmp/conflict-copy
          python3 - <<'EOF'
          # build the markers at runtime — spelling them here would trip the guard
          # on this very file
          o, c, s = "<" * 7, ">" * 7, "=" * 7
          p = "/tmp/conflict-copy/README.md"
          _orig_bytes = open(p, "rb").read()
          t = open(p, encoding="utf-8").read()
          open(p, "w", encoding="utf-8").write(
              t + f"\n{o} HEAD\nours\n{s}\ntheirs\n{c} branch\n")
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: an-unresolved-merge-conflict-must-fa"
          EOF
          if python3 /tmp/conflict-copy/test/validate.py; then
            echo "ERROR: validator passed a file with unresolved conflict markers — in a prose repo that ships as doctrine"
            exit 1
          else
            echo "OK: validator rejects unresolved merge conflict markers"
          fi

      - name: Negative self-test (the documentation-track doctrine must ship)
        run: |
          set -eu
          for f in documentation gates hooks; do
            rm -rf /tmp/doctrine-copy-2 && cp -R . /tmp/doctrine-copy-2
            rm /tmp/doctrine-copy-2/plugins/task-pipeline/skills/task-pipeline/references/$f.md
            if python3 /tmp/doctrine-copy-2/test/validate.py; then
              echo "ERROR: validator passed with references/$f.md missing"
              exit 1
            fi
          done
          echo "OK: validator requires documentation.md, gates.md and hooks.md"

      - name: Negative self-test (the doc map must carry its propagation matrix)
        run: |
          set -eu
          rm -rf /tmp/docmap-copy && cp -R . /tmp/docmap-copy
          python3 - <<'EOF'
          p = "/tmp/docmap-copy/plugins/task-pipeline/skills/task-pipeline/templates/docmap.md"
          t = open(p, encoding="utf-8").read()
          assert "## Propagation matrix" in t, "PLANT MISSED"
          open(p, "w", encoding="utf-8").write(t.replace("## Propagation matrix", "## What changes what", 1))
          EOF
          if python3 /tmp/docmap-copy/test/validate.py; then
            echo "ERROR: validator passed a doc map with no propagation matrix — that seeds a project with a register and no obligation"
            exit 1
          else
            echo "OK: validator requires the doc map's propagation matrix"
          fi

      - name: Negative self-test (a retro lesson must carry its commit)
        run: |
          set -eu
          rm -rf /tmp/retrocommit-copy && cp -R . /tmp/retrocommit-copy
          python3 - <<'EOF'
          p = "/tmp/retrocommit-copy/plugins/task-pipeline/skills/task-pipeline/templates/retro.md"
          t = open(p, encoding="utf-8").read()
          assert "Commit" in t, "PLANT MISSED"
          open(p, "w", encoding="utf-8").write(t.replace("Commit", "Ref"))
          EOF
          if python3 /tmp/retrocommit-copy/test/validate.py; then
            echo "ERROR: validator passed a retro with no Commit column — file:line evidence rots at the next edit"
            exit 1
          else
            echo "OK: validator requires the retro's commit columns"
          fi

      - name: Negative self-test (the retro archive template must ship)
        run: |
          set -eu
          rm -rf /tmp/retroarch-copy && cp -R . /tmp/retroarch-copy
          rm /tmp/retroarch-copy/plugins/task-pipeline/skills/task-pipeline/templates/retro-archive.md
          if python3 /tmp/retroarch-copy/test/validate.py; then
            echo "ERROR: validator passed with no archive template — the in-force retro is capped and read in full, so its history needs a home"
            exit 1
          else
            echo "OK: validator requires templates/retro-archive.md"
          fi

      - name: Negative self-test (the seeded gate must stay portable)
        run: |
          set -eu
          rm -rf /tmp/portable-copy-2 && cp -R . /tmp/portable-copy-2
          python3 - <<'EOF'
          p = "/tmp/portable-copy-2/plugins/task-pipeline/skills/task-pipeline/templates/docgate.sh"
          t = open(p, encoding="utf-8").read()
          # Build the construct at runtime — spelling it here would trip the
          # workflow's own portability guard on this very file.
          bad = "s" + "ed -" + "i 's/a/b/' /dev/null"
          # plant in CODE, not in a comment: the guard reads code only
          assert "set -u" in t, "PLANT MISSED"
          open(p, "w", encoding="utf-8").write(t.replace("set -u", "set -u\n" + bad, 1))
          EOF
          if python3 /tmp/portable-copy-2/test/validate.py; then
            echo "ERROR: validator passed a gate using an in-place stream edit — on BSD it silently edits nothing and reads as a guard that failed to fire"
            exit 1
          else
            echo "OK: validator rejects non-portable constructs in the seeded gate"
          fi

      - name: Negative self-test (the seeded gate must seed GREEN)
        run: |
          set -eu
          rm -rf /tmp/seedred-copy && cp -R . /tmp/seedred-copy
          python3 - <<'EOF'
          p = "/tmp/seedred-copy/plugins/task-pipeline/skills/task-pipeline/templates/decisions.md"
          t = open(p, encoding="utf-8").read()
          old = "**Next free ID:** `DEC-0002`"
          assert old in t, "PLANT MISSED"
          open(p, "w", encoding="utf-8").write(t.replace(old, "**Next free ID:** `DEC-0009`", 1))
          EOF
          if python3 /tmp/seedred-copy/test/validate.py; then
            echo "ERROR: validator passed templates whose own gate rejects them — a project that starts red learns on day one that the gate is noise"
            exit 1
          else
            echo "OK: validator runs the seeded gate over a seeded project and requires exit 0"
          fi

      - name: Negative self-test (every template must be listed in templates/README.md)
        run: |
          set -eu
          rm -rf /tmp/tplreadme-copy && cp -R . /tmp/tplreadme-copy
          echo "# stray" > /tmp/tplreadme-copy/plugins/task-pipeline/skills/task-pipeline/templates/unlisted.md
          if python3 /tmp/tplreadme-copy/test/validate.py; then
            echo "ERROR: validator passed an unlisted template — one nobody knows to seed"
            exit 1
          else
            echo "OK: validator requires templates/README.md to list every template"
          fi

      - name: Negative self-test (the doc track must reach the surfaces that enforce it)
        run: |
          set -eu
          rm -rf /tmp/anchor-copy && cp -R . /tmp/anchor-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/anchor-copy/plugins/task-pipeline/skills/task-pipeline/references/stages.md"
          t = open(p, encoding="utf-8").read()
          assert re.search("propagation", t, re.I), "PLANT MISSED"
          open(p, "w", encoding="utf-8").write(re.sub("propagation", "spreading", t, flags=re.I))
          EOF
          if python3 /tmp/anchor-copy/test/validate.py; then
            echo "ERROR: validator passed with the propagation sweep declared in SKILL.md and absent from stages.md — declared where it is not enforced is inert"
            exit 1
          else
            echo "OK: validator holds every surface that enforces stage 9 to the propagation sweep"
          fi

      - name: Negative self-test (the two decision shapes must agree on fields)
        run: |
          set -eu
          rm -rf /tmp/adrparity-copy && cp -R . /tmp/adrparity-copy
          python3 - <<'EOF'
          p = "/tmp/adrparity-copy/plugins/task-pipeline/skills/task-pipeline/templates/adr.md"
          t = open(p, encoding="utf-8").read()
          assert "Consequences / affects" in t, "PLANT MISSED"
          open(p, "w", encoding="utf-8").write(t.replace("Consequences / affects", "Downstream effects"))
          EOF
          if python3 /tmp/adrparity-copy/test/validate.py; then
            echo "ERROR: validator passed a register and an ADR set that disagree on fields — two shapes of one contract is a fork with extra steps"
            exit 1
          else
            echo "OK: validator holds decisions.md and adr.md to one field set"
          fi

      - name: Negative self-test (the seeded gate must read BOTH register shapes)
        run: |
          set -eu
          rm -rf /tmp/blindadr-copy && cp -R . /tmp/blindadr-copy
          python3 - <<'EOF'
          p = "/tmp/blindadr-copy/plugins/task-pipeline/skills/task-pipeline/templates/docgate.sh"
          s = open(p, encoding="utf-8").read()
          old = 'elif [ "$have_adr" = "1" ]; then'
          assert old in s, "PLANT MISSED"
          open(p, "w", encoding="utf-8").write(s.replace(old, "elif false; then", 1))
          EOF
          if python3 /tmp/blindadr-copy/test/validate.py; then
            echo "ERROR: validator passed a gate blind to the ADR shape — every section goes dormant, dormant is green, and a populated register sits behind a passing gate"
            exit 1
          else
            echo "OK: validator requires the seeded gate to report its shape and run live checks on both"
          fi

      - name: Negative self-test (the Doc Loop must reach the stages that settle decisions)
        run: |
          set -eu
          rm -rf /tmp/docloop-copy && cp -R . /tmp/docloop-copy
          python3 - <<'EOF'
          p = "/tmp/docloop-copy/plugins/task-pipeline/skills/task-pipeline/references/build.md"
          s = open(p, encoding="utf-8").read()
          assert "documentation.md" in s, "PLANT MISSED"
          open(p, "w", encoding="utf-8").write(s.replace("documentation.md", "somewhere-else.md"))
          EOF
          if python3 /tmp/docloop-copy/test/validate.py; then
            echo "ERROR: validator passed a stage doctrine that settles decisions and never names the Doc Loop"
            exit 1
          else
            echo "OK: validator holds the five decision-settling stages to the Doc Loop"
          fi

      - name: Negative self-test (the autonomy sweep is compared by topic, not by stage number)
        run: |
          set -eu
          rm -rf /tmp/sweep-copy-2 && cp -R . /tmp/sweep-copy-2
          python3 - <<'EOF'
          p = "/tmp/sweep-copy-2/plugins/task-pipeline/skills/task-pipeline/references/grill.md"
          t = open(p, encoding="utf-8").read()
          old = "| 0 Docs regime |"
          assert old in t, "PLANT MISSED"
          i = t.index(old); j = t.index("\n", i) + 1
          open(p, "w", encoding="utf-8").write(t[:i] + t[j:])
          EOF
          if python3 /tmp/sweep-copy-2/test/validate.py; then
            echo "ERROR: validator passed a sweep row the brief records and the grill never asks — both files still 'cover stage 0', which is why counting stage numbers was not enough"
            exit 1
          else
            echo "OK: validator compares the sweep per stage as a topic set"
          fi

      - name: Negative self-test (a long reference's Contents must match its headings)
        run: |
          set -eu
          rm -rf /tmp/toc-copy && cp -R . /tmp/toc-copy
          python3 - <<'EOF'
          p = "/tmp/toc-copy/plugins/task-pipeline/skills/task-pipeline/references/stages.md"
          t = open(p, encoding="utf-8").read()
          old = "## The program loop"
          assert old in t, "PLANT MISSED"
          open(p, "w", encoding="utf-8").write(
              t.replace(old, "## A section nobody listed\n\nbody.\n\n" + old, 1))
          EOF
          if python3 /tmp/toc-copy/test/validate.py; then
            echo "ERROR: validator passed a Contents list that no longer matches the headings — a hand-maintained list is a second source, and a partial read trusts it"
            exit 1
          else
            echo "OK: validator compares each long reference's Contents against its own headings"
          fi

      - name: Negative self-test (the evaluation suite must cover every category)
        run: |
          set -eu
          rm -rf /tmp/evals-copy && cp -R . /tmp/evals-copy
          python3 - <<'EOF'
          import json
          p = "/tmp/evals-copy/evals/task-pipeline.evals.json"
          d = json.load(open(p, encoding="utf-8"))
          n = len(d["evals"])
          d["evals"] = [e for e in d["evals"] if e["category"] != "coexistence"]
          assert len(d["evals"]) < n, "PLANT MISSED"
          json.dump(d, open(p, "w", encoding="utf-8"), indent=2)
          EOF
          if python3 /tmp/evals-copy/test/validate.py; then
            echo "ERROR: validator passed an eval suite with a missing dimension — coexistence is where a broad description steals triggers from narrower skills"
            exit 1
          else
            echo "OK: validator runs evals/run.py and requires all five dimensions"
          fi

      - name: Negative self-test (the description must say WHAT before WHEN)
        run: |
          set -eu
          rm -rf /tmp/desc-copy && cp -R . /tmp/desc-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/desc-copy/plugins/task-pipeline/skills/task-pipeline/SKILL.md"
          t = open(p, encoding="utf-8").read()
          t2 = re.sub(r'^description: ".*?"$',
                      'description: "Use when running a substantial task через полный цикл."',
                      t, count=1, flags=re.M | re.S)
          assert t2 != t, "PLANT MISSED"
          open(p, "w", encoding="utf-8").write(t2)
          EOF
          if python3 /tmp/desc-copy/test/validate.py; then
            echo "ERROR: validator passed a description that opens with the trigger and never says what the skill does"
            exit 1
          else
            echo "OK: validator requires the capability first, then 'Use when …'"
          fi

      - name: Negative self-test (the skill card must answer every risk indicator)
        run: |
          set -eu
          rm -rf /tmp/card-copy && cp -R . /tmp/card-copy
          python3 - <<'EOF'
          p = "/tmp/card-copy/SKILL-CARD.md"
          t = open(p, encoding="utf-8").read()
          assert "MCP server references" in t, "PLANT MISSED"
          open(p, "w", encoding="utf-8").write(t.replace("MCP server references", "Other notes"))
          EOF
          if python3 /tmp/card-copy/test/validate.py; then
            echo "ERROR: validator passed a skill card with a risk indicator omitted — an omitted row reads as 'does not apply', and here three of them do"
            exit 1
          else
            echo "OK: validator requires every risk indicator answered on the card"
          fi

      - name: Negative self-test (a citation must name a section that exists)
        run: |
          set -eu
          rm -rf /tmp/cite-copy && cp -R . /tmp/cite-copy
          python3 - <<'EOF'
          p = "/tmp/cite-copy/plugins/task-pipeline/skills/task-pipeline/references/stages.md"
          t = open(p, encoding="utf-8").read()
          assert "→ *Axis C*" in t, "PLANT MISSED"
          open(p, "w", encoding="utf-8").write(t.replace("→ *Axis C*", "→ *Axis Q*", 1))
          EOF
          if python3 /tmp/cite-copy/test/validate.py; then
            echo "ERROR: validator passed a citation whose file resolves and whose section does not — the one a link checker cannot catch and a reader believes"
            exit 1
          else
            echo "OK: validator checks section-qualified citations against the target's headings"
          fi

      - name: Negative self-test (a living document may not restate a stale count)
        run: |
          set -eu
          rm -rf /tmp/count-copy && cp -R . /tmp/count-copy
          python3 - <<'EOF'
          p = "/tmp/count-copy/SKILL-CARD.md"
          t = open(p, encoding="utf-8").read()
          import re
          # The guard rejects ANY hand-written guard count in a living document, so
          # the plant INTRODUCES one rather than editing an existing number. The
          # earlier plant edited the count in place and broke the day the prose
          # stopped restating it — a test coupled to the defect it was written
          # against instead of to the rule.
          assert not re.search(r"\b\d+\+?\s+(?:structural\s+)?guards\b", t), \
              "PLANT MISSED: SKILL-CARD.md already restates a count, so this plants nothing"
          anchor = "## What to check before you trust it"
          assert anchor in t, "PLANT MISSED: anchor heading absent"
          open(p, "w", encoding="utf-8").write(
              t.replace(anchor, anchor + "\n\nThis skill ships 3 guards.\n", 1))
          EOF
          if python3 /tmp/count-copy/test/validate.py; then
            echo "ERROR: validator passed a hand-written guard count that disagrees with the workflow"
            exit 1
          else
            echo "OK: validator computes the guard count instead of trusting the prose"
          fi

      - name: Negative self-test (a README link must ship in the npm package)
        run: |
          set -eu
          rm -rf /tmp/pkg-copy && cp -R . /tmp/pkg-copy
          python3 - <<'EOF'
          import json
          p = "/tmp/pkg-copy/package.json"
          d = json.load(open(p, encoding="utf-8"))
          assert "evals" in d["files"], "PLANT MISSED"
          d["files"] = [f for f in d["files"] if f != "evals"]
          json.dump(d, open(p, "w", encoding="utf-8"), indent=2)
          EOF
          if python3 /tmp/pkg-copy/test/validate.py; then
            echo "ERROR: validator passed a README that links to something files[] does not publish — the link dangles for every npm install"
            exit 1
          else
            echo "OK: validator holds README links to the published package"
          fi

      - name: Negative self-test (adoption must cover the brownfield half)
        run: |
          set -eu
          rm -rf /tmp/adopt-copy && cp -R . /tmp/adopt-copy
          python3 - <<'EOF'
          p = "/tmp/adopt-copy/plugins/task-pipeline/skills/task-pipeline/references/adoption.md"
          t = open(p, encoding="utf-8").read()
          assert "An existing project" in t, "PLANT MISSED"
          open(p, "w", encoding="utf-8").write(t.replace("An existing project", "A different heading"))
          EOF
          if python3 /tmp/adopt-copy/test/validate.py; then
            echo "ERROR: validator passed an adoption doctrine with only the greenfield half — the easy one, and not the one a real repository is in"
            exit 1
          else
            echo "OK: validator requires both entry conditions and the ratchet-baseline step"
          fi

      - name: Negative self-test (default-on needs an opt-out that is exercised)
        run: |
          set -eu
          rm -rf /tmp/optout-copy && cp -R . /tmp/optout-copy
          python3 - <<'EOF'
          import json
          p = "/tmp/optout-copy/evals/task-pipeline.evals.json"
          d = json.load(open(p, encoding="utf-8"))
          n = len(d["evals"])
          d["evals"] = [e for e in d["evals"] if "без пайплайна" not in e.get("query", "")]
          assert len(d["evals"]) < n, "PLANT MISSED"
          json.dump(d, open(p, "w", encoding="utf-8"), indent=2, ensure_ascii=False)
          EOF
          if python3 /tmp/optout-copy/test/validate.py; then
            echo "ERROR: validator passed a description promising an opt-out phrase that no eval exercises — an escape hatch nobody tests is a trap"
            exit 1
          else
            echo "OK: validator ties the description's opt-out to an eval that uses it"
          fi

      - name: Negative self-test (the stage/artifact relation must be mapped both ways)
        run: |
          set -eu
          rm -rf /tmp/artmap-copy && cp -R . /tmp/artmap-copy
          python3 - <<'EOF'
          p = "/tmp/artmap-copy/plugins/task-pipeline/skills/task-pipeline/references/artifacts.md"
          t = open(p, encoding="utf-8").read()
          h = "### Project-saved rules, and where each one binds"
          assert h in t, "PLANT MISSED"
          open(p, "w", encoding="utf-8").write(t.replace(h, "### Other notes", 1))
          EOF
          if python3 /tmp/artmap-copy/test/validate.py; then
            echo "ERROR: validator passed an artifact map with only the write direction — a stage whose inputs are unnamed reads whatever the context happens to hold"
            exit 1
          else
            echo "OK: validator requires stage->inputs, stage->writes and the project-rules map"
          fi

      - name: Negative self-test (a workflow decision must have a home inside the bundle)
        run: |
          set -eu
          rm -rf /tmp/port-copy && cp -R . /tmp/port-copy
          python3 - <<'EOF'
          p = "/tmp/port-copy/plugins/task-pipeline/skills/task-pipeline/references/portability.md"
          t = open(p, encoding="utf-8").read()
          old = "`references/setup.md`"
          assert old in t, "PLANT MISSED"
          open(p, "w", encoding="utf-8").write(t.replace(old, "`../../elsewhere/setup.md`", 1))
          EOF
          if python3 /tmp/port-copy/test/validate.py; then
            echo "ERROR: validator passed a manifest row pointing outside the bundle — a workflow decision that does not travel is a fork nobody named"
            exit 1
          else
            echo "OK: every manifest path must resolve inside the bundle"
          fi

      - name: Negative self-test (the routing rule must ship as a file)
        run: |
          set -eu
          rm -rf /tmp/rule-copy && cp -R . /tmp/rule-copy
          rm /tmp/rule-copy/plugins/task-pipeline/skills/task-pipeline/templates/routing-rule.md
          if python3 /tmp/rule-copy/test/validate.py; then
            echo "ERROR: validator passed with the routing rule hand-installed only — it would not travel with the bundle"
            exit 1
          else
            echo "OK: the routing default ships as a template"
          fi

      - name: Negative self-test (stage 2 must produce user paths and error paths)
        run: |
          set -eu
          rm -rf /tmp/ux2-copy && cp -R . /tmp/ux2-copy
          python3 - <<'EOF'
          p = "/tmp/ux2-copy/plugins/task-pipeline/skills/task-pipeline/references/brainstorm.md"
          t = open(p, encoding="utf-8").read()
          assert "User paths are a design output" in t, "PLANT MISSED"
          open(p, "w", encoding="utf-8").write(t.replace("User paths are a design output", "Some other heading"))
          EOF
          if python3 /tmp/ux2-copy/test/validate.py; then
            echo "ERROR: validator passed a design stage that never names user paths or error paths — the hardest third gets invented at stage 5 by whoever implements it"
            exit 1
          else
            echo "OK: the design stage owns paths, states and error paths"
          fi

      - name: Negative self-test (a new reference must reach the README and the manifest)
        run: |
          set -eu
          rm -rf /tmp/reach-copy && cp -R . /tmp/reach-copy
          python3 - <<'EOF'
          p = "/tmp/reach-copy/README.md"
          t = open(p, encoding="utf-8").read()
          assert "references/setup.md" in t, "PLANT MISSED"
          open(p, "w", encoding="utf-8").write(t.replace("references/setup.md", "references/gone.md"))
          EOF
          if python3 /tmp/reach-copy/test/validate.py; then
            echo "ERROR: validator passed a reference nothing tells a reader about — reachability from SKILL.md proves an agent can find it, not that anybody was told it exists"
            exit 1
          else
            echo "OK: every reference must reach the README map and the portability manifest"
          fi

      - name: Negative self-test (a long seeded template needs its own Contents)
        run: |
          set -eu
          rm -rf /tmp/tpltoc-copy && cp -R . /tmp/tpltoc-copy
          python3 - <<'EOF'
          p = "/tmp/tpltoc-copy/plugins/task-pipeline/skills/task-pipeline/templates/docmap.md"
          t = open(p, encoding="utf-8").read()
          assert "## Contents" in t, "PLANT MISSED"
          open(p, "w", encoding="utf-8").write(t.replace("## Contents", "## Overview", 1))
          EOF
          if python3 /tmp/tpltoc-copy/test/validate.py; then
            echo "ERROR: validator passed a 100+ line seeded template with no Contents — a host project reads that file and a partial read shows whichever sections come first"
            exit 1
          else
            echo "OK: seeded templates over 100 lines carry a Contents list"
          fi

      - name: Negative self-test (an invariant may not claim a guard that does not exist)
        run: |
          set -eu
          rm -rf /tmp/inv-copy && cp -R . /tmp/inv-copy
          python3 - <<'EOF'
          p = "/tmp/inv-copy/CONTRIBUTING.md"
          t = open(p, encoding="utf-8").read()
          old = "(guard: `manifest names`)"
          assert old in t, "PLANT MISSED"
          open(p, "w", encoding="utf-8").write(t.replace(old, "(guard: `a check nobody wrote`)", 1))
          EOF
          if python3 /tmp/inv-copy/test/validate.py; then
            echo "ERROR: validator passed an invariant list claiming an enforcement that does not exist — the list says it IS what the validator enforces"
            exit 1
          else
            echo "OK: every cited invariant names a guard this validator actually prints"
          fi

      - name: Negative self-test (a seeded matrix without the meta-row has a hole)
        run: |
          set -eu
          rm -rf /tmp/meta-copy && cp -R . /tmp/meta-copy
          python3 - <<'EOF'
          p = "/tmp/meta-copy/plugins/task-pipeline/skills/task-pipeline/templates/docmap.md"
          t = open(p, encoding="utf-8").read()
          assert "A new document or rule" in t, "PLANT MISSED"
          t = t.replace("A new document or rule", "A different change type", 1)
          t = t.replace("adding a document", "adding a thing")
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/meta-copy/test/validate.py; then
            echo "ERROR: validator passed a propagation matrix with no row for adding a document — nine findings across five audits were that one missing row"
            exit 1
          else
            echo "OK: the seeded matrix must carry the meta-row"
          fi

      - name: Negative self-test (the example must set run.loop.mode explicitly)
        run: |
          set -eu
          rm -rf /tmp/runmode-copy && cp -R . /tmp/runmode-copy
          python3 - <<'EOF'
          import json
          p = "/tmp/runmode-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.example.json"
          d = json.load(open(p, encoding="utf-8"))
          # Planted through the parser, not a literal: the previous needle was the
          # one-line `"loop": { "mode": "off", ... }` and it stopped matching the moment
          # the block grew a queue and an arm point. A probe keyed to formatting tests
          # the formatting.
          assert d.get("run", {}).get("loop", {}).get("mode"), "PLANT DID NOT LAND: no run.loop.mode to remove"
          del d["run"]["loop"]["mode"]
          json.dump(d, open(p, "w", encoding="utf-8"), ensure_ascii=False, indent=2)
          EOF
          if python3 /tmp/runmode-copy/test/validate.py 2>&1 | grep -q "the schema's legal set is"; then
            echo "OK: the example must DEMONSTRATE the default, not rely on its absence"
          else
            echo "ERROR: validator passed an example with no run.loop.mode — the example is what gets copied, so an absent field reads as an oversight rather than a decision"
            exit 1
          fi
      - name: Negative self-test (a run-wide rule every stage must name)
        run: |
          set -eu
          rm -rf /tmp/reach-copy-2 && cp -R . /tmp/reach-copy-2
          python3 - <<'EOF'
          p = "/tmp/reach-copy-2/plugins/task-pipeline/skills/task-pipeline/references/build.md"
          t = open(p, encoding="utf-8").read()
          assert "[`continuity.md`](continuity.md)" in t, "PLANT MISSED"
          open(p, "w", encoding="utf-8").write(
              t.replace("[`continuity.md`](continuity.md)", "`elsewhere`"))
          EOF
          if python3 /tmp/reach-copy-2/test/validate.py; then
            echo "ERROR: validator passed a stage doctrine that never names continuity.md — the continuous-execution rule sat inside build.md for nine releases and did not work for exactly this reason"
            exit 1
          else
            echo "OK: every stage that could be misled by the run mode names it"
          fi

      - name: Negative self-test (the context-evidence clause may not be softened)
        run: |
          set -eu
          rm -rf /tmp/clause1-copy && cp -R . /tmp/clause1-copy
          python3 - <<'EOF'
          p = "/tmp/clause1-copy/plugins/task-pipeline/skills/task-pipeline/references/continuity.md"
          t = open(p, encoding="utf-8").read()
          c = "**never announce that\nthe context is nearly spent without one of those signals**"
          assert c in t, "PLANT MISSED"
          open(p, "w", encoding="utf-8").write(t.replace(c, "**stay quiet**"))
          EOF
          if python3 /tmp/clause1-copy/test/validate.py; then
            echo "ERROR: validator passed continuity.md with the evidence clause removed — without it the rule degrades to a suggestion and the guess comes back"
            exit 1
          else
            echo "OK: the evidence clause is contractual"
          fi

      - name: Negative self-test (the harness-limit clause may not be softened)
        run: |
          set -eu
          rm -rf /tmp/clause2-copy && cp -R . /tmp/clause2-copy
          python3 - <<'EOF'
          p = "/tmp/clause2-copy/plugins/task-pipeline/skills/task-pipeline/references/continuity.md"
          t = open(p, encoding="utf-8").read()
          assert "Claude Code only" in t, "PLANT MISSED"
          open(p, "w", encoding="utf-8").write(t.replace("Claude Code only", "available everywhere"))
          EOF
          if python3 /tmp/clause2-copy/test/validate.py; then
            echo "ERROR: validator passed continuity.md claiming the loop primitive is universal — a run that reports itself as looping while nothing is scheduled claims a capability it does not have"
            exit 1
          else
            echo "OK: the harness limit is stated, not implied"
          fi

      - name: Negative self-test (a seeded template may not carry a relative link)
        run: |
          set -eu
          rm -rf /tmp/tpl-copy && cp -R . /tmp/tpl-copy
          python3 - <<'EOF'
          p = "/tmp/tpl-copy/plugins/task-pipeline/skills/task-pipeline/templates/brief.md"
          t = open(p, encoding="utf-8").read()
          assert "# Task brief" in t, "PLANT MISSED"
          open(p, "w", encoding="utf-8").write(
              t.replace("# Task brief",
                        "See [`audit.md`](../references/audit.md).\n\n# Task brief", 1))
          EOF
          if python3 /tmp/tpl-copy/test/validate.py; then
            echo "ERROR: validator passed a template carrying a relative link — it resolves from templates/ and from nowhere the file is ever actually read"
            exit 1
          else
            echo "OK: a seeded template is self-contained"
          fi

      - name: install.sh syntax check
        run: bash -n install.sh

      - name: Workflow YAML parses (validate + release)
        run: |
          python3 -m pip install --quiet pyyaml
          python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml')); yaml.safe_load(open('.github/workflows/validate.yml')); print('workflows parse OK')"

      - name: npm installer CLI check
        run: node --check bin/task-pipeline.js

      - name: npm installer functional test (fresh / rerun-skip / --force)
        run: |
          set -eu
          export HOME=/tmp/fakehome
          mkdir -p "$HOME"
          node bin/task-pipeline.js
          test -f "$HOME/.claude/skills/task-pipeline/SKILL.md"
          test -f "$HOME/.claude/commands/task-pipeline.md"
          node bin/task-pipeline.js | grep -q '^skip:'
          node bin/task-pipeline.js --force | grep -q '^Installed'
          if node bin/task-pipeline.js --wat; then
            echo "ERROR: unknown arg accepted"; exit 1
          fi
          echo "OK: npm installer behaves"
      - name: Official Claude Code conformance (plugin + marketplace, strict)
        run: |
          set -eu
          # The repo's own validator enforces house rules; this one is the
          # upstream spec. --strict fails on unrecognized manifest fields and on
          # front matter that silently drops at runtime -- both were real here.
          npm install -g @anthropic-ai/claude-code
          claude plugin validate . --strict
          claude plugin validate plugins/task-pipeline --strict
      - name: Negative self-test (the hygiene gate catches conflict markers)
        run: |
          set -eu
          rm -rf /tmp/hyg1 && mkdir -p /tmp/hyg1
          cp plugins/task-pipeline/skills/task-pipeline/templates/hygiene.sh /tmp/hyg1/gate.sh
          echo "clean" > /tmp/hyg1/base.md
          cd /tmp/hyg1
          python3 - <<'EOF'
          open('c1.md','w').write('a\n<<<<<<< HEAD\nx\n=======\ny\n>>>>>>> b\n')
          EOF
          test -s c1.md || { echo "ERROR: plant did not land — c1.md is empty"; exit 1; }
          if bash gate.sh; then
            echo "ERROR: hygiene gate passed with conflict markers present"
            exit 1
          else
            echo "OK: hygiene gate rejects conflict markers"
          fi
      - name: Negative self-test (the hygiene gate catches a surviving placeholder)
        run: |
          set -eu
          rm -rf /tmp/hyg2 && mkdir -p /tmp/hyg2
          cp plugins/task-pipeline/skills/task-pipeline/templates/hygiene.sh /tmp/hyg2/gate.sh
          echo "clean" > /tmp/hyg2/base.md
          cd /tmp/hyg2
          python3 - <<'EOF'
          open('c2.py','w').write('# TODO: finish this\ndef f(): pass\n')
          EOF
          test -s c2.py || { echo "ERROR: plant did not land — c2.py is empty"; exit 1; }
          if bash gate.sh; then
            echo "ERROR: hygiene gate passed with a surviving placeholder present"
            exit 1
          else
            echo "OK: hygiene gate rejects a surviving placeholder"
          fi
      - name: Negative self-test (the hygiene gate catches an unterminated fence)
        run: |
          set -eu
          rm -rf /tmp/hyg3 && mkdir -p /tmp/hyg3
          cp plugins/task-pipeline/skills/task-pipeline/templates/hygiene.sh /tmp/hyg3/gate.sh
          echo "clean" > /tmp/hyg3/base.md
          cd /tmp/hyg3
          python3 - <<'EOF'
          open('c3.md','w').write('# t\n\n```python\nx = 1\n')
          EOF
          test -s c3.md || { echo "ERROR: plant did not land — c3.md is empty"; exit 1; }
          if bash gate.sh; then
            echo "ERROR: hygiene gate passed with an unterminated fence present"
            exit 1
          else
            echo "OK: hygiene gate rejects an unterminated fence"
          fi
      - name: Negative self-test (the hygiene gate catches a truncation stub)
        run: |
          set -eu
          rm -rf /tmp/hyg4 && mkdir -p /tmp/hyg4
          cp plugins/task-pipeline/skills/task-pipeline/templates/hygiene.sh /tmp/hyg4/gate.sh
          echo "clean" > /tmp/hyg4/base.md
          cd /tmp/hyg4
          python3 - <<'EOF'
          open('c4.js','w').write('function a() {}\n// ... existing code ...\n')
          EOF
          test -s c4.js || { echo "ERROR: plant did not land — c4.js is empty"; exit 1; }
          if bash gate.sh; then
            echo "ERROR: hygiene gate passed with a truncation stub present"
            exit 1
          else
            echo "OK: hygiene gate rejects a truncation stub"
          fi
      - name: Negative self-test (the hygiene gate catches a duplicated adjacent block)
        run: |
          set -eu
          rm -rf /tmp/hyg5 && mkdir -p /tmp/hyg5
          cp plugins/task-pipeline/skills/task-pipeline/templates/hygiene.sh /tmp/hyg5/gate.sh
          echo "clean" > /tmp/hyg5/base.md
          cd /tmp/hyg5
          python3 - <<'EOF'
          open('c5.md','w').write('# t\n\nalpha\nbeta\ngamma\n\nalpha\nbeta\ngamma\n\ntail\n')
          EOF
          test -s c5.md || { echo "ERROR: plant did not land — c5.md is empty"; exit 1; }
          if bash gate.sh; then
            echo "ERROR: hygiene gate passed with a duplicated adjacent block present"
            exit 1
          else
            echo "OK: hygiene gate rejects a duplicated adjacent block"
          fi
      - name: Negative self-test (the hygiene gate catches an empty section)
        run: |
          set -eu
          rm -rf /tmp/hyg6 && mkdir -p /tmp/hyg6
          cp plugins/task-pipeline/skills/task-pipeline/templates/hygiene.sh /tmp/hyg6/gate.sh
          echo "clean" > /tmp/hyg6/base.md
          cd /tmp/hyg6
          python3 - <<'EOF'
          open('c6.md','w').write('# t\n\n## One\n\n## Two\n\nbody\n')
          EOF
          test -s c6.md || { echo "ERROR: plant did not land — c6.md is empty"; exit 1; }
          if bash gate.sh; then
            echo "ERROR: hygiene gate passed with an empty section present"
            exit 1
          else
            echo "OK: hygiene gate rejects an empty section"
          fi
      - name: Negative self-test (a gate script must keep its VERDICT block last)
        run: |
          set -eu
          rm -rf /tmp/hyg-verdict-copy && cp -R . /tmp/hyg-verdict-copy
          python3 - <<'EOF'
          p = "/tmp/hyg-verdict-copy/plugins/task-pipeline/skills/task-pipeline/templates/hygiene.sh"
          t = open(p, encoding="utf-8").read()
          marker = "# ---------- VERDICT"
          assert marker in t, "plant would not land: no VERDICT section marker"
          t2 = t.replace(marker, "# ---------- verdict-renamed")
          assert marker not in t2, "plant did not land"
          open(p, "w", encoding="utf-8").write(t2)
          EOF
          if python3 /tmp/hyg-verdict-copy/test/validate.py; then
            echo "ERROR: validator passed with the VERDICT block renamed"
            exit 1
          else
            echo "OK: validator requires the VERDICT block, on every gate script"
          fi
      - name: Negative self-test (two self-tests may not share a scratch directory)
        run: |
          set -eu
          rm -rf /tmp/scratchdupe-copy && cp -R . /tmp/scratchdupe-copy
          python3 - <<'EOF'
          p = "/tmp/scratchdupe-copy/.github/workflows/validate.yml"
          t = open(p, encoding="utf-8").read()
          t2 = t.replace("rm -rf /tmp/hyg-verdict-copy && cp -R . /tmp/hyg-verdict-copy",
                         "cp -R . /tmp/rel-copy", 1)
          assert t2.count("cp -R . /tmp/rel-copy") >= 2, "plant did not land"
          open(p, "w", encoding="utf-8").write(t2)
          EOF
          if python3 /tmp/scratchdupe-copy/test/validate.py; then
            echo "ERROR: validator passed with two self-tests sharing a scratch dir"
            exit 1
          else
            echo "OK: validator rejects a reused scratch directory"
          fi
      - name: Negative self-test (the spec must keep asking whether a named check is real)
        run: |
          set -eu
          rm -rf /tmp/readback-spec-copy && cp -R . /tmp/readback-spec-copy
          python3 - <<'EOF'
          p = "/tmp/readback-spec-copy/plugins/task-pipeline/skills/task-pipeline/references/spec.md"
          t = open(p, encoding="utf-8").read()
          n = "Every check this spec names resolves"
          assert n in t, "plant would not land: needle absent"
          t2 = t.replace(n, "removed-by-probe", 1)
          assert n not in t2, "plant did not land"
          open(p, "w", encoding="utf-8").write(t2)
          EOF
          if python3 /tmp/readback-spec-copy/test/validate.py; then
            echo "ERROR: validator passed without 'Every check this spec names resolves'"
            exit 1
          else
            echo "OK: validator requires it"
          fi
      - name: Negative self-test (a plan must keep asking whether a DoD's targets resolve)
        run: |
          set -eu
          rm -rf /tmp/readback-plan-copy && cp -R . /tmp/readback-plan-copy
          python3 - <<'EOF'
          p = "/tmp/readback-plan-copy/plugins/task-pipeline/skills/task-pipeline/references/planning.md"
          t = open(p, encoding="utf-8").read()
          n = "Every command, path and file a DoD names resolves"
          assert n in t, "plant would not land: needle absent"
          t2 = t.replace(n, "removed-by-probe", 1)
          assert n not in t2, "plant did not land"
          open(p, "w", encoding="utf-8").write(t2)
          EOF
          if python3 /tmp/readback-plan-copy/test/validate.py; then
            echo "ERROR: validator passed without 'Every command, path and file a DoD names resolves'"
            exit 1
          else
            echo "OK: validator requires it"
          fi
      - name: Negative self-test (the self-review must leave a committed trace)
        run: |
          set -eu
          rm -rf /tmp/readback-trace-copy && cp -R . /tmp/readback-trace-copy
          python3 - <<'EOF'
          p = "/tmp/readback-trace-copy/plugins/task-pipeline/skills/task-pipeline/references/spec.md"
          t = open(p, encoding="utf-8").read()
          n = "computed number, not a tick"
          assert n in t, "plant would not land: needle absent"
          t2 = t.replace(n, "removed-by-probe", 1)
          assert n not in t2, "plant did not land"
          open(p, "w", encoding="utf-8").write(t2)
          EOF
          if python3 /tmp/readback-trace-copy/test/validate.py; then
            echo "ERROR: validator passed without 'computed number, not a tick'"
            exit 1
          else
            echo "OK: validator requires it"
          fi
      - name: Negative self-test (rule 14 must bind the stages that write the targets)
        run: |
          set -eu
          rm -rf /tmp/readback-map-copy && cp -R . /tmp/readback-map-copy
          python3 - <<'EOF'
          p = "/tmp/readback-map-copy/plugins/task-pipeline/skills/task-pipeline/references/learned.md"
          t = open(p, encoding="utf-8").read()
          n = "| 3 Spec | 14"
          assert n in t, "plant would not land"
          t2 = t.replace(n, "| 3 Spec | none", 1)
          assert n not in t2, "plant did not land"
          open(p, "w", encoding="utf-8").write(t2)
          EOF
          if python3 /tmp/readback-map-copy/test/validate.py; then
            echo "ERROR: validator passed with rule 14 unmapped at stage 3"
            exit 1
          else
            echo "OK: validator requires rule 14 at the stages that write targets"
          fi
      - name: Negative self-test (the False success section must exist)
        run: |
          set -eu
          rm -rf /tmp/fs01-copy && cp -R . /tmp/fs01-copy
          python3 - <<'EOF'
          p = "/tmp/fs01-copy/plugins/task-pipeline/skills/task-pipeline/references/gates.md"
          t = open(p, encoding="utf-8").read()
          assert '## False success — when a mechanism reports a win it never checked' in t, "PLANT DID NOT LAND: needle absent before edit"
          t = t.replace('## False success — when a mechanism reports a win it never checked', '## Something else entirely')
          t = t.replace('- False success — when a mechanism reports a win it never checked', '- Something else entirely')
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/fs01-copy/test/validate.py; then
            echo "ERROR: validator passed with the False success section must exist broken"
            exit 1
          else
            echo "OK: validator correctly FAILs — the False success section must exist"
          fi
      - name: Negative self-test (the False success law must be stated)
        run: |
          set -eu
          rm -rf /tmp/fs02-copy && cp -R . /tmp/fs02-copy
          python3 - <<'EOF'
          p = "/tmp/fs02-copy/plugins/task-pipeline/skills/task-pipeline/references/gates.md"
          t = open(p, encoding="utf-8").read()
          assert 're-reading the state it changed' in t, "PLANT DID NOT LAND: needle absent before edit"
          t = t.replace('re-reading the state it changed', 'asking again politely')
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/fs02-copy/test/validate.py; then
            echo "ERROR: validator passed with the False success law must be stated broken"
            exit 1
          else
            echo "OK: validator correctly FAILs — the False success law must be stated"
          fi
      - name: Negative self-test (the did-not-look test must survive)
        run: |
          set -eu
          rm -rf /tmp/fs03-copy && cp -R . /tmp/fs03-copy
          python3 - <<'EOF'
          p = "/tmp/fs03-copy/plugins/task-pipeline/skills/task-pipeline/references/gates.md"
          t = open(p, encoding="utf-8").read()
          assert 'what does it print when it did not look' in t, "PLANT DID NOT LAND: needle absent before edit"
          t = t.replace('what does it print when it did not look', 'what does it print')
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/fs03-copy/test/validate.py; then
            echo "ERROR: validator passed with the did-not-look test must survive broken"
            exit 1
          else
            echo "OK: validator correctly FAILs — the did-not-look test must survive"
          fi
      - name: Negative self-test (rule 1 must survive)
        run: |
          set -eu
          rm -rf /tmp/fs04-copy && cp -R . /tmp/fs04-copy
          python3 - <<'EOF'
          p = "/tmp/fs04-copy/plugins/task-pipeline/skills/task-pipeline/references/gates.md"
          t = open(p, encoding="utf-8").read()
          assert 'Verify by re-reading, not by the reply' in t, "PLANT DID NOT LAND: needle absent before edit"
          t = t.replace('Verify by re-reading, not by the reply', 'Verify carefully')
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/fs04-copy/test/validate.py; then
            echo "ERROR: validator passed with rule 1 must survive broken"
            exit 1
          else
            echo "OK: validator correctly FAILs — rule 1 must survive"
          fi
      - name: Negative self-test (rule 2 must survive)
        run: |
          set -eu
          rm -rf /tmp/fs05-copy && cp -R . /tmp/fs05-copy
          python3 - <<'EOF'
          p = "/tmp/fs05-copy/plugins/task-pipeline/skills/task-pipeline/references/gates.md"
          t = open(p, encoding="utf-8").read()
          assert 'Assert the absence of the old, not the presence of the new' in t, "PLANT DID NOT LAND: needle absent before edit"
          t = t.replace('Assert the absence of the old, not the presence of the new', 'Assert the new value')
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/fs05-copy/test/validate.py; then
            echo "ERROR: validator passed with rule 2 must survive broken"
            exit 1
          else
            echo "OK: validator correctly FAILs — rule 2 must survive"
          fi
      - name: Negative self-test (audit.md must cite the class)
        run: |
          set -eu
          rm -rf /tmp/fs06-copy && cp -R . /tmp/fs06-copy
          python3 - <<'EOF'
          p = "/tmp/fs06-copy/plugins/task-pipeline/skills/task-pipeline/references/audit.md"
          t = open(p, encoding="utf-8").read()
          assert '*False success*' in t, "PLANT DID NOT LAND: needle absent before edit"
          t = t.replace('*False success*', '*that other thing*')
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/fs06-copy/test/validate.py; then
            echo "ERROR: validator passed with audit.md must cite the class broken"
            exit 1
          else
            echo "OK: validator correctly FAILs — audit.md must cite the class"
          fi
      - name: Negative self-test (build.md must cite the class)
        run: |
          set -eu
          rm -rf /tmp/fs07-copy && cp -R . /tmp/fs07-copy
          python3 - <<'EOF'
          p = "/tmp/fs07-copy/plugins/task-pipeline/skills/task-pipeline/references/build.md"
          t = open(p, encoding="utf-8").read()
          assert '*False success*' in t, "PLANT DID NOT LAND: needle absent before edit"
          t = t.replace('*False success*', '*that other thing*')
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/fs07-copy/test/validate.py; then
            echo "ERROR: validator passed with build.md must cite the class broken"
            exit 1
          else
            echo "OK: validator correctly FAILs — build.md must cite the class"
          fi
      - name: Negative self-test (review.md must cite the class)
        run: |
          set -eu
          rm -rf /tmp/fs08-copy && cp -R . /tmp/fs08-copy
          python3 - <<'EOF'
          p = "/tmp/fs08-copy/plugins/task-pipeline/skills/task-pipeline/references/review.md"
          t = open(p, encoding="utf-8").read()
          assert '*False success*' in t, "PLANT DID NOT LAND: needle absent before edit"
          t = t.replace('*False success*', '*that other thing*')
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/fs08-copy/test/validate.py; then
            echo "ERROR: validator passed with review.md must cite the class broken"
            exit 1
          else
            echo "OK: validator correctly FAILs — review.md must cite the class"
          fi
      - name: Negative self-test (continuity.md must cite the class)
        run: |
          set -eu
          rm -rf /tmp/fs09-copy && cp -R . /tmp/fs09-copy
          python3 - <<'EOF'
          p = "/tmp/fs09-copy/plugins/task-pipeline/skills/task-pipeline/references/continuity.md"
          t = open(p, encoding="utf-8").read()
          assert '*False success*' in t, "PLANT DID NOT LAND: needle absent before edit"
          t = t.replace('*False success*', '*that other thing*')
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/fs09-copy/test/validate.py; then
            echo "ERROR: validator passed with continuity.md must cite the class broken"
            exit 1
          else
            echo "OK: validator correctly FAILs — continuity.md must cite the class"
          fi
      - name: Negative self-test (the report must require verified-by lines)
        run: |
          set -eu
          rm -rf /tmp/fs10-copy && cp -R . /tmp/fs10-copy
          python3 - <<'EOF'
          p = "/tmp/fs10-copy/plugins/task-pipeline/skills/task-pipeline/references/build.md"
          t = open(p, encoding="utf-8").read()
          assert '`verified-by:` line' in t, "PLANT DID NOT LAND: needle absent before edit"
          t = t.replace('`verified-by:` line', '`looks-fine:` line')
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/fs10-copy/test/validate.py; then
            echo "ERROR: validator passed with the report must require verified-by lines broken"
            exit 1
          else
            echo "OK: validator correctly FAILs — the report must require verified-by lines"
          fi
      - name: Negative self-test (the implementer contract keeps the re-reading clause)
        run: |
          set -eu
          rm -rf /tmp/fs11-copy && cp -R . /tmp/fs11-copy
          python3 - <<'EOF'
          p = "/tmp/fs11-copy/plugins/task-pipeline/skills/task-pipeline/references/build.md"
          t = open(p, encoding="utf-8").read()
          assert 'confirmed by re-reading the state it changed' in t, "PLANT DID NOT LAND: needle absent before edit"
          t = t.replace('confirmed by re-reading the state it changed', "confirmed by the tool's reply")
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/fs11-copy/test/validate.py; then
            echo "ERROR: validator passed with the implementer contract keeps the re-reading clause broken"
            exit 1
          else
            echo "OK: validator correctly FAILs — the implementer contract keeps the re-reading clause"
          fi
      - name: Negative self-test (the hygiene gate must name its blind side)
        run: |
          set -eu
          rm -rf /tmp/fs12-copy && cp -R . /tmp/fs12-copy
          python3 - <<'EOF'
          p = "/tmp/fs12-copy/plugins/task-pipeline/skills/task-pipeline/references/build.md"
          t = open(p, encoding="utf-8").read()
          assert 'cannot see what the task did' in t, "PLANT DID NOT LAND: needle absent before edit"
          t = t.replace('cannot see what the task did', 'sees everything')
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/fs12-copy/test/validate.py; then
            echo "ERROR: validator passed with the hygiene gate must name its blind side broken"
            exit 1
          else
            echo "OK: validator correctly FAILs — the hygiene gate must name its blind side"
          fi
      - name: Negative self-test (the rubric keeps the Effect verification item)
        run: |
          set -eu
          rm -rf /tmp/fs13-copy && cp -R . /tmp/fs13-copy
          python3 - <<'EOF'
          p = "/tmp/fs13-copy/plugins/task-pipeline/skills/task-pipeline/references/review.md"
          t = open(p, encoding="utf-8").read()
          assert '**Effect verification.**' in t, "PLANT DID NOT LAND: needle absent before edit"
          t = t.replace('**Effect verification.**', '**Vibes check.**')
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/fs13-copy/test/validate.py; then
            echo "ERROR: validator passed with the rubric keeps the Effect verification item broken"
            exit 1
          else
            echo "OK: validator correctly FAILs — the rubric keeps the Effect verification item"
          fi
      - name: Negative self-test (an unverified effect must stay Important)
        run: |
          set -eu
          rm -rf /tmp/fs14-copy && cp -R . /tmp/fs14-copy
          python3 - <<'EOF'
          p = "/tmp/fs14-copy/plugins/task-pipeline/skills/task-pipeline/references/review.md"
          t = open(p, encoding="utf-8").read()
          assert 'unverified claim, and it is **Important**' in t, "PLANT DID NOT LAND: needle absent before edit"
          t = t.replace('unverified claim, and it is **Important**', 'unverified claim, and it is **Minor**')
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/fs14-copy/test/validate.py; then
            echo "ERROR: validator passed with an unverified effect must stay Important broken"
            exit 1
          else
            echo "OK: validator correctly FAILs — an unverified effect must stay Important"
          fi
      - name: Negative self-test (a guard below the verdict must be rejected)
        run: |
          set -eu
          rm -rf /tmp/fs15-copy && cp -R . /tmp/fs15-copy
          python3 - <<'EOF'
          p = "/tmp/fs15-copy/test/validate.py"
          t = open(p, encoding="utf-8").read()
          needle = 'print("PASS: task-pipeline structure valid")'
          assert needle in t, "PLANT DID NOT LAND: verdict line absent"
          t = t.replace(needle, needle + '\nfail("a check that can never run")')
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/fs15-copy/test/validate.py; then
            echo "ERROR: validator passed with a guard below the verdict"
            exit 1
          else
            echo "OK: validator correctly FAILs — a guard below the verdict"
          fi
      - name: Negative self-test (stage 0's config gate must require the measured lag)
        run: |
          set -eu
          rm -rf /tmp/gs01-copy && cp -R . /tmp/gs01-copy
          python3 - <<'EOF'
          p = "/tmp/gs01-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.example.json"
          t = open(p, encoding="utf-8").read()
          assert "THE GRAPH'S ROW CARRIES ITS MEASURED LAG" in t, "PLANT DID NOT LAND: needle absent before edit"
          t = t.replace("THE GRAPH'S ROW CARRIES ITS MEASURED LAG", "the graph's row carries its build date")
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/gs01-copy/test/validate.py; then
            echo "ERROR: validator passed with stage 0's gate no longer requiring the measured lag"
            exit 1
          else
            echo "OK: validator correctly FAILs — stage 0's config gate must require the measured lag"
          fi
      - name: Negative self-test (stage 0's own section must state the measured lag)
        run: |
          set -eu
          rm -rf /tmp/gs02-copy && cp -R . /tmp/gs02-copy
          python3 - <<'EOF'
          p = "/tmp/gs02-copy/plugins/task-pipeline/skills/task-pipeline/references/stages.md"
          t = open(p, encoding="utf-8").read()
          assert t.count("measured lag") == 2, "PLANT DID NOT LAND: expected 2 needles before edit"
          t = t.replace("measured lag", "build date")
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/gs02-copy/test/validate.py; then
            echo "ERROR: validator passed with stage 0's section no longer stating the measured lag"
            exit 1
          else
            echo "OK: validator correctly FAILs — stage 0's own section must state the measured lag"
          fi
      - name: Negative self-test (the measured-lag rule must keep its commands)
        run: |
          set -eu
          rm -rf /tmp/gs03-copy && cp -R . /tmp/gs03-copy
          python3 - <<'EOF'
          p = "/tmp/gs03-copy/plugins/task-pipeline/skills/task-pipeline/references/knowledge-graph.md"
          t = open(p, encoding="utf-8").read()
          assert "git rev-list --count" in t, "PLANT DID NOT LAND: needle absent before edit"
          t = t.replace("git rev-list --count", "look at how old it seems")
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/gs03-copy/test/validate.py; then
            echo "ERROR: validator passed with the measured-lag rule missing its counting command"
            exit 1
          else
            echo "OK: validator correctly FAILs — the measured-lag rule must keep its commands"
          fi
      - name: Negative self-test (a graph that cannot be measured needs its own state)
        run: |
          set -eu
          rm -rf /tmp/gs04-copy && cp -R . /tmp/gs04-copy
          python3 - <<'EOF'
          p = "/tmp/gs04-copy/plugins/task-pipeline/skills/task-pipeline/references/knowledge-graph.md"
          t = open(p, encoding="utf-8").read()
          assert "**unresolvable**" in t, "PLANT DID NOT LAND: needle absent before edit"
          t = t.replace("**unresolvable**", "**probably fine**")
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/gs04-copy/test/validate.py; then
            echo "ERROR: validator passed with the unresolvable state removed"
            exit 1
          else
            echo "OK: validator correctly FAILs — a graph that cannot be measured needs its own state"
          fi
      - name: Negative self-test (the seeded brief must not ship a bare build date)
        run: |
          set -eu
          rm -rf /tmp/gs05-copy && cp -R . /tmp/gs05-copy
          python3 - <<'EOF'
          p = "/tmp/gs05-copy/plugins/task-pipeline/skills/task-pipeline/templates/brief.md"
          t = open(p, encoding="utf-8").read()
          needle = "built `<sha>` — N commits / M days behind HEAD, signal: built_at_commit (exact) — ⚠ not trusted for reach until refreshed"
          assert needle in t, "PLANT DID NOT LAND: needle absent before edit"
          t = t.replace(needle, "built YYYY-MM-DD")
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/gs05-copy/test/validate.py; then
            echo "ERROR: validator passed with the seeded brief shipping a bare build date"
            exit 1
          else
            echo "OK: validator correctly FAILs — the seeded brief must not ship a bare build date"
          fi
      - name: Negative self-test (the distrust marker keeps its sigil, even wrapped)
        run: |
          set -eu
          rm -rf /tmp/gs06-copy && cp -R . /tmp/gs06-copy
          python3 - <<'EOF'
          p = "/tmp/gs06-copy/README.md"
          t = open(p, encoding="utf-8").read()
          needle = "`⚠ not trusted for reach until\nrefreshed`"
          assert needle in t, "PLANT DID NOT LAND: the wrapped marker is absent before edit"
          t = t.replace(needle, "`not trusted for reach until\nrefreshed`")
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/gs06-copy/test/validate.py; then
            echo "ERROR: validator passed with the marker's sigil dropped across a line wrap"
            exit 1
          else
            echo "OK: validator correctly FAILs — the distrust marker keeps its sigil, even wrapped"
          fi
      - name: Negative self-test (the distrust marker gets no second spelling)
        run: |
          set -eu
          rm -rf /tmp/gs07-copy && cp -R . /tmp/gs07-copy
          python3 - <<'EOF'
          p = "/tmp/gs07-copy/plugins/task-pipeline/skills/task-pipeline/references/knowledge-graph.md"
          t = open(p, encoding="utf-8").read()
          needle = "signal: none — ⚠ not trusted for reach until refreshed"
          assert needle in t, "PLANT DID NOT LAND: the unresolvable row's marker is absent before edit"
          t = t.replace(needle, "signal: none — treat as stale until refreshed")
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/gs07-copy/test/validate.py; then
            echo "ERROR: validator passed with a second spelling of the distrust marker"
            exit 1
          else
            echo "OK: validator correctly FAILs — the distrust marker gets no second spelling"
          fi
      - name: Negative self-test (the hygiene gate catches a blank line inside a table)
        run: |
          set -eu
          rm -rf /tmp/hyg7 && mkdir -p /tmp/hyg7
          cp plugins/task-pipeline/skills/task-pipeline/templates/hygiene.sh /tmp/hyg7/gate.sh
          cd /tmp/hyg7
          python3 - <<'EOF'
          open('t7.md','w').write(
              '# Ledger\n\n| id | what |\n|---|---|\n| 1 | first |\n\n| 2 | second |\n')
          EOF
          test -s t7.md || { echo "ERROR: plant did not land — t7.md is empty"; exit 1; }
          if bash gate.sh; then
            echo "ERROR: hygiene gate passed with a blank line splitting a table"
            exit 1
          else
            echo "OK: hygiene gate rejects a blank line inside a table"
          fi
      - name: Negative self-test (the hygiene gate refuses an undeclared floor)
        run: |
          set -eu
          rm -rf /tmp/hyg8 && mkdir -p /tmp/hyg8
          cp plugins/task-pipeline/skills/task-pipeline/templates/hygiene.sh /tmp/hyg8/gate.sh
          cd /tmp/hyg8
          python3 - <<'EOF'
          p = 'gate.sh'
          t = open(p, encoding='utf-8').read()
          needle = 'HYGIENE_FLOOR_7=${HYGIENE_FLOOR_7:-0}\n'
          assert needle in t, "PLANT DID NOT LAND: floor 7 is not declared in the gate"
          open(p, 'w', encoding='utf-8').write(t.replace(needle, ''))
          open('t8.md','w').write('# Ledger\n\n| id | what |\n|---|---|\n| 1 | first |\n\n| 2 | second |\n')
          EOF
          if bash gate.sh; then
            echo "ERROR: hygiene gate passed while judging a check whose floor is undeclared"
            exit 1
          else
            echo "OK: hygiene gate refuses an undeclared floor instead of printing ok"
          fi
      - name: Negative self-test (the canon list must exist)
        run: |
          set -eu
          rm -rf /tmp/cn1-copy && cp -R . /tmp/cn1-copy
          python3 - <<'EOF'
          p = "/tmp/cn1-copy/plugins/task-pipeline/skills/task-pipeline/references/documentation.md"
          t = open(p, encoding="utf-8").read()
          assert '## The canons' in t, "PLANT DID NOT LAND: needle absent before edit"
          open(p, "w", encoding="utf-8").write(t.replace('## The canons', '## Some notes'))
          EOF
          if python3 /tmp/cn1-copy/test/validate.py; then
            echo "ERROR: validator passed with the canon list must exist broken"
            exit 1
          else
            echo "OK: validator correctly FAILs — the canon list must exist"
          fi
      - name: Negative self-test (a dropped canon must be caught)
        run: |
          set -eu
          rm -rf /tmp/cn2-copy && cp -R . /tmp/cn2-copy
          python3 - <<'EOF'
          p = "/tmp/cn2-copy/plugins/task-pipeline/skills/task-pipeline/references/documentation.md"
          t = open(p, encoding="utf-8").read()
          assert '**7. Silence is not a pass.**' in t, "PLANT DID NOT LAND: needle absent before edit"
          open(p, "w", encoding="utf-8").write(t.replace('**7. Silence is not a pass.**', '**7. Something vague.**'))
          EOF
          if python3 /tmp/cn2-copy/test/validate.py; then
            echo "ERROR: validator passed with a dropped canon must be caught broken"
            exit 1
          else
            echo "OK: validator correctly FAILs — a dropped canon must be caught"
          fi
      - name: Negative self-test (canons must keep naming their enforcement)
        run: |
          set -eu
          rm -rf /tmp/cn3-copy && cp -R . /tmp/cn3-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/cn3-copy/plugins/task-pipeline/skills/task-pipeline/references/documentation.md"
          t = open(p, encoding="utf-8").read()
          # The guard wants an arrow inside EACH canon's span. This probe matched one
          # exact citation and broke the day that line rewrapped — the needle was still
          # there, one newline further on. Operate on the span the guard reads instead.
          m = re.search(r"\*\*5\..*?(?=\*\*6\.)", t, re.S)
          assert m, "PLANT DID NOT LAND: canon 5 span not found"
          assert "\u2192" in m.group(0), "PLANT DID NOT LAND: canon 5 names no enforcement already"
          _planted = t[:m.start()] + m.group(0).replace("\u2192", "to") + t[m.end():]
          assert _planted != t, "PLANT DID NOT LAND"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/cn3-copy/test/validate.py; then
            echo "ERROR: validator passed with canons must keep naming their enforcement broken"
            exit 1
          else
            echo "OK: validator correctly FAILs — canons must keep naming their enforcement"
          fi
      - name: Negative self-test (the canon/rule boundary must stay stated)
        run: |
          set -eu
          rm -rf /tmp/cn4-copy && cp -R . /tmp/cn4-copy
          python3 - <<'EOF'
          p = "/tmp/cn4-copy/plugins/task-pipeline/skills/task-pipeline/references/documentation.md"
          t = open(p, encoding="utf-8").read()
          assert 'epistemic' in t, "PLANT DID NOT LAND: needle absent before edit"
          open(p, "w", encoding="utf-8").write(t.replace('epistemic', 'vibes-based'))
          EOF
          if python3 /tmp/cn4-copy/test/validate.py; then
            echo "ERROR: validator passed with the canon/rule boundary must stay stated broken"
            exit 1
          else
            echo "OK: validator correctly FAILs — the canon/rule boundary must stay stated"
          fi
      - name: Negative self-test (the CI verdict must keep its commands)
        run: |
          set -eu
          rm -rf /tmp/ci01-copy && cp -R . /tmp/ci01-copy
          python3 - <<'EOF'
          p = "/tmp/ci01-copy/plugins/task-pipeline/skills/task-pipeline/references/conventions.md"
          t = open(p, encoding="utf-8").read()
          assert "gh run list --branch" in t, "PLANT DID NOT LAND: needle absent before edit"
          open(p, "w", encoding="utf-8").write(t.replace("gh run list --branch", "gh run glance --branch"))
          EOF
          if python3 /tmp/ci01-copy/test/validate.py; then
            echo "ERROR: validator passed with the CI-verdict commands gone"
            exit 1
          else
            echo "OK: validator correctly FAILs — the CI verdict must keep its commands"
          fi
      - name: Negative self-test (a run that cannot be established needs its own state)
        run: |
          set -eu
          rm -rf /tmp/ci02-copy && cp -R . /tmp/ci02-copy
          python3 - <<'EOF'
          p = "/tmp/ci02-copy/plugins/task-pipeline/skills/task-pipeline/references/conventions.md"
          t = open(p, encoding="utf-8").read()
          assert "**no run found**" in t, "PLANT DID NOT LAND: needle absent before edit"
          open(p, "w", encoding="utf-8").write(t.replace("**no run found**", "**probably fine**"))
          EOF
          if python3 /tmp/ci02-copy/test/validate.py; then
            echo "ERROR: validator passed with the 'no run found' state removed"
            exit 1
          else
            echo "OK: validator correctly FAILs — a run that cannot be established needs its own state"
          fi
      - name: Negative self-test (a pushing stage must name the CI verdict)
        run: |
          set -eu
          rm -rf /tmp/ci03-copy && cp -R . /tmp/ci03-copy
          python3 - <<'EOF'
          p = "/tmp/ci03-copy/plugins/task-pipeline/skills/task-pipeline/references/stages.md"
          t = open(p, encoding="utf-8").read()
          i, j = t.index("## 7 — "), t.index("## 8 — ")
          sec = t[i:j]
          block = ("**Before\n  tagging, the CI verdict for what was just pushed is READ, not assumed**\n"
                   "  ([`conventions.md`](conventions.md) → *The CI verdict*) — a tag on a commit whose\n"
                   "  run nobody read is how a red `main` ships.")
          assert block in sec, "PLANT DID NOT LAND: the stage-7 CI block is absent before edit"
          open(p, "w", encoding="utf-8").write(t[:i] + sec.replace(block, "") + t[j:])
          EOF
          if python3 /tmp/ci03-copy/test/validate.py; then
            echo "ERROR: validator passed with a pushing stage no longer naming the CI verdict"
            exit 1
          else
            echo "OK: validator correctly FAILs — a pushing stage must name the CI verdict"
          fi
      - name: Negative self-test (a stage may not carry its own copy of the CI commands)
        run: |
          set -eu
          rm -rf /tmp/ci04-copy && cp -R . /tmp/ci04-copy
          python3 - <<'EOF'
          p = "/tmp/ci04-copy/plugins/task-pipeline/skills/task-pipeline/references/stages.md"
          t = open(p, encoding="utf-8").read()
          anchor = "- **Read the CI verdict for the deploy's own commit**"
          assert anchor in t, "PLANT DID NOT LAND: anchor absent before edit"
          assert "gh run list --branch" not in t, "PLANT DID NOT LAND: a copy already exists"
          open(p, "w", encoding="utf-8").write(
              t.replace(anchor, "- run `gh run list --branch main --limit 1`\n" + anchor, 1))
          EOF
          if python3 /tmp/ci04-copy/test/validate.py; then
            echo "ERROR: validator passed with a second copy of the CI-verdict commands"
            exit 1
          else
            echo "OK: validator correctly FAILs — a stage may not carry its own copy of the CI commands"
          fi
      - name: Negative self-test (the stage-8 gate must require the CI verdict)
        run: |
          set -eu
          rm -rf /tmp/ci05-copy && cp -R . /tmp/ci05-copy
          python3 - <<'EOF'
          import json, re
          p = "/tmp/ci05-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.example.json"
          d = json.load(open(p, encoding="utf-8"))
          s = [x for x in d["stages"] if x["state"] == "post-deploy"][0]
          c = s["gate"]["check"]
          assert "CI VERDICT" in c, "PLANT DID NOT LAND: the stage-8 gate does not require it before the edit"
          s["gate"]["check"] = re.sub(r"(?i)ci verdict", "vibes check", c)
          open(p, "w", encoding="utf-8").write(json.dumps(d, indent=2, ensure_ascii=False) + "\n")
          EOF
          if python3 /tmp/ci05-copy/test/validate.py; then
            echo "ERROR: validator passed with the stage-8 gate no longer requiring the CI verdict"
            exit 1
          else
            echo "OK: validator correctly FAILs — the stage-8 gate must require the CI verdict"
          fi
      - name: Negative self-test (the negatives floor may not lag the workflow)
        run: |
          set -eu
          rm -rf /tmp/ci06-copy && cp -R . /tmp/ci06-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/ci06-copy/test/negatives.py"
          t = open(p, encoding="utf-8").read()
          m = re.search(r"^MIN_EXPECTED\s*=\s*(\d+)", t, re.M)
          assert m, "PLANT DID NOT LAND: MIN_EXPECTED not found"
          lowered = int(m.group(1)) - 4
          open(p, "w", encoding="utf-8").write(
              t.replace(m.group(0), f"MIN_EXPECTED = {lowered}", 1))
          EOF
          if python3 /tmp/ci06-copy/test/validate.py; then
            echo "ERROR: validator passed with the negatives floor lagging the workflow"
            exit 1
          else
            echo "OK: validator correctly FAILs — the negatives floor may not lag the workflow"
          fi
      - name: Negative self-test (the navigator must exist)
        run: |
          set -eu
          rm -rf /tmp/ed1-copy && cp -R . /tmp/ed1-copy
          python3 - <<'EOF'
          import os
          os.remove("/tmp/ed1-copy/plugins/task-pipeline/skills/evidence-docs/SKILL.md")
          EOF
          if python3 /tmp/ed1-copy/test/validate.py; then
            echo "ERROR: validator passed with the navigator must exist broken"
            exit 1
          else
            echo "OK: validator correctly FAILs — the navigator must exist"
          fi
      - name: Negative self-test (the index must not drift from the canons)
        run: |
          set -eu
          rm -rf /tmp/ed2-copy && cp -R . /tmp/ed2-copy
          python3 - <<'EOF'
          p = "/tmp/ed2-copy/plugins/task-pipeline/skills/evidence-docs/SKILL.md"
          t = open(p, encoding="utf-8").read()
          assert '**Silence is not a pass**' in t, "PLANT DID NOT LAND: needle absent before edit"
          open(p, "w", encoding="utf-8").write(t.replace('**Silence is not a pass**', '**Vibes are fine**'))
          EOF
          if python3 /tmp/ed2-copy/test/validate.py; then
            echo "ERROR: validator passed with the index must not drift from the canons broken"
            exit 1
          else
            echo "OK: validator correctly FAILs — the index must not drift from the canons"
          fi
      - name: Negative self-test (the navigator must point at the one home)
        run: |
          set -eu
          rm -rf /tmp/ed3-copy && cp -R . /tmp/ed3-copy
          python3 - <<'EOF'
          p = "/tmp/ed3-copy/plugins/task-pipeline/skills/evidence-docs/SKILL.md"
          t = open(p, encoding="utf-8").read()
          assert 'documentation.md) → *The canons*' in t, "PLANT DID NOT LAND: needle absent before edit"
          open(p, "w", encoding="utf-8").write(t.replace('documentation.md) → *The canons*', 'documentation.md)'))
          EOF
          if python3 /tmp/ed3-copy/test/validate.py; then
            echo "ERROR: validator passed with the navigator must point at the one home broken"
            exit 1
          else
            echo "OK: validator correctly FAILs — the navigator must point at the one home"
          fi
      - name: Negative self-test (a navigator link must resolve from its own directory)
        run: |
          set -eu
          rm -rf /tmp/ed4-copy && cp -R . /tmp/ed4-copy
          python3 - <<'EOF'
          p = "/tmp/ed4-copy/plugins/task-pipeline/skills/evidence-docs/SKILL.md"
          t = open(p, encoding="utf-8").read()
          assert '../task-pipeline/references/gates.md' in t, "PLANT DID NOT LAND: needle absent before edit"
          open(p, "w", encoding="utf-8").write(t.replace('../task-pipeline/references/gates.md', '../task-pipeline/references/gaits.md'))
          EOF
          if python3 /tmp/ed4-copy/test/validate.py; then
            echo "ERROR: validator passed with a navigator link must resolve from its own directory broken"
            exit 1
          else
            echo "OK: validator correctly FAILs — a navigator link must resolve from its own directory"
          fi
      - name: Negative self-test (a colon-space in frontmatter must be rejected)
        run: |
          set -eu
          rm -rf /tmp/ed5-copy && cp -R . /tmp/ed5-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/ed5-copy/plugins/task-pipeline/skills/evidence-docs/SKILL.md"
          t = open(p, encoding="utf-8").read()
          # The hazard is a colon-space in an UNQUOTED scalar — that is what YAML
          # truncates. This probe used to plant one inside the description and stopped
          # reproducing anything the day that description was quoted: `: ` between
          # quotes is legal and loses nothing, so the guard fell silent for the right
          # reason and the probe called it a failure. Strip the quotes first, then
          # plant, so the plant recreates the hazard rather than a legal string.
          m = re.search(r'^description: "(.*)"\s*$', t, re.M)
          assert m, "PLANT DID NOT LAND: description is not a quoted scalar"
          _unquoted = "description: " + m.group(1)
          needle = "will be read as true — a decision record"
          assert needle in _unquoted, "PLANT DID NOT LAND: needle absent before edit"
          _planted = t[:m.start()] + _unquoted.replace(needle, "will be read as true: a decision record", 1) + t[m.end():]
          assert _planted != t, "PLANT DID NOT LAND"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/ed5-copy/test/validate.py; then
            echo "ERROR: validator passed frontmatter that YAML drops silently"
            exit 1
          else
            echo "OK: validator correctly FAILs — a colon-space in frontmatter"
          fi
      - name: Negative self-test (rule 16 in force with a consumer that dropped its citation)
        run: |
          set -eu
          rm -rf /tmp/r16-copy && cp -R . /tmp/r16-copy
          python3 - <<'EOF'
          p = "/tmp/r16-copy/plugins/task-pipeline/skills/task-pipeline/references/continuity.md"
          t = open(p, encoding="utf-8").read()
          needle = "Each iteration re-measures the work-list"
          assert needle in t, "PLANT DID NOT LAND: needle absent before edit"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "Iterations carry the work-list"))
          EOF
          if python3 /tmp/r16-copy/test/validate.py; then
            echo "ERROR: validator passed a run mode that never re-measures the work-list"
            exit 1
          else
            echo "OK: validator correctly FAILs — a rule-16 consumer dropped its citation"
          fi

      - name: Negative self-test (retro order — an adjacent enumeration puts prune before stamp)
        run: |
          set -eu
          rm -rf /tmp/ord1-copy && cp -R . /tmp/ord1-copy
          python3 - <<'EOF'
          p = "/tmp/ord1-copy/plugins/task-pipeline/skills/task-pipeline/references/companion-skills.md"
          t = open(p, encoding="utf-8").read()
          needle = "last act: stamp, prune, entry"
          assert needle in t, "PLANT DID NOT LAND: anchor absent before edit"
          t = t.replace(needle, "last act: prune, stamp, entry", 1)
          open(p, "w", encoding="utf-8").write(t)
          assert "last act: prune, stamp, entry" in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/ord1-copy/test/validate.py; then
            echo "ERROR: validator passed a surface enumerating prune before stamp"
            exit 1
          else
            echo "OK: validator correctly FAILs — rule 21's order contradicted by a consumer"
          fi

      - name: Negative self-test (retro order — an ordered list runs prune before stamp)
        run: |
          set -eu
          rm -rf /tmp/ord2-copy && cp -R . /tmp/ord2-copy
          python3 - <<'EOF'
          p = "/tmp/ord2-copy/plugins/task-pipeline/skills/task-pipeline/references/acceptance.md"
          t = open(p, encoding="utf-8").read()
          a, b = "1. **Stamp the run first**", "2. **Then prune.**"
          assert a in t and b in t, "PLANT DID NOT LAND: list anchors absent before edit"
          t = t.replace(a, "1. **Prune the list first**", 1).replace(b, "2. **Then stamp the run.**", 1)
          open(p, "w", encoding="utf-8").write(t)
          s = open(p, encoding="utf-8").read()
          assert "1. **Prune the list first**" in s and "2. **Then stamp the run.**" in s, "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/ord2-copy/test/validate.py; then
            echo "ERROR: validator passed an ordered list running prune before stamp"
            exit 1
          else
            echo "OK: validator correctly FAILs — the ordered-list shape is covered"
          fi

      - name: Negative self-test (retro order — a then-sequence without the word first)
        run: |
          set -eu
          rm -rf /tmp/ord3-copy && cp -R . /tmp/ord3-copy
          python3 - <<'EOF'
          p = "/tmp/ord3-copy/plugins/task-pipeline/commands/task-pipeline.md"
          t = open(p, encoding="utf-8").read()
          needle = "**stamp the run first** (its commit"
          assert needle in t, "PLANT DID NOT LAND: anchor absent before edit"
          t = t.replace(needle, "**prune the list** before you add — every trigger — then stamp the run (its commit", 1)
          open(p, "w", encoding="utf-8").write(t)
          assert "then stamp the run (its commit" in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/ord3-copy/test/validate.py; then
            echo "ERROR: validator passed a prune-then-stamp sequence written without the word 'first'"
            exit 1
          else
            echo "OK: validator correctly FAILs — the bare then-sequence shape is covered"
          fi

      - name: Negative self-test (retro order — a lone prune-first directive with no second act)
        run: |
          set -eu
          rm -rf /tmp/ord4-copy && cp -R . /tmp/ord4-copy
          python3 - <<'EOF'
          p = "/tmp/ord4-copy/plugins/task-pipeline/skills/evidence-docs/SKILL.md"
          t = open(p, encoding="utf-8").read()
          needle = "stamp first (the cold trigger reads it), then prune to a cap of ten"
          assert needle in t, "PLANT DID NOT LAND: anchor absent before edit"
          t = t.replace(needle, "prune first, cap of ten", 1)
          open(p, "w", encoding="utf-8").write(t)
          assert "prune first, cap of ten" in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/ord4-copy/test/validate.py; then
            echo "ERROR: validator passed a lone prune-first directive — the table-cell shape"
            exit 1
          else
            echo "OK: validator correctly FAILs — a one-act directive is covered"
          fi

      - name: Negative self-test (claim registry — a restated rule count that disagrees)
        run: |
          set -eu
          rm -rf /tmp/cr1-copy && cp -R . /tmp/cr1-copy
          python3 - <<'EOF'
          p = "/tmp/cr1-copy/README.md"
          t = open(p, encoding="utf-8").read()
          anchor = "## Contributing"
          assert anchor in t, "PLANT DID NOT LAND: anchor absent before edit"
          t = t.replace(anchor, "The doctrine ships 15 rules earned by failure.\n\n" + anchor, 1)
          open(p, "w", encoding="utf-8").write(t)
          assert "15 rules earned by failure" in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/cr1-copy/test/validate.py; then
            echo "ERROR: validator passed a restated rule count that disagrees with learned.md"
            exit 1
          else
            echo "OK: validator correctly FAILs — the claim registry caught a stale rule count"
          fi

      - name: Negative self-test (claim registry — a restated standing-instruction count)
        run: |
          set -eu
          rm -rf /tmp/cr2-copy && cp -R . /tmp/cr2-copy
          python3 - <<'EOF'
          p = "/tmp/cr2-copy/docs/DOCMAP.md"
          t = open(p, encoding="utf-8").read()
          anchor = "## Ratchets"
          assert anchor in t, "PLANT DID NOT LAND: anchor absent before edit"
          t = t.replace(anchor, anchor + "\n\nThe retro currently holds 2 of a hard cap of 10.\n", 1)
          open(p, "w", encoding="utf-8").write(t)
          assert "2 of a hard cap of 10" in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/cr2-copy/test/validate.py; then
            echo "ERROR: validator passed a standing-instruction count that disagrees with the retro"
            exit 1
          else
            echo "OK: validator correctly FAILs — the claim registry caught a stale ratchet"
          fi

      - name: Positive control (claim registry — a QUOTED stale number is a citation, not a claim)
        run: |
          set -eu
          rm -rf /tmp/cr3-copy && cp -R . /tmp/cr3-copy
          python3 - <<'EOF'
          p = "/tmp/cr3-copy/README.md"
          t = open(p, encoding="utf-8").read()
          anchor = "## Contributing"
          assert anchor in t, "PLANT DID NOT LAND: anchor absent before edit"
          t = t.replace(anchor, 'It once said "15 rules earned by failure" and was wrong.\n\n' + anchor, 1)
          open(p, "w", encoding="utf-8").write(t)
          assert '"15 rules earned by failure"' in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/cr3-copy/test/validate.py; then
            echo "OK: validator correctly PASSES — a quoted number is a citation of what a document said"
          else
            echo "ERROR: the registry rejected a quoted citation; documents cannot narrate their own drift"
            exit 1
          fi

      - name: Negative self-test (claim registry — a stale canon count in word form)
        run: |
          set -eu
          rm -rf /tmp/cr4-copy && cp -R . /tmp/cr4-copy
          python3 - <<'EOF'
          p = "/tmp/cr4-copy/plugins/task-pipeline/skills/evidence-docs/SKILL.md"
          t = open(p, encoding="utf-8").read()
          needle = "## The ten canons"
          assert needle in t, "PLANT DID NOT LAND: anchor absent before edit"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "## The nine canons", 1))
          assert "## The nine canons" in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/cr4-copy/test/validate.py; then
            echo "ERROR: validator passed a canon count stated as a WORD that disagrees with the doctrine"
            exit 1
          else
            echo "OK: validator correctly FAILs — word-form numbers are read, not skipped"
          fi

      - name: Negative self-test (claim registry — a stale reference-file count)
        run: |
          set -eu
          rm -rf /tmp/cr5-copy && cp -R . /tmp/cr5-copy
          python3 - <<'EOF'
          p = "/tmp/cr5-copy/SKILL-CARD.md"
          t = open(p, encoding="utf-8").read()
          anchor = "## What to check before you trust it"
          assert anchor in t, "PLANT DID NOT LAND: anchor absent before edit"
          t = t.replace(anchor, anchor + "\n\nThere are 26 files under `references/`.\n", 1)
          open(p, "w", encoding="utf-8").write(t)
          assert "26 files under" in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/cr5-copy/test/validate.py; then
            echo "ERROR: validator passed a reference-file count that disagrees with the directory"
            exit 1
          else
            echo "OK: validator correctly FAILs — the reference-file class is armed"
          fi

      - name: Negative self-test (claim registry — a stale guard count)
        run: |
          set -eu
          rm -rf /tmp/cr6-copy && cp -R . /tmp/cr6-copy
          python3 - <<'EOF'
          p = "/tmp/cr6-copy/README.md"
          t = open(p, encoding="utf-8").read()
          anchor = "## Contributing"
          assert anchor in t, "PLANT DID NOT LAND: anchor absent before edit"
          t = t.replace(anchor, "The suite carries 46 guards.\n\n" + anchor, 1)
          open(p, "w", encoding="utf-8").write(t)
          assert "46 guards" in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/cr6-copy/test/validate.py; then
            echo "ERROR: validator passed a guard count that disagrees with the workflow"
            exit 1
          else
            echo "OK: validator correctly FAILs — the founding class is still armed"
          fi

      - name: Negative self-test (claim registry — a stale dated-eval-run count)
        run: |
          set -eu
          rm -rf /tmp/cr7-copy && cp -R . /tmp/cr7-copy
          python3 - <<'EOF'
          p = "/tmp/cr7-copy/docs/DOCMAP.md"
          t = open(p, encoding="utf-8").read()
          anchor = "## Ratchets"
          assert anchor in t, "PLANT DID NOT LAND: anchor absent before edit"
          t = t.replace(anchor, anchor + "\n\nDated eval runs recorded: 0.\n", 1)
          open(p, "w", encoding="utf-8").write(t)
          assert "Dated eval runs recorded: 0" in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/cr7-copy/test/validate.py; then
            echo "ERROR: validator passed a dated-run count that disagrees with evals/run.py"
            exit 1
          else
            echo "OK: validator correctly FAILs — the eval-run class is armed"
          fi

      - name: Positive control (claim registry — a citation split by the repo's ~80-char wrap)
        run: |
          set -eu
          rm -rf /tmp/cr8-copy && cp -R . /tmp/cr8-copy
          python3 - <<'EOF'
          p = "/tmp/cr8-copy/README.md"
          t = open(p, encoding="utf-8").read()
          anchor = "## Contributing"
          assert anchor in t, "PLANT DID NOT LAND: anchor absent before edit"
          cite = 'It used to say "fifteen rules earned by failure on a real multi-repository\nbuild" and that was wrong.\n\n'
          open(p, "w", encoding="utf-8").write(t.replace(anchor, cite + anchor, 1))
          assert "multi-repository\nbuild\"" in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/cr8-copy/test/validate.py; then
            echo "OK: validator correctly PASSES — a quotation stays a quotation across a line break"
          else
            echo "ERROR: the quote exemption is per-line again; this corpus wraps at ~80 characters"
            exit 1
          fi

      - name: Negative self-test (a companion in the matrix and not in the preflight block)
        run: |
          set -eu
          rm -rf /tmp/comp-copy && cp -R . /tmp/comp-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/comp-copy/plugins/task-pipeline/skills/task-pipeline/references/companion-skills.md"
          t = open(p, encoding="utf-8").read()
          assert "✗ chrome-devtools" in t, "PLANT DID NOT LAND: preflight line absent before edit"
          t2 = re.sub(r"\n  ✗ chrome-devtools.*?(?=\n🧠)", "", t, flags=re.S)
          assert t2 != t, "PLANT DID NOT LAND: the preflight line was not removed"
          open(p, "w", encoding="utf-8").write(t2)
          s = open(p, encoding="utf-8").read()
          assert "✗ chrome-devtools" not in s, "PLANT DID NOT LAND"
          assert "**chrome-devtools**" in s, "PLANT WRONG: the matrix row must survive"
          EOF
          if python3 /tmp/comp-copy/test/validate.py; then
            echo "ERROR: validator passed a companion the operator is never offered"
            exit 1
          else
            echo "OK: validator correctly FAILs — the matrix and the preflight are compared"
          fi

      - name: Negative self-test (the cold trigger stated with one unit on one surface)
        run: |
          set -eu
          rm -rf /tmp/cold-copy && cp -R . /tmp/cold-copy
          python3 - <<'EOF'
          p = "/tmp/cold-copy/plugins/task-pipeline/skills/task-pipeline/references/acceptance.md"
          t = open(p, encoding="utf-8").read()
          needle = "fired in the last five run stamps **or in the last sixty days**"
          assert needle in t, "PLANT DID NOT LAND: anchor absent before edit"
          t = t.replace(needle, "fired in the last five run stamps", 1)
          open(p, "w", encoding="utf-8").write(t)
          s = open(p, encoding="utf-8").read()
          assert "five run stamps **or in the last sixty days**" not in s, "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/cold-copy/test/validate.py; then
            echo "ERROR: validator passed a cold trigger whose second unit is missing on one surface"
            exit 1
          else
            echo "OK: validator correctly FAILs — both units, on every surface that states it"
          fi

      - name: Negative self-test (cold trigger — the canonical row, where bold interrupts the phrase)
        run: |
          set -eu
          rm -rf /tmp/cold2-copy && cp -R . /tmp/cold2-copy
          python3 - <<'EOF'
          p = "/tmp/cold2-copy/plugins/task-pipeline/skills/task-pipeline/references/retrospective.md"
          t = open(p, encoding="utf-8").read()
          needle = "**five run stamps** — **or** in the last **sixty days**"
          assert needle in t, "PLANT DID NOT LAND: canonical row absent before edit"
          t = t.replace(needle, "**five run stamps**", 1)
          open(p, "w", encoding="utf-8").write(t)
          s = open(p, encoding="utf-8").read()
          assert "**five run stamps** — **or**" not in s, "PLANT DID NOT LAND"
          assert "**five run stamps**" in s, "PLANT WRONG: the bold phrase must survive"
          EOF
          if python3 /tmp/cold2-copy/test/validate.py; then
            echo "ERROR: the guard skipped the file that DEFINES the trigger — markdown emphasis defeated it"
            exit 1
          else
            echo "OK: validator correctly FAILs — emphasis is normalised before matching"
          fi

      - name: Negative self-test (a worked GATE verdict that omits only `unlooked`)
        run: |
          set -eu
          rm -rf /tmp/disc2-copy && cp -R . /tmp/disc2-copy
          python3 - <<'EOF'
          p = "/tmp/disc2-copy/plugins/task-pipeline/skills/task-pipeline/references/acceptance.md"
          t = open(p, encoding="utf-8").read()
          needle = "  abstained: 2 (1 partial · 1 cannot-verify) · unlooked: 3 dormant"
          assert needle in t, "PLANT DID NOT LAND: anchor absent before edit"
          t = t.replace(needle, "  abstained: 2 (1 partial · 1 cannot-verify)", 1)
          open(p, "w", encoding="utf-8").write(t)
          s = open(p, encoding="utf-8").read()
          assert "unlooked: 3 dormant" not in s and "abstained: 2" in s, "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/disc2-copy/test/validate.py; then
            echo "ERROR: validator passed a verdict that discloses what was declined but not what was never looked at"
            exit 1
          else
            echo "OK: validator correctly FAILs — each disclosure is required on its own"
          fi

      - name: Negative self-test (a worked GATE verdict that omits only `abstained`)
        run: |
          set -eu
          rm -rf /tmp/disc3-copy && cp -R . /tmp/disc3-copy
          python3 - <<'EOF'
          p = "/tmp/disc3-copy/plugins/task-pipeline/skills/task-pipeline/references/audit.md"
          t = open(p, encoding="utf-8").read()
          needle = "  abstained: 1 (1 cannot-verify) · unlooked: 2 dormant"
          assert needle in t, "PLANT DID NOT LAND: anchor absent before edit"
          t = t.replace(needle, "  unlooked: 2 dormant", 1)
          open(p, "w", encoding="utf-8").write(t)
          s = open(p, encoding="utf-8").read()
          assert "abstained: 1" not in s and "unlooked: 2 dormant" in s, "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/disc3-copy/test/validate.py; then
            echo "ERROR: validator passed a verdict that hides what the run declined to claim"
            exit 1
          else
            echo "OK: validator correctly FAILs — each disclosure is required on its own"
          fi

      - name: Negative self-test (a worked GATE verdict that prints no disclosures)
        run: |
          set -eu
          rm -rf /tmp/disc-copy && cp -R . /tmp/disc-copy
          python3 - <<'EOF'
          p = "/tmp/disc-copy/plugins/task-pipeline/skills/task-pipeline/references/gates.md"
          t = open(p, encoding="utf-8").read()
          needle = "  abstained: 0 · unlooked: 4 (3 dormant · 1 skip — no submodules in this repo)\n"
          assert needle in t, "PLANT DID NOT LAND: anchor absent before edit"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "", 1))
          assert "abstained: 0 · unlooked: 4" not in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/disc-copy/test/validate.py; then
            echo "ERROR: validator passed a GATE verdict that reads as 'verified' rather than as 'green, and here is what nobody claimed'"
            exit 1
          else
            echo "OK: validator correctly FAILs — both disclosures print beside every worked verdict"
          fi

      - name: Negative self-test (a summary that names most of the rotation axes)
        run: |
          set -eu
          rm -rf /tmp/axis1-copy && cp -R . /tmp/axis1-copy
          python3 - <<'EOF'
          p = "/tmp/axis1-copy/cursor/rules/task-pipeline.mdc"
          t = open(p, encoding="utf-8").read()
          needle = ", then **false success** (not \"is this\n  check correct\" but \"what does this mechanism print when it did not look?\"), then\n  **re-derivation**"
          assert needle in t, "PLANT DID NOT LAND: anchor absent before edit"
          t = t.replace(needle, ", then\n  **re-derivation**", 1)
          open(p, "w", encoding="utf-8").write(t)
          assert "false success" not in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/axis1-copy/test/validate.py; then
            echo "ERROR: validator passed an enumeration missing an axis — the drift this guard exists for"
            exit 1
          else
            echo "OK: validator correctly FAILs — a surface that enumerates the axes names all of them"
          fi

      - name: Negative self-test (an axis added to audit.md that no summary echoes)
        run: |
          set -eu
          rm -rf /tmp/axis2-copy && cp -R . /tmp/axis2-copy
          python3 - <<'EOF'
          p = "/tmp/axis2-copy/plugins/task-pipeline/skills/task-pipeline/references/audit.md"
          t = open(p, encoding="utf-8").read()
          needle = "\n**The crossover is measurable, so measure it.**"
          assert needle in t, "PLANT DID NOT LAND: anchor absent before edit"
          t = t.replace(needle, "\n7. **Adversarial reading** — an axis defined and never summarised.\n" + needle, 1)
          open(p, "w", encoding="utf-8").write(t)
          assert "Adversarial reading" in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/axis2-copy/test/validate.py; then
            echo "ERROR: validator passed a new axis that reached the definition and no consumer"
            exit 1
          else
            echo "OK: validator correctly FAILs — the plant lands in the file that DEFINES the axes"
          fi

      - name: Negative self-test (the axis list moved out from under its own guard)
        run: |
          set -eu
          rm -rf /tmp/axis3-copy && cp -R . /tmp/axis3-copy
          python3 - <<'EOF'
          p = "/tmp/axis3-copy/plugins/task-pipeline/skills/task-pipeline/references/audit.md"
          t = open(p, encoding="utf-8").read()
          needle = "### 2. Every pass changes the axis, not the effort"
          assert needle in t, "PLANT DID NOT LAND: anchor absent before edit"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "### 2. Rotating the search", 1))
          assert "Every pass changes the axis" not in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/axis3-copy/test/validate.py; then
            echo "ERROR: validator passed with its axis source of truth unfindable — a guard silently checking nothing"
            exit 1
          else
            echo "OK: validator correctly FAILs — a guard that lost its source says so instead of passing"
          fi

      - name: Negative self-test (a restated count of gates.md's own axes)
        run: |
          set -eu
          rm -rf /tmp/axis4-copy && cp -R . /tmp/axis4-copy
          python3 - <<'EOF'
          p = "/tmp/axis4-copy/plugins/task-pipeline/skills/task-pipeline/references/gates.md"
          t = open(p, encoding="utf-8").read()
          needle = "# Gates — the three axes,"
          assert needle in t, "PLANT DID NOT LAND: anchor absent before edit"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "# Gates — the two axes,", 1))
          assert "the two axes" in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/axis4-copy/test/validate.py; then
            echo "ERROR: validator passed a title claiming two axes over a file with three"
            exit 1
          else
            echo "OK: validator correctly FAILs — the count is computed from the Axis headings"
          fi

      - name: Negative self-test (the re-derivation axis stops printing its pair)
        run: |
          set -eu
          rm -rf /tmp/axis5-copy && cp -R . /tmp/axis5-copy
          python3 - <<'EOF'
          p = "/tmp/axis5-copy/plugins/task-pipeline/skills/task-pipeline/references/audit.md"
          t = open(p, encoding="utf-8").read()
          needle = "   verdict:    REFUTED — the sentence and the corpus disagree; the corpus wins\n"
          assert needle in t, "PLANT DID NOT LAND: anchor absent before edit"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "", 1))
          assert "verdict:    REFUTED" not in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/axis5-copy/test/validate.py; then
            echo "ERROR: validator passed an axis that demands a printed pair while showing none"
            exit 1
          else
            echo "OK: validator correctly FAILs — the axis shows the pair it demands"
          fi

      - name: Negative self-test (the re-derivation axis renamed out from under its guard)
        run: |
          set -eu
          rm -rf /tmp/axis6-copy && cp -R . /tmp/axis6-copy
          python3 - <<'EOF'
          p = "/tmp/axis6-copy/plugins/task-pipeline/skills/task-pipeline/references/audit.md"
          t = open(p, encoding="utf-8").read()
          needle = "6. **Re-derivation**"
          assert needle in t, "PLANT DID NOT LAND: anchor absent before edit"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "6. **Second route**", 1))
          assert "**Re-derivation**" not in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/axis6-copy/test/validate.py; then
            echo "ERROR: validator passed with the guard's subject renamed away — a guard checking nothing"
            exit 1
          else
            echo "OK: validator correctly FAILs — a guard whose subject vanished says so"
          fi

      - name: Negative self-test (the Cursor rule restating a stale count of gates.md's axes)
        run: |
          set -eu
          rm -rf /tmp/axis7-copy && cp -R . /tmp/axis7-copy
          python3 - <<'EOF'
          p = "/tmp/axis7-copy/cursor/rules/task-pipeline.mdc"
          t = open(p, encoding="utf-8").read()
          needle = "## Gates: three axes, and how to build one that cannot lie"
          assert needle in t, "PLANT DID NOT LAND: anchor absent before edit"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "## Gates: two axes, and how to build one that cannot lie", 1))
          assert "Gates: two axes" in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/axis7-copy/test/validate.py; then
            echo "ERROR: validator passed a shipped surface the claim registry could not see"
            exit 1
          else
            echo "OK: validator correctly FAILs — the Cursor rule and the command are in the registry corpus"
          fi

      - name: Negative self-test (a stated count of the rotation axes, the dormant class proven)
        run: |
          set -eu
          rm -rf /tmp/axis8-copy && cp -R . /tmp/axis8-copy
          python3 - <<'EOF'
          p = "/tmp/axis8-copy/README.md"
          t = open(p, encoding="utf-8").read()
          needle = "when the second overtakes the first, **rotate the axis**."
          assert needle in t, "PLANT DID NOT LAND: anchor absent before edit"
          open(p, "w", encoding="utf-8").write(t.replace(needle, needle + " There are five rotation axes.", 1))
          assert "five rotation axes" in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/axis8-copy/test/validate.py; then
            echo "ERROR: validator passed a restated axis count — a class that is dormant is not a class that works"
            exit 1
          else
            echo "OK: validator correctly FAILs — the dormant class has now been watched firing"
          fi

      - name: Negative self-test (a rule leaves learned.md with no logged line)
        run: |
          set -eu
          rm -rf /tmp/lrn1-copy && cp -R . /tmp/lrn1-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/lrn1-copy/plugins/task-pipeline/skills/task-pipeline/references/learned.md"
          t = open(p, encoding="utf-8").read()
          m = re.search(r"^\|\s*12\s*\|\s*\*\*.*$", t, re.M)
          assert m, "PLANT DID NOT LAND: rule 12's row absent before edit"
          open(p, "w", encoding="utf-8").write(t[:m.start()] + t[m.end() + 1:])
          assert re.search(r"^\|\s*12\s*\|", open(p, encoding="utf-8").read(), re.M) is None, "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/lrn1-copy/test/validate.py; then
            echo "ERROR: validator passed a rule that vanished, taking its incident with it"
            exit 1
          else
            echo "OK: validator correctly FAILs — a gap the Retired log does not name is a silent deletion"
          fi

      - name: Negative self-test (the Retired log itself removed from learned.md)
        run: |
          set -eu
          rm -rf /tmp/lrn2-copy && cp -R . /tmp/lrn2-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/lrn2-copy/plugins/task-pipeline/skills/task-pipeline/references/learned.md"
          t = open(p, encoding="utf-8").read()
          assert re.search(r"^### Retired\s*$", t, re.M), "PLANT DID NOT LAND: heading absent before edit"
          open(p, "w", encoding="utf-8").write(re.sub(r"^### Retired\s*$", "### Departures", t, count=1, flags=re.M))
          assert "### Retired" not in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/lrn2-copy/test/validate.py; then
            echo "ERROR: validator passed with no departure log — an absent log reads exactly like an empty one"
            exit 1
          else
            echo "OK: validator correctly FAILs — the log exists, so 'nothing retired' is a statement"
          fi

      - name: Negative self-test (a NEW surface states the cold trigger with one unit)
        run: |
          set -eu
          rm -rf /tmp/coldnew-copy && cp -R . /tmp/coldnew-copy
          python3 - <<'EOF'
          p = "/tmp/coldnew-copy/docs/DOCMAP.md"
          t = open(p, encoding="utf-8").read()
          t += "\n\nA rule retires when it has not fired in the last five run stamps.\n"
          open(p, "w", encoding="utf-8").write(t)
          assert "five run stamps" in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/coldnew-copy/test/validate.py; then
            echo "ERROR: validator passed a surface that was never on any hand-written list"
            exit 1
          else
            echo "OK: validator correctly FAILs — the corpus is discovered, so a new surface joins it by existing"
          fi

      - name: Negative self-test (the HIGHEST-numbered rule deleted — the gap that does not open)
        run: |
          set -eu
          rm -rf /tmp/hw1-copy && cp -R . /tmp/hw1-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/hw1-copy/plugins/task-pipeline/skills/task-pipeline/references/learned.md"
          t = open(p, encoding="utf-8").read()
          nums = [int(n) for n in re.findall(r"^\|\s*(\d+)\s*\|\s*\*\*", t, re.M)]
          assert nums, "PLANT DID NOT LAND: no rule rows found"
          m = re.search(r"^\|\s*%d\s*\|\s*\*\*.*$" % max(nums), t, re.M)
          assert m, "PLANT DID NOT LAND: highest row absent before edit"
          open(p, "w", encoding="utf-8").write(t[:m.start()] + t[m.end() + 1:])
          after = [int(n) for n in re.findall(r"^\|\s*(\d+)\s*\|\s*\*\*", open(p, encoding="utf-8").read(), re.M)]
          assert max(after) < max(nums), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/hw1-copy/test/validate.py; then
            echo "ERROR: deleting the highest rule shrinks max() with it and opens no gap — the false negative this guard shipped with"
            exit 1
          else
            echo "OK: validator correctly FAILs — the high-water mark comes from the file, not from max()"
          fi

      - name: Negative self-test (a digit in the log body masking a real deletion)
        run: |
          set -eu
          rm -rf /tmp/hw2-copy && cp -R . /tmp/hw2-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/hw2-copy/plugins/task-pipeline/skills/task-pipeline/references/learned.md"
          t = open(p, encoding="utf-8").read()
          m = re.search(r"^\|\s*9\s*\|\s*\*\*.*$", t, re.M)
          assert m, "PLANT DID NOT LAND: rule 9's row absent before edit"
          t = t[:m.start()] + t[m.end() + 1:]
          needle = "*None retired yet.*"
          assert needle in t, "PLANT DID NOT LAND: log anchor absent"
          # The leading number must NOT be a live table row, or the plant trips the
          # resurrection check instead and the test can no longer tell a working anchor
          # from a regressed one — verified by reverting the anchor and still failing.
          t = t.replace(needle, "- **99 · Something** — trigger: subsumed by 9; `deadbeef`\n\n" + needle, 1)
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/hw2-copy/test/validate.py; then
            echo "ERROR: a bare digit anywhere in the log masked a genuine silent deletion"
            exit 1
          else
            echo "OK: validator correctly FAILs — the log match is anchored to each line's leading number"
          fi

      - name: Negative self-test (the high-water mark removed from learned.md)
        run: |
          set -eu
          rm -rf /tmp/hw3-copy && cp -R . /tmp/hw3-copy
          python3 - <<'EOF'
          p = "/tmp/hw3-copy/plugins/task-pipeline/skills/task-pipeline/references/learned.md"
          t = open(p, encoding="utf-8").read()
          needle = "**Numbers issued so far: 22.**"
          assert needle in t, "PLANT DID NOT LAND: high-water mark absent before edit"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "**A note.**", 1))
          assert "Numbers issued so far" not in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/hw3-copy/test/validate.py; then
            echo "ERROR: validator passed with no high-water mark — the gap guard has nothing to measure against"
            exit 1
          else
            echo "OK: validator correctly FAILs — the mark is required, and it may not fall below the table"
          fi

      - name: Negative self-test (a worked verdict on a surface no list named)
        run: |
          set -eu
          rm -rf /tmp/discnew-copy && cp -R . /tmp/discnew-copy
          python3 - <<'EOF'
          p = "/tmp/discnew-copy/README.md"
          t = open(p, encoding="utf-8").read()
          needle = "  abstained: 1 (1 cannot-verify) · unlooked: 0\n"
          assert needle in t, "PLANT DID NOT LAND: anchor absent before edit"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "", 1))
          assert "abstained: 1 (1 cannot-verify)" not in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/discnew-copy/test/validate.py; then
            echo "ERROR: validator passed a worked verdict on a surface the hand-written list had missed"
            exit 1
          else
            echo "OK: validator correctly FAILs — the disclosure corpus is discovered, not listed"
          fi

      - name: Negative self-test (the high-water mark lowered in a COMMITTED change)
        run: |
          set -eu
          rm -rf /tmp/hw4-copy && cp -R . /tmp/hw4-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/hw4-copy/plugins/task-pipeline/skills/task-pipeline/references/learned.md"
          t = open(p, encoding="utf-8").read()
          nums = [int(n) for n in re.findall(r"^\|\s*(\d+)\s*\|\s*\*\*", t, re.M)]
          hi = max(nums)
          m = re.search(r"^\|\s*%d\s*\|\s*\*\*.*$" % hi, t, re.M)
          assert m, "PLANT DID NOT LAND: highest row absent before edit"
          t = t[:m.start()] + t[m.end() + 1:]
          old = "**Numbers issued so far: %d.**" % hi
          assert old in t, "PLANT DID NOT LAND: mark absent"
          open(p, "w", encoding="utf-8").write(t.replace(old, "**Numbers issued so far: %d.**" % (hi - 1), 1))
          EOF
          # COMMIT it. The first version of this test left the edit uncommitted, and the
          # check compared the working tree against HEAD — identical on a committed
          # checkout, which is what CI runs. The test passed while the check was blind.
          git -C /tmp/hw4-copy add -A
          git -C /tmp/hw4-copy -c user.email=t@t -c user.name=t commit -q -m "coordinated edit"
          if python3 /tmp/hw4-copy/test/validate.py; then
            echo "ERROR: a committed coordinated edit passed — the check compares against the checkout it is in"
            exit 1
          else
            echo "OK: validator correctly FAILs — the mark is compared against every value this file has held"
          fi

      - name: Negative self-test (every rule row deleted at once)
        run: |
          set -eu
          rm -rf /tmp/hw7-copy && cp -R . /tmp/hw7-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/hw7-copy/plugins/task-pipeline/skills/task-pipeline/references/learned.md"
          t = open(p, encoding="utf-8").read()
          out = re.sub(r"^\|\s*\d+\s*\|\s*\*\*.*$\n", "", t, flags=re.M)
          assert out != t, "PLANT DID NOT LAND: no rule rows removed"
          open(p, "w", encoding="utf-8").write(out)
          EOF
          if python3 /tmp/hw7-copy/test/validate.py; then
            echo "ERROR: emptying the table made the rule list falsy and skipped the whole check"
            exit 1
          else
            echo "OK: validator correctly FAILs — wholesale deletion is the loudest case, not an exemption"
          fi

      - name: Property check (the cross-commit skip is printed, never swallowed)
        run: |
          set -eu
          rm -rf /tmp/hw5-copy && cp -R . /tmp/hw5-copy && rm -rf /tmp/hw5-copy/.git
          out=$(python3 /tmp/hw5-copy/test/validate.py) || { echo "ERROR: validator failed for an unrelated reason"; exit 1; }
          if printf '%s' "$out" | grep -q "high-water mark vs its own history"; then
            echo "OK: outside a checkout the skip is printed as an unlooked disclosure, not swallowed"
          else
            echo "ERROR: the check could not run and said nothing — a silent skip reads as a pass"
            exit 1
          fi

      - name: Negative self-test (a second guard named inside one citation, and it does not exist)
        run: |
          set -eu
          rm -rf /tmp/cite2-copy && cp -R . /tmp/cite2-copy
          python3 - <<'EOF'
          p = "/tmp/cite2-copy/CONTRIBUTING.md"
          t = open(p, encoding="utf-8").read()
          needle = "`states the cold-retirement condition as`"
          assert needle in t, "PLANT DID NOT LAND: second citation absent before edit"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "`this guard does not exist anywhere`", 1))
          assert "this guard does not exist anywhere" in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/cite2-copy/test/validate.py; then
            echo "ERROR: only the first literal in a citation was checked — the rest rode free"
            exit 1
          else
            echo "OK: validator correctly FAILs — every literal inside a *(guard: ...)* parenthetical is checked"
          fi

      - name: Negative self-test (a rule numbered above the high-water mark)
        run: |
          set -eu
          rm -rf /tmp/hw6-copy && cp -R . /tmp/hw6-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/hw6-copy/plugins/task-pipeline/skills/task-pipeline/references/learned.md"
          t = open(p, encoding="utf-8").read()
          rows = list(re.finditer(r"^\|\s*(\d+)\s*\|\s*\*\*.*$", t, re.M))
          assert rows, "PLANT DID NOT LAND: no rule rows"
          last = rows[-1]
          new_no = int(last.group(1)) + 1
          row = re.sub(r"^\|\s*\d+\s*\|", "| %d |" % new_no, last.group(0))
          open(p, "w", encoding="utf-8").write(t[:last.end()] + "\n" + row + t[last.end():])
          EOF
          if python3 /tmp/hw6-copy/test/validate.py; then
            echo "ERROR: a rule was added without advancing the mark, so its later deletion would be invisible"
            exit 1
          else
            echo "OK: validator correctly FAILs — the mark may not sit below the table"
          fi

      - name: Negative self-test (a rule listed as retired that is still in the table)
        run: |
          set -eu
          rm -rf /tmp/hw8-copy && cp -R . /tmp/hw8-copy
          python3 - <<'EOF'
          p = "/tmp/hw8-copy/plugins/task-pipeline/skills/task-pipeline/references/learned.md"
          t = open(p, encoding="utf-8").read()
          needle = "*None retired yet.*"
          assert needle in t, "PLANT DID NOT LAND: log anchor absent before edit"
          t = t.replace(needle, "- **12 · Tests create what they assert on** — trigger: subsumed; `deadbeef`\n\n" + needle, 1)
          open(p, "w", encoding="utf-8").write(t)
          assert "**12 · Tests create" in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/hw8-copy/test/validate.py; then
            echo "ERROR: a rule was logged as retired while its row still stands — the reverse direction nobody checked"
            exit 1
          else
            echo "OK: validator correctly FAILs — a gap has one side and so does a resurrection"
          fi

      - name: Negative self-test (an open ledger row with no board id)
        run: |
          set -eu
          rm -rf /tmp/bd1-copy && cp -R . /tmp/bd1-copy
          python3 - <<'EOF'
          p = "/tmp/bd1-copy/docs/superpowers/specs/2026-08-08-audit-followup-carryover.md"
          t = open(p, encoding="utf-8").read()
          needle = " → `B-001`"
          assert needle in t, "PLANT DID NOT LAND: the board id is absent before the edit"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "", 1))
          assert "B-001" not in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/bd1-copy/test/validate.py; then
            echo "ERROR: an open row with no board id passed — the dangling pointer the board exists to resolve"
            exit 1
          else
            echo "OK: validator correctly FAILs — every open ledger row names a board id"
          fi

      - name: Negative self-test (a ledger pointing at a board id nobody issued)
        run: |
          set -eu
          rm -rf /tmp/bd2-copy && cp -R . /tmp/bd2-copy
          python3 - <<'EOF'
          p = "/tmp/bd2-copy/docs/superpowers/specs/2026-08-08-audit-followup-carryover.md"
          t = open(p, encoding="utf-8").read()
          assert "B-001" in t, "PLANT DID NOT LAND: anchor absent before the edit"
          open(p, "w", encoding="utf-8").write(t.replace("B-001", "B-999", 1))
          assert "B-999" in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/bd2-copy/test/validate.py; then
            echo "ERROR: a pointer to an id nobody issued reads as filed and is not"
            exit 1
          else
            echo "OK: validator correctly FAILs — the id has to exist on the board"
          fi

      - name: Negative self-test (a board row with no Source — the reverse direction)
        run: |
          set -eu
          rm -rf /tmp/bd3-copy && cp -R . /tmp/bd3-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/bd3-copy/docs/superpowers/backlog.md"
          t = open(p, encoding="utf-8").read()
          # DERIVED, not a literal id: this probe named B-009 and broke the day that row
          # closed. A needle that has to be maintained goes stale between the change and
          # the run — B-057's own class, caught loudly by negatives.py rather than silently.
          m = next((x for x in re.finditer(r"^\| (B-\d+\w*) \|([^|]*)\|([^|]*)\|", t, re.M)
                    if x.group(3).strip()), None)
          assert m, "PLANT DID NOT LAND: no open board row carries a Source"
          open(p, "w", encoding="utf-8").write(t[:m.start(3)] + " " + t[m.end(3):])
          EOF
          if python3 /tmp/bd3-copy/test/validate.py; then
            echo "ERROR: a board row traceable to nothing passed — a gap has one side and so does an invention"
            exit 1
          else
            echo "OK: validator correctly FAILs — both directions are checked"
          fi

      - name: Negative self-test (the board emptied of every row)
        run: |
          set -eu
          rm -rf /tmp/bd4-copy && cp -R . /tmp/bd4-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/bd4-copy/docs/superpowers/backlog.md"
          t = open(p, encoding="utf-8").read()
          assert re.search(r"B-\d+", t), "PLANT DID NOT LAND: no ids before the edit"
          open(p, "w", encoding="utf-8").write(re.sub(r"B-(\d+)", r"X-\1", t))
          assert not re.search(r"B-\d+", open(p, encoding="utf-8").read()), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/bd4-copy/test/validate.py; then
            echo "ERROR: an empty board passed — it reads exactly like a board with nothing queued"
            exit 1
          else
            echo "OK: validator correctly FAILs — the plant lands in the file that DEFINES the board"
          fi

      - name: Negative self-test (a board priority that does not follow from its inputs)
        run: |
          set -eu
          rm -rf /tmp/bd5-copy && cp -R . /tmp/bd5-copy
          python3 - <<'EOF'
          p = "/tmp/bd5-copy/docs/superpowers/backlog.md"
          t = open(p, encoding="utf-8").read()
          needle = "| 1 | **6** | open"
          assert needle in t, "PLANT DID NOT LAND: anchor absent before the edit"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "| 1 | **9** | open", 1))
          EOF
          if python3 /tmp/bd5-copy/test/validate.py; then
            echo "ERROR: a priority contradicting its own sev/blast/age passed — the hand-assigned number this column replaced"
            exit 1
          else
            echo "OK: validator correctly FAILs — the one number a reader is invited to check is checked"
          fi

      - name: Negative self-test (the seeded template's own worked example broken)
        run: |
          set -eu
          rm -rf /tmp/bd6-copy && cp -R . /tmp/bd6-copy
          python3 - <<'EOF'
          p = "/tmp/bd6-copy/plugins/task-pipeline/skills/task-pipeline/templates/backlog.md"
          t = open(p, encoding="utf-8").read()
          needle = "| 12 | **2** | open"
          assert needle in t, "PLANT DID NOT LAND: anchor absent before the edit"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "| 12 | **7** | open", 1))
          EOF
          if python3 /tmp/bd6-copy/test/validate.py; then
            echo "ERROR: the file seeded verbatim into every host project shipped an example that fails its own formula"
            exit 1
          else
            echo "OK: validator correctly FAILs — the plant lands in the template, not only in this repo's board"
          fi

      - name: Negative self-test (an open row whose ledger has two status columns)
        run: |
          set -eu
          rm -rf /tmp/bd7-copy && cp -R . /tmp/bd7-copy
          python3 - <<'EOF'
          p = "/tmp/bd7-copy/docs/superpowers/specs/2026-08-03-setup-and-autonomy-carryover.md"
          t = open(p, encoding="utf-8").read()
          needle = " → `B-015`"
          assert needle in t, "PLANT DID NOT LAND: the board id is absent before the edit"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "", 1))
          assert "B-015" not in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/bd7-copy/test/validate.py; then
            echo "ERROR: a ledger with both Status and Home hid an open row — five of ten ledgers here have that shape"
            exit 1
          else
            echo "OK: validator correctly FAILs — the test is position-free, so a shape nobody anticipated cannot defeat it"
          fi

      - name: Negative self-test (a blank line splitting a markdown table)
        run: |
          set -eu
          rm -rf /tmp/tbl-copy && cp -R . /tmp/tbl-copy
          python3 - <<'EOF'
          p = "/tmp/tbl-copy/docs/superpowers/backlog.md"
          t = open(p, encoding="utf-8").read()
          needle = "| B-015 |"
          assert needle in t, "PLANT DID NOT LAND: anchor absent before the edit"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "\n" + needle, 1))
          EOF
          if python3 /tmp/tbl-copy/test/validate.py; then
            echo "ERROR: a split table passed — every row below the gap renders as prose, silently"
            exit 1
          else
            echo "OK: validator correctly FAILs — the class that hit this repo's own board is now a check"
          fi

      - name: Negative self-test (a row homed `backlog` — the trigger the templates promise)
        run: |
          set -eu
          rm -rf /tmp/bd8-copy && cp -R . /tmp/bd8-copy
          python3 - <<'EOF'
          p = "/tmp/bd8-copy/docs/superpowers/specs/2026-08-09-scratch-carryover.md"
          open(p, "w", encoding="utf-8").write(
              "# Carry-over — scratch\n\n"
              "| # | Stage | What | Why it isn't done | REQ | Where it lives now |\n"
              "|---|---|---|---|---|---|\n"
              "| 1 | 5 Dev | a thing | later | — | backlog |\n")
          assert "backlog |" in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/bd8-copy/test/validate.py; then
            echo "ERROR: the doctrine promised the gate refuses this shape and the gate did not"
            exit 1
          else
            echo "OK: validator correctly FAILs — both triggers are enforced, not only the one the rows happened to use"
          fi

      - name: Property check (two adjacent tables are not a split table)
        run: |
          set -eu
          rm -rf /tmp/adj-copy && cp -R . /tmp/adj-copy
          python3 - <<'EOF'
          p = "/tmp/adj-copy/README.md"
          t = open(p, encoding="utf-8").read()
          open(p, "w", encoding="utf-8").write(
              t + "\n| a | b |\n|---|---|\n| 1 | 2 |\n\n| c | d |\n|---|---|\n| 3 | 4 |\n")
          EOF
          if python3 /tmp/adj-copy/test/validate.py >/dev/null; then
            echo "OK: two independent tables one blank line apart are left alone — the shape the guard's comment claims"
          else
            echo "ERROR: the split-table guard fires on a valid pattern; a checker with false positives is worse than none"
            exit 1
          fi

      - name: Negative self-test (a row homed `unresolved` — the third promised trigger)
        run: |
          set -eu
          rm -rf /tmp/bd9-copy && cp -R . /tmp/bd9-copy
          python3 - <<'EOF'
          p = "/tmp/bd9-copy/docs/superpowers/specs/2026-08-09-scratch2-carryover.md"
          open(p, "w", encoding="utf-8").write(
              "# Carry-over — scratch\n\n"
              "| # | Stage | What | Why it isn't done | REQ | Where it lives now |\n"
              "|---|---|---|---|---|---|\n"
              "| 1 | 5 Dev | a thing | later | — | unresolved |\n")
          assert "unresolved |" in open(p, encoding="utf-8").read(), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/bd9-copy/test/validate.py; then
            echo "ERROR: templates/carryover.md says unresolved blocks the stage-10 gate and it did not"
            exit 1
          else
            echo "OK: validator correctly FAILs — all three not-done values are checked, not only the one the rows used"
          fi

      - name: Negative self-test (a board row too short to compute a priority)
        run: |
          set -eu
          rm -rf /tmp/bd10-copy && cp -R . /tmp/bd10-copy
          python3 - <<'EOF'
          p = "/tmp/bd10-copy/docs/superpowers/backlog.md"
          t = open(p, encoding="utf-8").read()
          row = [l for l in t.splitlines() if l.startswith("| B-008 |")]
          assert row, "PLANT DID NOT LAND: B-008 absent before the edit"
          open(p, "w", encoding="utf-8").write(t.replace(row[0], "| B-008 | short | src | S | 999 |", 1))
          EOF
          if python3 /tmp/bd10-copy/test/validate.py; then
            echo "ERROR: a malformed row was skipped in silence, carrying any priority it liked"
            exit 1
          else
            echo "OK: validator correctly FAILs — a row it cannot compute is reported, not skipped"
          fi

      - name: Negative self-test (a blanked Source in the seeded template)
        run: |
          set -eu
          rm -rf /tmp/bd11-copy && cp -R . /tmp/bd11-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/bd11-copy/plugins/task-pipeline/skills/task-pipeline/templates/backlog.md"
          t = open(p, encoding="utf-8").read()
          m = re.search(r"^\| B-001 \|([^|]*)\|([^|]*)\|", t, re.M)
          assert m and m.group(2).strip(), "PLANT DID NOT LAND: Source already empty"
          open(p, "w", encoding="utf-8").write(t[:m.start(2)] + " " + t[m.end(2):])
          EOF
          if python3 /tmp/bd11-copy/test/validate.py; then
            echo "ERROR: the reverse check covered this repo's board and not the file seeded into every host project"
            exit 1
          else
            echo "OK: validator correctly FAILs — both boards, like the arithmetic check beside it"
          fi

      - name: Property check (a description beginning "Open" is not an open row)
        run: |
          set -eu
          rm -rf /tmp/fp-copy && cp -R . /tmp/fp-copy
          python3 - <<'EOF'
          p = "/tmp/fp-copy/docs/superpowers/specs/2026-08-09-scratch3-carryover.md"
          open(p, "w", encoding="utf-8").write(
              "# Carry-over — scratch\n\n"
              "| # | Stage | What | Why it isn't done | REQ | Where it lives now |\n"
              "|---|---|---|---|---|---|\n"
              "| 1 | 5 Dev | Open source license text is missing from the bundle | done | — | closed |\n")
          EOF
          if python3 /tmp/fp-copy/test/validate.py >/dev/null; then
            echo "OK: a cell must BE a status, not merely begin with one — rule 10, a checker with false positives is worse than none"
          else
            echo "ERROR: a closed row was reported as open because its description started with the word Open"
            exit 1
          fi

      - name: Negative self-test (a status column worded in prose — "open as a printed exclusion")
        run: |
          set -eu
          rm -rf /tmp/bd12-copy && cp -R . /tmp/bd12-copy
          python3 - <<'EOF'
          p = "/tmp/bd12-copy/docs/superpowers/specs/2026-08-09-prose-carryover.md"
          open(p, "w", encoding="utf-8").write(
              "# Carry-over — scratch\n\n"
              "| # | Stage | What | Why it isn't done | REQ | Where it lives now |\n"
              "|---|---|---|---|---|---|\n"
              "| 1 | 5 Dev | a thing | later | — | open as a printed exclusion |\n")
          EOF
          if python3 /tmp/bd12-copy/test/validate.py; then
            echo "ERROR: a status column saying 'open …' in prose was not read as open — this repo has live rows worded that way"
            exit 1
          else
            echo "OK: validator correctly FAILs — inside a status column, a word boundary is enough"
          fi

      - name: Property check (a hyphenated "Open-source" in a description is not a status)
        run: |
          set -eu
          rm -rf /tmp/hyp-copy && cp -R . /tmp/hyp-copy
          python3 - <<'EOF'
          p = "/tmp/hyp-copy/docs/superpowers/specs/2026-08-09-hyphen-carryover.md"
          open(p, "w", encoding="utf-8").write(
              "# Carry-over — scratch\n\n"
              "| # | Stage | What | Why it isn't done | REQ | Where it lives now |\n"
              "|---|---|---|---|---|---|\n"
              "| 1 | 5 Dev | Open-source license text is missing | done | — | closed |\n")
          EOF
          if python3 /tmp/hyp-copy/test/validate.py >/dev/null; then
            echo "OK: the What column is never read as a status, so punctuation in prose cannot fake one"
          else
            echo "ERROR: a closed row was flagged because its description began 'Open-source'"
            exit 1
          fi

      - name: Negative self-test (a mismatched-shape row losing its board id)
        run: |
          set -eu
          rm -rf /tmp/bd13-copy && cp -R . /tmp/bd13-copy
          python3 - <<'EOF'
          p = "/tmp/bd13-copy/docs/superpowers/specs/2026-08-08-audit-followup-carryover.md"
          t = open(p, encoding="utf-8").read()
          assert "B-019" in t, "PLANT DID NOT LAND: anchor absent before the edit"
          open(p, "w", encoding="utf-8").write(t.replace("B-019", "B-999", 1))
          EOF
          if python3 /tmp/bd13-copy/test/validate.py; then
            echo "ERROR: a row whose cell count differs from its header was skipped before the id check ran"
            exit 1
          else
            echo "OK: validator correctly FAILs — the fallback matches on a word boundary, not exact equality"
          fi

      - name: Negative self-test (a trigger the code accepts and the doctrine never names)
        run: |
          set -eu
          rm -rf /tmp/tg1-copy && cp -R . /tmp/tg1-copy
          python3 - <<'EOF'
          p = "/tmp/tg1-copy/test/validate.py"
          t = open(p, encoding="utf-8").read()
          needle = 'r"^(?:open|unresolved|backlog)\\b"'
          assert needle in t, "PLANT DID NOT LAND: the trigger regex is not where the guard reads it"
          open(p, "w", encoding="utf-8").write(t.replace(needle, 'r"^(?:open|unresolved|backlog|parked)\\b"', 1))
          EOF
          if python3 /tmp/tg1-copy/test/validate.py; then
            echo "ERROR: the code grew a trigger the doctrine never names — that shipped three times in one module"
            exit 1
          else
            echo "OK: validator correctly FAILs — the enumeration is computed from the regex, both directions"
          fi

      - name: Negative self-test (the trigger regex renamed out from under its own comparison)
        run: |
          set -eu
          rm -rf /tmp/tg3-copy && cp -R . /tmp/tg3-copy
          python3 - <<'EOF'
          p = "/tmp/tg3-copy/test/validate.py"
          t = open(p, encoding="utf-8").read()
          assert "_UNRES_RE = re.compile" in t, "PLANT DID NOT LAND: anchor absent"
          open(p, "w", encoding="utf-8").write(
              t.replace("_UNRES_RE = re.compile", "_UNRESOLVED_RE = re.compile")
               .replace("_UNRES_RE.match", "_UNRESOLVED_RE.match"))
          EOF
          if python3 /tmp/tg3-copy/test/validate.py; then
            echo "ERROR: the comparison lost its source and passed everything in silence"
            exit 1
          else
            echo "OK: validator correctly FAILs — a guard with no source says so"
          fi

      - name: Negative self-test (the seeded ledger template showing `backlog` as settled)
        run: |
          set -eu
          rm -rf /tmp/cot-copy && cp -R . /tmp/cot-copy
          python3 - <<'EOF'
          p = "/tmp/cot-copy/plugins/task-pipeline/skills/task-pipeline/templates/carryover.md"
          t = open(p, encoding="utf-8").read()
          needle = "| 2 | 5 Review | `export.ts` lacks a size guard | minor, non-blocking | — | `B-014` |"
          assert needle in t, "PLANT DID NOT LAND: anchor absent before the edit"
          open(p, "w", encoding="utf-8").write(t.replace(
              needle,
              "| 2 | 5 Review | `export.ts` lacks a size guard | minor, non-blocking | — | backlog |", 1))
          EOF
          if python3 /tmp/cot-copy/test/validate.py; then
            echo "ERROR: the file seeded into every host project taught that a bare `backlog` home is a settled outcome"
            exit 1
          else
            echo "OK: validator correctly FAILs — the worked example carries a real board id"
          fi

      - name: Negative self-test (a trigger named on the page but not in the enumerating paragraph)
        run: |
          set -eu
          rm -rf /tmp/tg4-copy && cp -R . /tmp/tg4-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/tg4-copy/plugins/task-pipeline/skills/task-pipeline/references/backlog.md"
          t = open(p, encoding="utf-8").read()
          parts = re.split(r"(\n\s*\n)", t)
          hit = [i for i, x in enumerate(parts) if "three triggers" in x]
          assert hit, "PLANT DID NOT LAND: the enumerating paragraph is not where the plant looks"
          parts[hit[0]] = parts[hit[0]].replace("unresolved", "not-done")
          t = "".join(parts).rstrip() + "\n\nA later note mentioning unresolved in passing.\n"
          open(p, "w", encoding="utf-8").write(t)
          seg = [x for x in re.split(r"\n\s*\n", t) if "three triggers" in x][0]
          assert "unresolved" not in seg, "PLANT DID NOT LAND: the literal is still in the paragraph"
          EOF
          if python3 /tmp/tg4-copy/test/validate.py; then
            echo "ERROR: a trigger word floating elsewhere on the page satisfied the enumeration — the false-success class this guard closes"
            exit 1
          else
            echo "OK: validator correctly FAILs — the check is scoped to the paragraph that enumerates"
          fi

      - name: Negative self-test (a table split by TWO blank lines)
        run: |
          set -eu
          rm -rf /tmp/tbl2-copy && cp -R . /tmp/tbl2-copy
          python3 - <<'EOF'
          p = "/tmp/tbl2-copy/docs/superpowers/backlog.md"
          t = open(p, encoding="utf-8").read()
          needle = "| B-015 |"
          assert needle in t, "PLANT DID NOT LAND: anchor absent before the edit"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "\n\n" + needle, 1))
          EOF
          if python3 /tmp/tbl2-copy/test/validate.py; then
            echo "ERROR: two blank lines end a table exactly like one, and the guard saw neither"
            exit 1
          else
            echo "OK: validator correctly FAILs — the gap is any length, because GFM ends the table at the first blank"
          fi

      - name: Negative self-test (a verification row whose REQ is in no brief)
        run: |
          set -eu
          rm -rf /tmp/vf1-copy && cp -R . /tmp/vf1-copy
          python3 - <<'EOF'
          p = "/tmp/vf1-copy/docs/superpowers/verification.md"
          _orig_bytes = open(p, "rb").read()
          t = open(p, encoding="utf-8").read()
          open(p, "w", encoding="utf-8").write(
              t + "| REQ-999 | invented | `nowhere` | v1.0.0 | pass | never | — |\n")
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: a-verification-row-whose-req-is-in-n"
          EOF
          if python3 /tmp/vf1-copy/test/validate.py; then
            echo "ERROR: a ledger row about a requirement nobody wrote down passed"
            exit 1
          else
            echo "OK: validator correctly FAILs — the reverse direction, a row about nothing"
          fi

      - name: Negative self-test (prose in the Human column)
        run: |
          set -eu
          rm -rf /tmp/vf2-copy && cp -R . /tmp/vf2-copy
          python3 - <<'EOF'
          p = "/tmp/vf2-copy/docs/superpowers/verification.md"
          t = open(p, encoding="utf-8").read()
          needle = "| pass | never | — |"
          assert needle in t, "PLANT DID NOT LAND: anchor absent before the edit"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "| pass | soon | — |", 1))
          EOF
          if python3 /tmp/vf2-copy/test/validate.py; then
            echo "ERROR: 'soon' in the one column a machine may not fill — the question stops being answerable"
            exit 1
          else
            echo "OK: validator correctly FAILs — a date or the literal never, nothing else"
          fi

      - name: Negative self-test (the verification ledger emptied of rows)
        run: |
          set -eu
          rm -rf /tmp/vf3-copy && cp -R . /tmp/vf3-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/vf3-copy/docs/superpowers/verification.md"
          t = open(p, encoding="utf-8").read()
          assert re.search(r"^\| REQ-\d+ ", t, re.M), "PLANT DID NOT LAND: no rows before the edit"
          open(p, "w", encoding="utf-8").write(re.sub(r"^\| REQ-\d+ .*$", "", t, flags=re.M))
          EOF
          if python3 /tmp/vf3-copy/test/validate.py; then
            echo "ERROR: an empty ledger reads exactly like a project that has shipped nothing"
            exit 1
          else
            echo "OK: validator correctly FAILs — the plant lands in the file that DEFINES the ledger"
          fi

      - name: Property check (a real date in the Human column is accepted)
        run: |
          set -eu
          rm -rf /tmp/vf4-copy && cp -R . /tmp/vf4-copy
          python3 - <<'EOF'
          p = "/tmp/vf4-copy/docs/superpowers/verification.md"
          t = open(p, encoding="utf-8").read()
          needle = "| pass | never | — |"
          assert needle in t, "PLANT DID NOT LAND: anchor absent"
          open(p, "w", encoding="utf-8").write(
              t.replace(needle, "| pass | 2026-08-09 | opened the rendered page |", 1))
          EOF
          if python3 /tmp/vf4-copy/test/validate.py >/dev/null; then
            echo "OK: filling the column is the point of the file, so filling it must not fail the build"
          else
            echo "ERROR: recording that a human looked broke the gate — the column would never be filled again"
            exit 1
          fi

      - name: Negative self-test (prose in Human, rescued by a date in Note)
        run: |
          set -eu
          rm -rf /tmp/vf5-copy && cp -R . /tmp/vf5-copy
          python3 - <<'EOF'
          p = "/tmp/vf5-copy/docs/superpowers/verification.md"
          t = open(p, encoding="utf-8").read()
          needle = "| pass | never | — |"
          assert needle in t, "PLANT DID NOT LAND: anchor absent before the edit"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "| pass | soon | 2026-08-09 |", 1))
          EOF
          if python3 /tmp/vf5-copy/test/validate.py; then
            echo "ERROR: a bare date in Note satisfied a row whose Human said 'soon' — the one thing this guard rejects"
            exit 1
          else
            echo "OK: validator correctly FAILs — the Human column is located by name, not by scanning every cell"
          fi

      - name: Negative self-test (the Human column renamed away)
        run: |
          set -eu
          rm -rf /tmp/vf6-copy && cp -R . /tmp/vf6-copy
          python3 - <<'EOF'
          p = "/tmp/vf6-copy/docs/superpowers/verification.md"
          t = open(p, encoding="utf-8").read()
          needle = "| Auto | Human | Note |"
          assert needle in t, "PLANT DID NOT LAND: the header is not where the guard reads it"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "| Auto | Person | Note |", 1))
          EOF
          if python3 /tmp/vf6-copy/test/validate.py; then
            echo "ERROR: the guard could not find its subject and passed everything"
            exit 1
          else
            echo "OK: validator correctly FAILs — a guard that cannot locate its column says so"
          fi

      - name: Negative self-test (the layout tree drifting from the tables above it)
        run: |
          set -eu
          rm -rf /tmp/tr1-copy && cp -R . /tmp/tr1-copy
          python3 - <<'EOF'
          p = "/tmp/tr1-copy/plugins/task-pipeline/skills/task-pipeline/references/artifacts.md"
          t = open(p, encoding="utf-8").read()
          needle = "    verification.md                   # one row per shipped REQ; `Human` is a date or `never`\n"
          assert needle in t, "PLANT DID NOT LAND: the tree line is not where the guard reads it"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "", 1))
          EOF
          if python3 /tmp/tr1-copy/test/validate.py; then
            echo "ERROR: one file states the same truth twice and the two drifted for two modules unnoticed"
            exit 1
          else
            echo "OK: validator correctly FAILs — the tree is computed against the tables beside it"
          fi

      - name: Property check (the never-count is printed beside the verdict)
        run: |
          set -eu
          rm -rf /tmp/vf7-copy && cp -R . /tmp/vf7-copy
          out=$(python3 /tmp/vf7-copy/test/validate.py) || { echo "ERROR: validator failed for an unrelated reason"; exit 1; }
          if printf '%s' "$out" | grep -q "never confirmed by a person"; then
            echo "OK: the count is surfaced — a measurement nobody prints is the same silence as no measurement"
          else
            echo "ERROR: the never-count is computed and dropped on the floor"
            exit 1
          fi

      - name: Negative self-test (the layout tree block gone entirely)
        run: |
          set -eu
          rm -rf /tmp/tr2-copy && cp -R . /tmp/tr2-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/tr2-copy/plugins/task-pipeline/skills/task-pipeline/references/artifacts.md"
          t = open(p, encoding="utf-8").read()
          m = re.search(r"^  superpowers/\n(?:    .*\n)+", t, re.M)
          assert m, "PLANT DID NOT LAND: the tree block is not where the guard reads it"
          open(p, "w", encoding="utf-8").write(t[:m.start()] + t[m.end():])
          assert not re.search(r"^  superpowers/\n    ", open(p, encoding="utf-8").read(), re.M), "PLANT DID NOT LAND"
          EOF
          if python3 /tmp/tr2-copy/test/validate.py; then
            echo "ERROR: the tree vanished and the guard comparing against it passed"
            exit 1
          else
            echo "OK: validator correctly FAILs — the missing-block branch is watched too, not only the missing-line one"
          fi

      - name: Property check (the Human column survives a header reorder)
        run: |
          set -eu
          rm -rf /tmp/hr-copy && cp -R . /tmp/hr-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/hr-copy/docs/superpowers/verification.md"
          t = open(p, encoding="utf-8").read()
          old = "| REQ | What | Run | Shipped in | Auto | Human | Note |"
          assert old in t, "PLANT DID NOT LAND: header absent"
          t = t.replace(old, "| REQ | What | Run | Shipped in | Auto | Note | Human |")
          # Any Auto value, not just `pass`: four rows read `none` after a review round,
          # and a plant that skips them leaves half the table in the old column order.
          t = re.sub(r"\| (pass|none|partial) \| (never|\d{4}-\d{2}-\d{2}) \| — \|",
                     r"| \1 | — | \2 |", t)
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/hr-copy/test/validate.py >/dev/null; then
            echo "OK: the column is found by name, so moving it does not silently misread a neighbour"
          else
            echo "ERROR: reordering the header broke the check — it was reading a position, not a column"
            exit 1
          fi

      - name: Negative self-test (a stale guard count in the newest CHANGELOG section)
        run: |
          set -eu
          rm -rf /tmp/cl1-copy && cp -R . /tmp/cl1-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/cl1-copy/CHANGELOG.md"
          t = open(p, encoding="utf-8").read()
          # Scope to the NEWEST section, which is the only one the guard reads. The
          # first version searched the whole file, so an entry writing the count in a
          # slightly different form sent the plant into a released section and the
          # guard stayed correctly silent — a probe that lands somewhere real and
          # proves nothing (R-001).
          heads = [x.start() for x in re.finditer(r"^## v", t, re.M)]
          assert len(heads) >= 2, "PLANT DID NOT LAND: fewer than two release sections"
          top = t[heads[0]:heads[1]]
          m = re.search(r"(Guards:\s*\d+\s*→\s*\*\*)(\d+)(\*\*)", top)
          assert m, "PLANT DID NOT LAND: no guard count in the NEWEST section"
          patched = top[:m.start(2)] + str(int(m.group(2)) - 1) + top[m.end(2):]
          open(p, "w", encoding="utf-8").write(t[:heads[0]] + patched + t[heads[1]:])
          EOF
          if python3 /tmp/cl1-copy/test/validate.py; then
            echo "ERROR: the release entry claimed a guard count the workflow does not define"
            exit 1
          else
            echo "OK: validator correctly FAILs — the newest entry's counts are claims about now"
          fi

      - name: Property check (older CHANGELOG sections are history, not claims about now)
        run: |
          set -eu
          rm -rf /tmp/cl3-copy && cp -R . /tmp/cl3-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/cl3-copy/CHANGELOG.md"
          t = open(p, encoding="utf-8").read()
          i = t.index("\n## v", t.index("\n## v") + 4)          # the SECOND version heading
          seg = t[i:]
          m = re.search(r"(Guards:\s*\d+\s*→\s*\*\*)(\d+)(\*\*)", seg)
          assert m, "PLANT DID NOT LAND: no guard count in an older section"
          open(p, "w", encoding="utf-8").write(
              t[:i] + seg[:m.start(2)] + "999" + seg[m.end(2):])
          EOF
          if python3 /tmp/cl3-copy/test/validate.py >/dev/null; then
            echo "OK: an older section states what was true on its own day and is left alone"
          else
            echo "ERROR: the guard reached back into history, where every count would need rewriting"
            exit 1
          fi

      - name: Negative self-test (MIN_PROPS drifting from the workflow's property count)
        run: |
          set -eu
          rm -rf /tmp/mp-copy && cp -R . /tmp/mp-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/mp-copy/test/negatives.py"
          t = open(p, encoding="utf-8").read()
          m = re.search(r"^MIN_PROPS\s*=\s*(\d+)", t, re.M)
          assert m, "PLANT DID NOT LAND: MIN_PROPS is not where the guard reads it"
          open(p, "w", encoding="utf-8").write(
              t[:m.start(1)] + str(int(m.group(1)) - 1) + t[m.end(1):])
          EOF
          if python3 /tmp/mp-copy/test/validate.py; then
            echo "ERROR: the property floor drifted below the count, exactly as MIN_EXPECTED did twice"
            exit 1
          else
            echo "OK: validator correctly FAILs — both floors are tied to the workflow now, not only one"
          fi

      - name: Negative self-test (a REQ paired with a run whose brief does not carry it)
        run: |
          set -eu
          rm -rf /tmp/wr-copy && cp -R . /tmp/wr-copy
          python3 - <<'EOF'
          p = "/tmp/wr-copy/docs/superpowers/verification.md"
          _orig_bytes = open(p, "rb").read()
          t = open(p, encoding="utf-8").read()
          open(p, "w", encoding="utf-8").write(
              t + "| REQ-014 | wrong pairing | `2026-08-04-run-continuity` | v1.10.0 | pass | never | — |\n")
          assert open(p, "rb").read() != _orig_bytes, "PLANT DID NOT LAND: a-req-paired-with-a-run-whose-brief"
          EOF
          if python3 /tmp/wr-copy/test/validate.py; then
            echo "ERROR: ids 001-014 recur across every brief, so a union check passes almost any mispairing"
            exit 1
          else
            echo "OK: validator correctly FAILs — the REQ is checked against the brief the row NAMES"
          fi

      - name: Negative self-test (a percentage reaching the exposure line)
        run: |
          set -eu
          rm -rf /tmp/pct-copy && cp -R . /tmp/pct-copy
          python3 - <<'EOF'
          p = "/tmp/pct-copy/test/validate.py"
          t = open(p, encoding="utf-8").read()
          needle = 'f"  exposure: {_VERIF_NEVER} unverified'
          assert needle in t, "PLANT DID NOT LAND: the exposure print is not where the guard reads it"
          open(p, "w", encoding="utf-8").write(
              t.replace(needle, 'f"  exposure: {_VERIF_NEVER}% unverified', 1))
          EOF
          if python3 /tmp/pct-copy/test/validate.py; then
            echo "ERROR: the exposure line rendered as a percentage — P(defect) is not computable from these inputs"
            exit 1
          else
            echo "OK: validator correctly FAILs — the one rendering this line may never take"
          fi

      - name: Negative self-test (the exposure doctrine dropping its never-checked rule)
        run: |
          set -eu
          rm -rf /tmp/nc-copy && cp -R . /tmp/nc-copy
          python3 - <<'EOF'
          p = "/tmp/nc-copy/plugins/task-pipeline/skills/task-pipeline/references/exposure.md"
          t = open(p, encoding="utf-8").read()
          assert "never checked" in t, "PLANT DID NOT LAND: the rule is not in the doctrine"
          open(p, "w", encoding="utf-8").write(t.replace("never checked", "zero days"))
          EOF
          if python3 /tmp/nc-copy/test/validate.py; then
            echo "ERROR: the doctrine lost the rule that zero days would read as 'checked today'"
            exit 1
          else
            echo "OK: validator correctly FAILs — the decision lives where the next reader looks"
          fi

      - name: Negative self-test (a step wiping a scratch dir it does not own)
        run: |
          set -eu
          rm -rf /tmp/mm-copy && cp -R . /tmp/mm-copy
          python3 - <<'EOF'
          p = "/tmp/mm-copy/.github/workflows/validate.yml"
          t = open(p, encoding="utf-8").read()
          needle = "rm -rf /tmp/sweep-copy-2 && cp -R . /tmp/sweep-copy-2"
          assert needle in t, "PLANT DID NOT LAND: the repaired pair is not where the guard reads it"
          open(p, "w", encoding="utf-8").write(
              t.replace(needle, "rm -rf /tmp/sweep-copy && cp -R . /tmp/sweep-copy-2", 1))
          EOF
          if python3 /tmp/mm-copy/test/validate.py; then
            echo "ERROR: a step deleting a directory it does not own passed — harmless serially, a race in parallel"
            exit 1
          else
            echo "OK: validator correctly FAILs — rm and cp in one step name the same directory"
          fi

      - name: Negative self-test (the header block drifting between its two copies)
        run: |
          set -eu
          rm -rf /tmp/hdr-copy && cp -R . /tmp/hdr-copy
          python3 - <<'EOF'
          p = "/tmp/hdr-copy/plugins/task-pipeline/skills/task-pipeline/references/stages.md"
          t = open(p, encoding="utf-8").read()
          assert " · unlooked N" in t, "PLANT DID NOT LAND: the field is not in the block"
          open(p, "w", encoding="utf-8").write(t.replace(" · unlooked N", "", 1))
          EOF
          if python3 /tmp/hdr-copy/test/validate.py; then
            echo "ERROR: one block, two copies, and a field silently in only one of them"
            exit 1
          else
            echo "OK: validator correctly FAILs — both directions of the header field set"
          fi

      - name: Negative self-test (a rail glyph the legend does not define)
        run: |
          set -eu
          rm -rf /tmp/glyph-copy && cp -R . /tmp/glyph-copy
          python3 - <<'EOF'
          p = "/tmp/glyph-copy/plugins/task-pipeline/skills/task-pipeline/references/progress.md"
          t = open(p, encoding="utf-8").read()
          assert "3 ▶" in t, "PLANT DID NOT LAND: the rail's in-flight glyph moved"
          open(p, "w", encoding="utf-8").write(t.replace("3 ▶", "3 »", 1))
          EOF
          if python3 /tmp/glyph-copy/test/validate.py; then
            echo "ERROR: the rail prints a symbol a reader has nothing to look up"
            exit 1
          else
            echo "OK: validator correctly FAILs — every printed glyph is in the legend"
          fi

      - name: Negative self-test (the rail claiming a stage count of its own)
        run: |
          set -eu
          rm -rf /tmp/rail-copy && cp -R . /tmp/rail-copy
          python3 - <<'EOF'
          p = "/tmp/rail-copy/plugins/task-pipeline/skills/task-pipeline/references/progress.md"
          t = open(p, encoding="utf-8").read()
          needle = "come from the project's `pipeline.json`"
          assert needle in t, "PLANT DID NOT LAND: the computed-rail clause moved"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "come from the eleven stages", 1))
          EOF
          if python3 /tmp/rail-copy/test/validate.py; then
            echo "ERROR: a rail hardcoding the example flow is wrong in every project that replaced it"
            exit 1
          else
            echo "OK: validator correctly FAILs — the rail is computed, never eleven"
          fi

      - name: Negative self-test (a ledger shape declared and never shown)
        run: |
          set -eu
          rm -rf /tmp/led-copy && cp -R . /tmp/led-copy
          python3 - <<'EOF'
          p = "/tmp/led-copy/plugins/task-pipeline/skills/task-pipeline/templates/run.md"
          t = open(p, encoding="utf-8").read()
          assert t.count("touch: src/export.ts") >= 2, "PLANT DID NOT LAND: the Log fence lost its touch lines"
          open(p, "w", encoding="utf-8").write(t.replace("touch: src/export.ts", "seen:  src/export.ts"))
          EOF
          if python3 /tmp/led-copy/test/validate.py; then
            echo "ERROR: a declared line shape with no worked example, and an example with no declaration"
            exit 1
          else
            echo "OK: validator correctly FAILs — declared and shown, compared both ways"
          fi

      - name: Negative self-test (a ledger shape no reader names)
        run: |
          set -eu
          rm -rf /tmp/ledr-copy && cp -R . /tmp/ledr-copy
          python3 - <<'EOF'
          p = "/tmp/ledr-copy/plugins/task-pipeline/skills/task-pipeline/templates/run.md"
          t = open(p, encoding="utf-8").read()
          assert "iter: " in t, "PLANT DID NOT LAND: the iteration line moved"
          open(p, "w", encoding="utf-8").write(t.replace("iter: ", "cycle:"))
          EOF
          if python3 /tmp/ledr-copy/test/validate.py; then
            echo "ERROR: a ledger shape nothing reads is a shape nothing writes"
            exit 1
          else
            echo "OK: validator correctly FAILs — every declared shape has a named reader"
          fi

      - name: Negative self-test (the review loop left with no cap)
        run: |
          set -eu
          rm -rf /tmp/rc1-copy && cp -R . /tmp/rc1-copy
          python3 - <<'EOF'
          p = "/tmp/rc1-copy/plugins/task-pipeline/skills/task-pipeline/references/loop-guard.md"
          t = open(p, encoding="utf-8").read()
          needle = "**3 review rounds** per artifact"
          assert needle in t, "PLANT DID NOT LAND: the cap is not where the guard reads it"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "review rounds per artifact", 1))
          EOF
          if python3 /tmp/rc1-copy/test/validate.py; then
            echo "ERROR: the loop this repository ran ten rounds of is capped by nothing again"
            exit 1
          else
            echo "OK: validator correctly FAILs — the review loop carries a stated cap"
          fi

      - name: Negative self-test (the review cap moved in one file only)
        run: |
          set -eu
          rm -rf /tmp/rc2-copy && cp -R . /tmp/rc2-copy
          python3 - <<'EOF'
          p = "/tmp/rc2-copy/plugins/task-pipeline/skills/task-pipeline/references/loop-guard.md"
          t = open(p, encoding="utf-8").read()
          needle = "**3 review rounds** per artifact"
          assert needle in t, "PLANT DID NOT LAND: the cap moved"
          open(p, "w", encoding="utf-8").write(
              t.replace(needle, "**4 review rounds** per artifact", 1))
          EOF
          if python3 /tmp/rc2-copy/test/validate.py; then
            echo "ERROR: the cap says one number in the file that owns it and another where the loop runs"
            exit 1
          else
            echo "OK: validator correctly FAILs — the cap is computed from one file and required in the other"
          fi

      - name: Negative self-test (the review cap with no config key)
        run: |
          set -eu
          rm -rf /tmp/rc3-copy && cp -R . /tmp/rc3-copy
          python3 - <<'EOF'
          p = "/tmp/rc3-copy/plugins/task-pipeline/skills/task-pipeline/references/stages.md"
          t = open(p, encoding="utf-8").read()
          assert "`run.review.maxRounds`" in t, "PLANT DID NOT LAND: the config key moved"
          open(p, "w", encoding="utf-8").write(t.replace("`run.review.maxRounds`", "the config", 1))
          EOF
          if python3 /tmp/rc3-copy/test/validate.py; then
            echo "ERROR: a default nobody can change is a default everybody overrides in their head"
            exit 1
          else
            echo "OK: validator correctly FAILs — the cap names the key that changes it"
          fi

      - name: Negative self-test (the short path marking a stage with an undefined glyph)
        run: |
          set -eu
          rm -rf /tmp/sp-copy && cp -R . /tmp/sp-copy
          python3 - <<'EOF'
          p = "/tmp/sp-copy/plugins/task-pipeline/skills/task-pipeline/references/stages.md"
          t = open(p, encoding="utf-8").read()
          needle = "marked `⊘` with the triage answer"
          assert needle in t, "PLANT DID NOT LAND: the short path's glyph moved"
          open(p, "w", encoding="utf-8").write(
              t.replace(needle, "marked `※` with the triage answer", 1))
          EOF
          if python3 /tmp/sp-copy/test/validate.py; then
            echo "ERROR: a skipped stage marked with a symbol no legend defines"
            exit 1
          else
            echo "OK: validator correctly FAILs — every skip glyph is in the legend"
          fi

      - name: Negative self-test (the exposure example drifting from its own print)
        run: |
          set -eu
          rm -rf /tmp/ex1-copy && cp -R . /tmp/ex1-copy
          python3 - <<'EOF'
          p = "/tmp/ex1-copy/plugins/task-pipeline/skills/task-pipeline/references/exposure.md"
          t = open(p, encoding="utf-8").read()
          assert "releases carry one" in t, "PLANT DID NOT LAND: the third component moved"
          open(p, "w", encoding="utf-8").write(
              t.replace("releases carry one", "releases since the last confirmation"))
          EOF
          if python3 /tmp/ex1-copy/test/validate.py; then
            echo "ERROR: the doctrine teaches a format the code has never printed"
            exit 1
          else
            echo "OK: validator correctly FAILs — the example is computed from the print"
          fi

      - name: Negative self-test (a live count baked into the exposure example)
        run: |
          set -eu
          rm -rf /tmp/ex2-copy && cp -R . /tmp/ex2-copy
          python3 - <<'EOF'
          p = "/tmp/ex2-copy/plugins/task-pipeline/skills/task-pipeline/references/exposure.md"
          t = open(p, encoding="utf-8").read()
          assert "exposure: N unverified" in t, "PLANT DID NOT LAND: the example line moved"
          open(p, "w", encoding="utf-8").write(
              t.replace("exposure: N unverified", "exposure: 99 unverified", 1))
          EOF
          if python3 /tmp/ex2-copy/test/validate.py; then
            echo "ERROR: a live count in a doctrine is a count nobody updates"
            exit 1
          else
            echo "OK: validator correctly FAILs — the worked example carries no digits"
          fi

      - name: Negative self-test (a companion pointed at a stage that never names it)
        run: |
          set -eu
          rm -rf /tmp/cw1-copy && cp -R . /tmp/cw1-copy
          python3 - <<'EOF'
          p = "/tmp/cw1-copy/plugins/task-pipeline/skills/task-pipeline/references/companion-skills.md"
          t = open(p, encoding="utf-8").read()
          needle = "| stage 3 — the **VISUAL track**"
          assert needle in t, "PLANT DID NOT LAND: the visual row moved"
          open(p, "w", encoding="utf-8").write(t.replace(needle, "| stage 4 — the **VISUAL track**", 1))
          EOF
          if python3 /tmp/cw1-copy/test/validate.py; then
            echo "ERROR: the operator is told to install a companion for a stage that has not heard of it"
            exit 1
          else
            echo "OK: validator correctly FAILs — the matrix and the stage it points at agree"
          fi

      - name: Negative self-test (a stage dropping the companion the matrix aims at it)
        run: |
          set -eu
          rm -rf /tmp/cw2-copy && cp -R . /tmp/cw2-copy
          python3 - <<'EOF'
          p = "/tmp/cw2-copy/plugins/task-pipeline/skills/task-pipeline/references/stages.md"
          t = open(p, encoding="utf-8").read()
          assert "`sheleg-design`" in t, "PLANT DID NOT LAND: the visual companion moved"
          open(p, "w", encoding="utf-8").write(t.replace("`sheleg-design`", "the visual skill"))
          EOF
          if python3 /tmp/cw2-copy/test/validate.py; then
            echo "ERROR: a recommended companion no stage names is a recommendation for the reader only"
            exit 1
          else
            echo "OK: validator correctly FAILs — the stage names its own companions"
          fi

      - name: Negative self-test (a stage-3 track with no owning skill)
        run: |
          set -eu
          rm -rf /tmp/trk1-copy && cp -R . /tmp/trk1-copy
          python3 - <<'EOF'
          p = "/tmp/trk1-copy/plugins/task-pipeline/skills/task-pipeline/references/stages.md"
          t = open(p, encoding="utf-8").read()
          assert "`copywriting`" in t, "PLANT DID NOT LAND: the copy companion moved"
          open(p, "w", encoding="utf-8").write(t.replace("`copywriting`", "the writing skill"))
          EOF
          if python3 /tmp/trk1-copy/test/validate.py; then
            echo "ERROR: a track heading with no skill behind it — which is what the copy half was for a year"
            exit 1
          else
            echo "OK: validator correctly FAILs — every stage-3 track names its owner"
          fi

      - name: Negative self-test (a stage-3 track deleted outright)
        run: |
          set -eu
          rm -rf /tmp/trk2-copy && cp -R . /tmp/trk2-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/trk2-copy/plugins/task-pipeline/skills/task-pipeline/references/stages.md"
          t = open(p, encoding="utf-8").read()
          # Case-insensitive on purpose. The first version replaced only the heading's
          # SHOUTED form and left "ends the visual track" in the refusal sentence, so
          # the guard kept finding the track and stayed correctly quiet — a probe that
          # deletes one spelling of a phrase and calls the class removed.
          n, k = re.subn(r"visual track", "look-and-feel pass", t, flags=re.I)
          assert k >= 2, "PLANT DID NOT LAND: the visual track is spelled fewer ways than expected"
          open(p, "w", encoding="utf-8").write(n)
          EOF
          if python3 /tmp/trk2-copy/test/validate.py; then
            echo "ERROR: the stage names what the interface must do and not how it looks"
            exit 1
          else
            echo "OK: validator correctly FAILs — all three tracks are named at stage 3"
          fi

      - name: Negative self-test (a redaction rule added without the count moving)
        run: |
          set -eu
          rm -rf /tmp/rp1-copy && cp -R . /tmp/rp1-copy
          python3 - <<'EOF'
          p = "/tmp/rp1-copy/plugins/task-pipeline/skills/task-pipeline/references/retrospective.md"
          t = open(p, encoding="utf-8").read()
          needle = "5. **The title states the class"
          assert needle in t, "PLANT DID NOT LAND: the rule list moved"
          open(p, "w", encoding="utf-8").write(t.replace(
              needle,
              "5. **No timestamps.** A wall-clock time narrows a repository to a person's day.\n"
              "6. **The title states the class", 1))
          EOF
          if python3 /tmp/rp1-copy/test/validate.py; then
            echo "ERROR: an enumeration counted in prose drifted from its own items"
            exit 1
          else
            echo "OK: validator correctly FAILs — the rule count is computed from the rules"
          fi

      - name: Negative self-test (the redaction rules flattened into prose)
        run: |
          set -eu
          rm -rf /tmp/rp2-copy && cp -R . /tmp/rp2-copy
          python3 - <<'EOF'
          p = "/tmp/rp2-copy/plugins/task-pipeline/skills/task-pipeline/references/retrospective.md"
          t = open(p, encoding="utf-8").read()
          assert "1. **No host paths.**" in t, "PLANT DID NOT LAND: rule 1 moved"
          open(p, "w", encoding="utf-8").write(t.replace("1. **No host paths.**", "- **No host paths.**", 1))
          EOF
          if python3 /tmp/rp2-copy/test/validate.py; then
            echo "ERROR: a list nobody can point at an item of is advice, not a contract"
            exit 1
          else
            echo "OK: validator correctly FAILs — the redaction rules stay an enumeration"
          fi

      - name: Negative self-test (an absolute host path in the worked issue body)
        run: |
          set -eu
          rm -rf /tmp/rp3-copy && cp -R . /tmp/rp3-copy
          python3 - <<'EOF'
          p = "/tmp/rp3-copy/plugins/task-pipeline/skills/task-pipeline/references/retrospective.md"
          t = open(p, encoding="utf-8").read()
          needle = "title: [retro] a queue is not a diagnosis"
          assert needle in t, "PLANT DID NOT LAND: the worked issue moved"
          open(p, "w", encoding="utf-8").write(
              t.replace(needle, needle + "\nwhere: /Users/someone/work/api", 1))
          EOF
          if python3 /tmp/rp3-copy/test/validate.py; then
            echo "ERROR: the doctrine's own example breaks rule 1 of the list beside it"
            exit 1
          else
            echo "OK: validator correctly FAILs — the example obeys the rules it teaches"
          fi

      - name: Negative self-test (a commit id in the worked issue body)
        run: |
          set -eu
          rm -rf /tmp/rp4-copy && cp -R . /tmp/rp4-copy
          python3 - <<'EOF'
          p = "/tmp/rp4-copy/plugins/task-pipeline/skills/task-pipeline/references/retrospective.md"
          t = open(p, encoding="utf-8").read()
          needle = "title: [retro] a queue is not a diagnosis"
          assert needle in t, "PLANT DID NOT LAND: the worked issue moved"
          open(p, "w", encoding="utf-8").write(t.replace(needle, needle + "\nfound at: 4233c3d", 1))
          EOF
          if python3 /tmp/rp4-copy/test/validate.py; then
            echo "ERROR: a host commit in a public issue body — rule 2, in the example itself"
            exit 1
          else
            echo "OK: validator correctly FAILs — no host identifier in the worked body"
          fi

      - name: Negative self-test (a host repository slug in the worked issue body)
        run: |
          set -eu
          rm -rf /tmp/rp5-copy && cp -R . /tmp/rp5-copy
          python3 - <<'EOF'
          p = "/tmp/rp5-copy/plugins/task-pipeline/skills/task-pipeline/references/retrospective.md"
          t = open(p, encoding="utf-8").read()
          needle = "repo:  ssheleg/task-pipeline"
          assert needle in t, "PLANT DID NOT LAND: the repo line moved"
          open(p, "w", encoding="utf-8").write(
              t.replace(needle, needle + "\nseen in: acme-corp/billing-api", 1))
          EOF
          if python3 /tmp/rp5-copy/test/validate.py; then
            echo "ERROR: the example names a repository that is not the skill's own"
            exit 1
          else
            echo "OK: validator correctly FAILs — only the skill's own repo appears"
          fi

      - name: Negative self-test (the schema losing the key the doctrine names)
        run: |
          set -eu
          rm -rf /tmp/rp6-copy && cp -R . /tmp/rp6-copy
          python3 - <<'EOF'
          p = "/tmp/rp6-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.schema.json"
          t = open(p, encoding="utf-8").read()
          assert '"publish"' in t, "PLANT DID NOT LAND: the publish key moved"
          open(p, "w", encoding="utf-8").write(t.replace('"publish"', '"publish_disabled"', 1))
          EOF
          if python3 /tmp/rp6-copy/test/validate.py; then
            echo "ERROR: the key the doctrine tells an operator to set validates nothing"
            exit 1
          else
            echo "OK: validator correctly FAILs — doctrine and schema name one key"
          fi

      - name: Negative self-test (no stage naming the publish step)
        run: |
          set -eu
          rm -rf /tmp/rp7-copy && cp -R . /tmp/rp7-copy
          python3 - <<'EOF'
          p = "/tmp/rp7-copy/plugins/task-pipeline/skills/task-pipeline/references/stages.md"
          t = open(p, encoding="utf-8").read()
          assert "`retro.publish`" in t, "PLANT DID NOT LAND: the stage reference moved"
          open(p, "w", encoding="utf-8").write(t.replace("`retro.publish`", "the publish setting"))
          EOF
          if python3 /tmp/rp7-copy/test/validate.py; then
            echo "ERROR: a step in the retro's doctrine and in no stage is a step that never runs"
            exit 1
          else
            echo "OK: validator correctly FAILs — the outward step is named by a stage"
          fi

      - name: Negative self-test (the reader losing its NO READER state)
        run: |
          set -eu
          rm -rf /tmp/rd1-copy && cp -R . /tmp/rd1-copy
          python3 - <<'EOF'
          p = "/tmp/rd1-copy/plugins/task-pipeline/skills/task-pipeline/references/review.md"
          t = open(p, encoding="utf-8").read()
          n = "reader: NO READER — <why>"
          assert n in t, "PLANT DID NOT LAND: the reader states moved"
          open(p, "w", encoding="utf-8").write(t.replace(n, "reader: unknown", 1))
          EOF
          if python3 /tmp/rd1-copy/test/validate.py; then
            echo "ERROR: a reader that never read is now indistinguishable from one that found nothing"
            exit 1
          else
            echo "OK: validator correctly FAILs — NO READER is a printed state"
          fi

      - name: Negative self-test (a stage dropping a reader state)
        run: |
          set -eu
          rm -rf /tmp/rd2-copy && cp -R . /tmp/rd2-copy
          python3 - <<'EOF'
          p = "/tmp/rd2-copy/plugins/task-pipeline/skills/task-pipeline/references/stages.md"
          t = open(p, encoding="utf-8").read()
          assert "`reader: none found`" in t, "PLANT DID NOT LAND: the stage's reader states moved"
          open(p, "w", encoding="utf-8").write(t.replace("`reader: none found`", "a clean verdict", 1))
          EOF
          if python3 /tmp/rd2-copy/test/validate.py; then
            echo "ERROR: a state the doctrine defines and no stage emits is a state nobody records"
            exit 1
          else
            echo "OK: validator correctly FAILs — every reader state is named by the stage"
          fi

      - name: Negative self-test (the probe harness losing its target-guard assertion)
        run: |
          set -eu
          rm -rf /tmp/ph1-copy && cp -R . /tmp/ph1-copy
          python3 - <<'EOF'
          p = "/tmp/ph1-copy/test/probe.py"
          t = open(p, encoding="utf-8").read()
          assert "expect" in t, "PLANT DID NOT LAND: the harness moved"
          open(p, "w", encoding="utf-8").write(t.replace("expect", "wanted"))
          EOF
          if python3 /tmp/ph1-copy/test/validate.py; then
            echo "ERROR: a probe that proves only that SOMETHING broke proves nothing about its guard"
            exit 1
          else
            echo "OK: validator correctly FAILs — a plant names the guard it targets"
          fi

      - name: Negative self-test (the probe harness losing its entry point)
        run: |
          set -eu
          rm -rf /tmp/ph2-copy && cp -R . /tmp/ph2-copy
          python3 - <<'EOF'
          p = "/tmp/ph2-copy/package.json"
          t = open(p, encoding="utf-8").read()
          n = "test/probe.py --self-test"
          assert n in t, "PLANT DID NOT LAND: the script moved"
          open(p, "w", encoding="utf-8").write(t.replace(n, "true", 1))
          EOF
          if python3 /tmp/ph2-copy/test/validate.py; then
            echo "ERROR: a harness with no entry point is a file, not a gate"
            exit 1
          else
            echo "OK: validator correctly FAILs — the harness is runnable"
          fi

      - name: Negative self-test (gates.md teaching probing without naming the harness)
        run: |
          set -eu
          rm -rf /tmp/ph3-copy && cp -R . /tmp/ph3-copy
          python3 - <<'EOF'
          p = "/tmp/ph3-copy/plugins/task-pipeline/skills/task-pipeline/references/gates.md"
          t = open(p, encoding="utf-8").read()
          n = "`test/probe.py` does all three"
          assert n in t, "PLANT DID NOT LAND: the pointer moved"
          open(p, "w", encoding="utf-8").write(t.replace(n, "a harness does all three", 1))
          EOF
          if python3 /tmp/ph3-copy/test/validate.py; then
            echo "ERROR: the next probe gets hand-rolled for the fourth time"
            exit 1
          else
            echo "OK: validator correctly FAILs — the probing doctrine points at the harness"
          fi

      - name: Negative self-test (the retro's uncapped log slipping back into "read in full")
        run: |
          set -eu
          rm -rf /tmp/rf1-copy && cp -R . /tmp/rf1-copy
          python3 - <<'EOF'
          p = "/tmp/rf1-copy/docs/superpowers/retro.md"
          t = open(p, encoding="utf-8").read()
          n = "The **Recent log** and the **archive**"
          assert n in t, "PLANT DID NOT LAND: the read-in-full sentence moved"
          open(p, "w", encoding="utf-8").write(
              t.replace(n, "The **Recent log** is read in full. The **archive**", 1))
          EOF
          if python3 /tmp/rf1-copy/test/validate.py; then
            echo "ERROR: an uncapped section is back inside the source whose cap justifies reading it"
            exit 1
          else
            echo "OK: validator correctly FAILs — the narrative log is queried, not read"
          fi

      - name: Negative self-test (a consumer re-binding the log to "in full")
        run: |
          set -eu
          rm -rf /tmp/rf2-copy && cp -R . /tmp/rf2-copy
          python3 - <<'EOF'
          p = "/tmp/rf2-copy/plugins/task-pipeline/skills/task-pipeline/references/knowledge-sources.md"
          t = open(p, encoding="utf-8").read()
          n = "the *Recent log* is queried by the task's nouns like the archive"
          assert n in t, "PLANT DID NOT LAND: the harvest clause moved"
          open(p, "w", encoding="utf-8").write(t.replace(n, "the *Recent log* is read in full", 1))
          EOF
          if python3 /tmp/rf2-copy/test/validate.py; then
            echo "ERROR: the harvest reads in full what nothing caps"
            exit 1
          else
            echo "OK: validator correctly FAILs — every consumer agrees on what is read"
          fi

      - name: Negative self-test (a preflight silent about the skill's own evidence)
        run: |
          set -eu
          rm -rf /tmp/pf1-copy && cp -R . /tmp/pf1-copy
          python3 - <<'EOF'
          p = "/tmp/pf1-copy/plugins/task-pipeline/skills/task-pipeline/references/companion-skills.md"
          t = open(p, encoding="utf-8").read()
          n = "This skill's own behaviour is unverified"
          assert n in t, "PLANT DID NOT LAND: the behaviour line moved"
          open(p, "w", encoding="utf-8").write(t.replace(n, "This skill is ready", 1))
          EOF
          if python3 /tmp/pf1-copy/test/validate.py; then
            echo "ERROR: a skill silent about its own evidence is read as tested"
            exit 1
          else
            echo "OK: validator correctly FAILs — the preflight states what has not been measured"
          fi
      - name: Negative self-test (the change half of the boundary leaves the description)
        run: |
          set -eu
          rm -rf /tmp/rt01-copy && cp -R . /tmp/rt01-copy
          python3 - <<'EOF'
          p = "/tmp/rt01-copy/plugins/task-pipeline/skills/task-pipeline/SKILL.md"
          t = open(p, encoding="utf-8").read()
          n = 'changes the repository'
          assert n in t, "PLANT DID NOT LAND: rt01"
          open(p, "w", encoding="utf-8").write(t.replace(n, 'changes the codebase', 1))
          EOF
          if python3 /tmp/rt01-copy/test/validate.py 2>&1 | grep -q 'the change half of the boundary is gone'; then
            echo "OK: validator FAILs — a feature must still have a boundary that names it"
          else
            echo "ERROR: the description lost the change half and nothing objected"
            exit 1
          fi
      - name: Negative self-test (the findings half of the boundary leaves the description)
        run: |
          set -eu
          rm -rf /tmp/rt02-copy && cp -R . /tmp/rt02-copy
          python3 - <<'EOF'
          p = "/tmp/rt02-copy/plugins/task-pipeline/skills/task-pipeline/SKILL.md"
          t = open(p, encoding="utf-8").read()
          n = 'finding that lands in it'
          assert n in t, "PLANT DID NOT LAND: rt02"
          open(p, "w", encoding="utf-8").write(t.replace(n, 'thing produced by it', 1))
          EOF
          if python3 /tmp/rt02-copy/test/validate.py 2>&1 | grep -q 'the findings half of the boundary is gone'; then
            echo "OK: validator FAILs — an audit is refused when nothing names it"
          else
            echo "ERROR: the description lost the findings half and nothing objected"
            exit 1
          fi
      - name: Negative self-test (reading returns to the exclusion clause)
        run: |
          set -eu
          rm -rf /tmp/rt03-copy && cp -R . /tmp/rt03-copy
          python3 - <<'EOF'
          p = "/tmp/rt03-copy/plugins/task-pipeline/skills/task-pipeline/SKILL.md"
          t = open(p, encoding="utf-8").read()
          n = 'explaining code, a typo'
          assert n in t, "PLANT DID NOT LAND: rt03"
          open(p, "w", encoding="utf-8").write(t.replace(n, 'explaining or reading code, a typo', 1))
          EOF
          if python3 /tmp/rt03-copy/test/validate.py 2>&1 | grep -q 'SKILL.md description: reading is named as an exclusion again'; then
            echo "OK: validator FAILs — reading is the opening move of every findings class"
          else
            echo "ERROR: reading came back to the exclusions and nothing objected"
            exit 1
          fi
      - name: Negative self-test (an exclusion locked in 2026-08-03 drops out)
        run: |
          set -eu
          rm -rf /tmp/rt04-copy && cp -R . /tmp/rt04-copy
          python3 - <<'EOF'
          p = "/tmp/rt04-copy/plugins/task-pipeline/skills/task-pipeline/SKILL.md"
          t = open(p, encoding="utf-8").read()
          n = ', a typo or a one-line edit'
          assert n in t, "PLANT DID NOT LAND: rt04"
          open(p, "w", encoding="utf-8").write(t.replace(n, '', 1))
          EOF
          if python3 /tmp/rt04-copy/test/validate.py 2>&1 | grep -q "left the 'Not for:' clause"; then
            echo "OK: validator FAILs — the clause and the NOTRIG evals may not drift"
          else
            echo "ERROR: a locked exclusion vanished and nothing objected"
            exit 1
          fi
      - name: Negative self-test (a verb locked by the design never reaches the surface)
        run: |
          set -eu
          rm -rf /tmp/rt05-copy && cp -R . /tmp/rt05-copy
          python3 - <<'EOF'
          p = "/tmp/rt05-copy/plugins/task-pipeline/skills/task-pipeline/SKILL.md"
          t = open(p, encoding="utf-8").read()
          n = 'внедрить, перевести'
          assert n in t, "PLANT DID NOT LAND: rt05"
          open(p, "w", encoding="utf-8").write(t.replace(n, 'внедрить', 1))
          EOF
          if python3 /tmp/rt05-copy/test/validate.py 2>&1 | grep -q 'locked by the 2026-08-03 design'; then
            echo "OK: validator FAILs — this is exactly how перевести sat unshipped for a week"
          else
            echo "ERROR: a locked verb vanished and nothing objected"
            exit 1
          fi
      - name: Negative self-test (a findings class is missing from the portable routing rule)
        run: |
          set -eu
          rm -rf /tmp/rt06-copy && cp -R . /tmp/rt06-copy
          python3 - <<'EOF'
          p = "/tmp/rt06-copy/plugins/task-pipeline/skills/task-pipeline/templates/routing-rule.md"
          t = open(p, encoding="utf-8").read()
          n = 'a production check'
          assert n in t, "PLANT DID NOT LAND: rt06"
          open(p, "w", encoding="utf-8").write(t.replace(n, 'a deployment check', 1))
          EOF
          if python3 /tmp/rt06-copy/test/validate.py 2>&1 | grep -q 'templates/routing-rule.md: the boundary is stated here too'; then
            echo "OK: validator FAILs — a portable rule must agree with the description"
          else
            echo "ERROR: the portable rule lost a class and nothing objected"
            exit 1
          fi
      - name: Negative self-test (a findings class is missing from the Cursor rule)
        run: |
          set -eu
          rm -rf /tmp/rt07-copy && cp -R . /tmp/rt07-copy
          python3 - <<'EOF'
          p = "/tmp/rt07-copy/cursor/rules/task-pipeline.mdc"
          t = open(p, encoding="utf-8").read()
          n = 'PR review'
          assert n in t, "PLANT DID NOT LAND: rt07"
          open(p, "w", encoding="utf-8").write(t.replace(n, 'merge inspection', -1))
          EOF
          if python3 /tmp/rt07-copy/test/validate.py 2>&1 | grep -q 'task-pipeline.mdc: the boundary is stated here too'; then
            echo "OK: validator FAILs — the Cursor rule states the same boundary"
          else
            echo "ERROR: the Cursor rule lost a class and nothing objected"
            exit 1
          fi
      - name: Negative self-test (a findings class has no eval query)
        run: |
          set -eu
          rm -rf /tmp/rt08-copy && cp -R . /tmp/rt08-copy
          python3 - <<'EOF'
          p = "/tmp/rt08-copy/evals/task-pipeline.evals.json"
          t = open(p, encoding="utf-8").read()
          n = 'нет ли ошибок в обработчике вебхуков'
          assert n in t, "PLANT DID NOT LAND: rt08"
          open(p, "w", encoding="utf-8").write(t.replace(n, 'is the webhook handler sound', 1))
          EOF
          if python3 /tmp/rt08-copy/test/validate.py 2>&1 | grep -q 'with no should_trigger query'; then
            echo "OK: validator FAILs — named and untested is the state B-046 measured"
          else
            echo "ERROR: a class lost its only eval and nothing objected"
            exit 1
          fi
      - name: Negative self-test (a release entry that talks about guards without a readable count)
        run: |
          set -eu
          rm -rf /tmp/cl9-copy && cp -R . /tmp/cl9-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/cl9-copy/CHANGELOG.md"
          t = open(p, encoding="utf-8").read()
          # DERIVED, not literal. This probe held `Guards: A -> **B**` as a hard-coded
          # string and broke on three consecutive releases, once letting a red CI reach
          # main. A needle that has to be maintained is a needle that goes stale between
          # the change and the run.
          heads = [m.start() for m in re.finditer(r"^## v", t, re.M)]
          assert len(heads) >= 2, "PLANT DID NOT LAND: fewer than two release sections"
          top = t[heads[0]:heads[1]]
          m = re.search(r"Guards:\s*(\d+)\s*(?:\u2192|->)\s*(\*{0,2})(\d+)(\*{0,2})", top)
          assert m, "PLANT DID NOT LAND: the newest section states no readable guard count"
          # The exact shape the guard went dormant on: same numbers, no colon.
          patched = top[:m.start()] + "Guards %s \u2192 %s%s%s" % (
              m.group(1), m.group(2), m.group(3), m.group(4)) + top[m.end():]
          open(p, "w", encoding="utf-8").write(t[:heads[0]] + patched + t[heads[1]:])
          EOF
          if python3 /tmp/cl9-copy/test/validate.py 2>&1 | grep -q "states no .Guards: N"; then
            echo "OK: validator FAILs — a count-shaped sentence with no readable count is not silence"
          else
            echo "ERROR: the guard went dormant on a reworded entry and the suite stayed green"
            exit 1
          fi
      - name: Negative self-test (a findings class added with no eval-query alias)
        run: |
          set -eu
          rm -rf /tmp/rt09-copy && cp -R . /tmp/rt09-copy
          python3 - <<'EOF'
          p = "/tmp/rt09-copy/plugins/task-pipeline/skills/task-pipeline/SKILL.md"
          t = open(p, encoding="utf-8").read()
          n = 'PR review/ревью PR —'
          assert n in t, "PLANT DID NOT LAND: rt09"
          open(p, "w", encoding="utf-8").write(t.replace(n, 'PR review/ревью PR, security review/секьюрити —', 1))
          EOF
          if python3 /tmp/rt09-copy/test/validate.py 2>&1 | grep -q 'no eval-query alias declared'; then
            echo "OK: validator FAILs — a class with no declared alias would measure nothing"
          else
            echo "ERROR: a fifth class slipped in unmapped and the guard reported coverage it never looked for"
            exit 1
          fi
      - name: Negative self-test (the explanation exclusion leaves the clause)
        run: |
          set -eu
          rm -rf /tmp/rt10-copy && cp -R . /tmp/rt10-copy
          python3 - <<'EOF'
          p = "/tmp/rt10-copy/plugins/task-pipeline/skills/task-pipeline/SKILL.md"
          t = open(p, encoding="utf-8").read()
          n = 'answering a question, explaining code'
          assert n in t, "PLANT DID NOT LAND: rt10"
          open(p, "w", encoding="utf-8").write(t.replace(n, 'answering a question, describing code', 1))
          EOF
          if python3 /tmp/rt10-copy/test/validate.py 2>&1 | grep -q 'the explanation exclusion left'; then
            echo "OK: validator FAILs — all three locked exclusions are held, not just the one probed"
          else
            echo "ERROR: the explanation exclusion vanished and nothing objected"
            exit 1
          fi
      - name: Negative self-test (the findings clause loses the shape four checks read)
        run: |
          set -eu
          rm -rf /tmp/rt11-copy && cp -R . /tmp/rt11-copy
          python3 - <<'EOF'
          p = "/tmp/rt11-copy/plugins/task-pipeline/skills/task-pipeline/SKILL.md"
          t = open(p, encoding="utf-8").read()
          n = 'finding that lands in it: audit'
          assert n in t, "PLANT DID NOT LAND: rt11"
          open(p, "w", encoding="utf-8").write(t.replace(n, 'finding that lands in it — audit', 1))
          EOF
          if python3 /tmp/rt11-copy/test/validate.py 2>&1 | grep -q 'no longer has the shape'; then
            echo "OK: validator FAILs — a colon changed to a dash took four checks offline in silence"
          else
            echo "ERROR: the clause changed shape and four checks vanished quietly"
            exit 1
          fi
      - name: Negative self-test (the locked verb list stops being readable)
        run: |
          set -eu
          rm -rf /tmp/rt12-copy && cp -R . /tmp/rt12-copy
          python3 - <<'EOF'
          p = "/tmp/rt12-copy/docs/superpowers/specs/2026-08-03-default-routing-adoption-design.md"
          t = open(p, encoding="utf-8").read()
          n = '**work verbs, RU + EN:**'
          assert n in t, "PLANT DID NOT LAND: rt12"
          open(p, "w", encoding="utf-8").write(t.replace(n, '**work verbs (RU + EN):**', 1))
          EOF
          if python3 /tmp/rt12-copy/test/validate.py 2>&1 | grep -q 'must not be edited to suit a guard'; then
            echo "OK: validator FAILs — the anti-drift guard says so instead of going dormant"
          else
            echo "ERROR: the locked list became unreadable and the guard went silent"
            exit 1
          fi
      - name: Negative self-test (a class separator the parser cannot see)
        run: |
          set -eu
          rm -rf /tmp/rt13-copy && cp -R . /tmp/rt13-copy
          python3 - <<'EOF'
          p = "/tmp/rt13-copy/plugins/task-pipeline/skills/task-pipeline/SKILL.md"
          t = open(p, encoding="utf-8").read()
          n = 'bug hunt/проверь ошибки, production check'
          assert n in t, "PLANT DID NOT LAND: rt13"
          open(p, "w", encoding="utf-8").write(t.replace(n, 'bug hunt/проверь ошибки and production check', 1))
          EOF
          if python3 /tmp/rt13-copy/test/validate.py 2>&1 | grep -q 'separator it cannot see'; then
            echo "OK: validator FAILs — a shorter class list is a failure, never a silence"
          else
            echo "ERROR: an unparseable separator shrank the class list and nothing objected"
            exit 1
          fi
      - name: Negative self-test (reading returns as an exclusion in the portable rule)
        run: |
          set -eu
          rm -rf /tmp/rt15-copy && cp -R . /tmp/rt15-copy
          python3 - <<'EOF'
          p = "/tmp/rt15-copy/plugins/task-pipeline/skills/task-pipeline/templates/routing-rule.md"
          t = open(p, encoding="utf-8").read()
          n = 'an explanation, mapping code so a person can read it'
          assert n in t, "PLANT DID NOT LAND: rt15"
          open(p, "w", encoding="utf-8").write(t.replace(n, 'an explanation, reading code', 1))
          EOF
          if python3 /tmp/rt15-copy/test/validate.py 2>&1 | grep -q 'reading is named as an exclusion again'; then
            echo "OK: validator FAILs — the defect may not return in the file that ships the rule elsewhere"
          else
            echo "ERROR: the portable rule re-excluded reading code and only SKILL.md was checked"
            exit 1
          fi
      - name: Negative self-test (the loop loses its queue)
        run: |
          set -eu
          rm -rf /tmp/lp01-copy && cp -R . /tmp/lp01-copy
          python3 - <<'EOF'
          p = "/tmp/lp01-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.schema.json"
          t = open(p, encoding="utf-8").read()
          n = '"queue": {'
          assert n in t, "PLANT DID NOT LAND: lp01"
          open(p, "w", encoding="utf-8").write(t.replace(n, '"queue_": {', 1))
          EOF
          if python3 /tmp/lp01-copy/test/validate.py 2>&1 | grep -q 'run.loop has no .queue'; then
            echo "OK: validator FAILs — a loop with no queue is a timer"
          else
            echo "ERROR: the queue field vanished and an armed loop still picks by recollection"
            exit 1
          fi
      - name: Negative self-test (the loop loses its arming point)
        run: |
          set -eu
          rm -rf /tmp/lp02-copy && cp -R . /tmp/lp02-copy
          python3 - <<'EOF'
          p = "/tmp/lp02-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.schema.json"
          t = open(p, encoding="utf-8").read()
          n = '"arm": {'
          assert n in t, "PLANT DID NOT LAND: lp02"
          open(p, "w", encoding="utf-8").write(t.replace(n, '"arm_": {', 1))
          EOF
          if python3 /tmp/lp02-copy/test/validate.py 2>&1 | grep -q 'run.loop has no .arm'; then
            echo "OK: validator FAILs — arming at preflight arms a loop with nothing to walk"
          else
            echo "ERROR: the arm field vanished and nothing objected"
            exit 1
          fi
      - name: Negative self-test (the self-pacing mode is withdrawn)
        run: |
          set -eu
          rm -rf /tmp/lp03-copy && cp -R . /tmp/lp03-copy
          python3 - <<'EOF'
          p = "/tmp/lp03-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.schema.json"
          t = open(p, encoding="utf-8").read()
          n = '"dynamic"'
          assert n in t, "PLANT DID NOT LAND: lp03"
          open(p, "w", encoding="utf-8").write(t.replace(n, '"paced"', 1))
          EOF
          if python3 /tmp/lp03-copy/test/validate.py 2>&1 | grep -q 'has no .dynamic'; then
            echo "OK: validator FAILs — a self-pacing run needs a way to record that it is one"
          else
            echo "ERROR: dynamic left the enum and nothing objected"
            exit 1
          fi
      - name: Negative self-test (the queue names a source no stage produces)
        run: |
          set -eu
          rm -rf /tmp/lp04-copy && cp -R . /tmp/lp04-copy
          python3 - <<'EOF'
          p = "/tmp/lp04-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.schema.json"
          t = open(p, encoding="utf-8").read()
          n = '"module-map"'
          assert n in t, "PLANT DID NOT LAND: lp04"
          open(p, "w", encoding="utf-8").write(t.replace(n, '"whatever-is-next"', 1))
          EOF
          if python3 /tmp/lp04-copy/test/validate.py 2>&1 | grep -q 'does not offer .module-map'; then
            echo "OK: validator FAILs — the queue must name a list this pipeline actually builds"
          else
            echo "ERROR: the queue named a source nothing produces and nothing objected"
            exit 1
          fi
      - name: Negative self-test (the authorization floor leaves the file that defines the mode)
        run: |
          set -eu
          rm -rf /tmp/lp05-copy && cp -R . /tmp/lp05-copy
          python3 - <<'EOF'
          p = "/tmp/lp05-copy/plugins/task-pipeline/skills/task-pipeline/references/continuity.md"
          t = open(p, encoding="utf-8").read()
          n = 'authorization'
          assert t.count(n) >= 3, "PLANT DID NOT LAND: lp05 — fewer occurrences than expected"
          # The guard reads _flatten()ed text, where a line-wrapped occurrence rejoins:
          # replacing the single-line copies left the wrapped one matching and the guard
          # silent. Plant on the word itself so no form of the phrase survives.
          open(p, "w", encoding="utf-8").write(t.replace(n, 'permission', -1))
          EOF
          if python3 /tmp/lp05-copy/test/validate.py 2>&1 | grep -q 'keeps arming from reading as blanket permission'; then
            echo "OK: validator FAILs — arming may never read as blanket permission"
          else
            echo "ERROR: the deploy floor's distinction vanished from continuity.md"
            exit 1
          fi
      - name: Negative self-test (a dynamic run owes no disclosure)
        run: |
          set -eu
          rm -rf /tmp/lp06-copy && cp -R . /tmp/lp06-copy
          python3 - <<'EOF'
          p = "/tmp/lp06-copy/plugins/task-pipeline/skills/task-pipeline/references/continuity.md"
          t = open(p, encoding="utf-8").read()
          n = 'prints **the delay it chose and why**'
          assert n in t, "PLANT DID NOT LAND: lp06"
          open(p, "w", encoding="utf-8").write(t.replace(n, 'runs quietly', 1))
          EOF
          if python3 /tmp/lp06-copy/test/validate.py 2>&1 | grep -q 'never says a self-pacing run prints the delay'; then
            echo "OK: validator FAILs — a run silent about its pacing looks like one that stopped"
          else
            echo "ERROR: the dynamic disclosure vanished and nothing objected"
            exit 1
          fi
      - name: Negative self-test (stage 2 keeps its gate but loses where the queue comes from)
        run: |
          set -eu
          rm -rf /tmp/lp07-copy && cp -R . /tmp/lp07-copy
          python3 - <<'EOF'
          p = "/tmp/lp07-copy/plugins/task-pipeline/skills/task-pipeline/references/stages.md"
          t = open(p, encoding="utf-8").read()
          head = "- **The queue exists here, so the loop arms here**"
          tail = "- **GATE (manual):**"
          assert head in t and tail in t, "PLANT DID NOT LAND: lp07"
          # The GATE clause is left INTACT. Rekeying the guard onto the gate silently
          # dropped this case — deleting the whole bullet passed, and this probe kept
          # firing only because it used to delete the gate clause too, which made it a
          # duplicate of nb03. The obligation and its reason are two statements of one
          # rule, and a probe that tests both tests neither.
          i = t.index(head); j = t.index(tail, i)
          open(p, "w", encoding="utf-8").write(t[:i] + t[j:])
          EOF
          if python3 /tmp/lp07-copy/test/validate.py 2>&1 | grep -q "no longer explains where the queue comes from"; then
            echo "OK: validator FAILs — the gate's obligation without its reason is half a rule"
          else
            echo "ERROR: the queue bullet vanished while the gate kept its clause, and nothing objected"
            exit 1
          fi
      - name: Negative self-test (the machine-readable stage 2 drops the arming)
        run: |
          set -eu
          rm -rf /tmp/lp08-copy && cp -R . /tmp/lp08-copy
          python3 - <<'EOF'
          p = "/tmp/lp08-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.example.json"
          t = open(p, encoding="utf-8").read()
          # Replacing only the headline left 'a loop armed earlier' in the same gate
          # string, so the guard's substring test still matched. Drop the sentence whole.
          n = " THE LOOP'S ARMING STATE IS PRINTED \u2014 armed with its queue and pacing, or not armed with the reason; the queue exists at this stage and a loop armed earlier has nothing to walk."
          assert n in t, "PLANT DID NOT LAND: lp08"
          open(p, "w", encoding="utf-8").write(t.replace(n, "", 1))
          EOF
          if python3 /tmp/lp08-copy/test/validate.py 2>&1 | grep -q "does not require the loop's arming state"; then
            echo "OK: validator FAILs — the two statements of one rule may not drift"
          else
            echo "ERROR: the example's gate lost the arming while the prose kept it"
            exit 1
          fi
      - name: Negative self-test (the whole loop contract is deleted from the schema)
        run: |
          set -eu
          rm -rf /tmp/lp09-copy && cp -R . /tmp/lp09-copy
          python3 - <<'EOF'
          import json
          p = "/tmp/lp09-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.schema.json"
          d = json.load(open(p, encoding="utf-8"))
          assert "loop" in d["definitions"]["run"]["properties"], "PLANT DID NOT LAND: lp09"
          del d["definitions"]["run"]["properties"]["loop"]
          json.dump(d, open(p, "w", encoding="utf-8"), ensure_ascii=False, indent=2)
          EOF
          if python3 /tmp/lp09-copy/test/validate.py 2>&1 | grep -q 'no run.loop block at definitions.run.properties.loop'; then
            echo "OK: validator FAILs — a deleted contract is a failure, not a tidy skip line"
          else
            echo "ERROR: the entire loop contract vanished and the suite stayed green"
            exit 1
          fi
      - name: Negative self-test (the queue enum becomes an open string)
        run: |
          set -eu
          rm -rf /tmp/lp10-copy && cp -R . /tmp/lp10-copy
          python3 - <<'EOF'
          import json
          p = "/tmp/lp10-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.schema.json"
          d = json.load(open(p, encoding="utf-8"))
          q = d["definitions"]["run"]["properties"]["loop"]["properties"]["queue"]
          assert "enum" in q, "PLANT DID NOT LAND: lp10"
          del q["enum"]; q["type"] = "string"
          json.dump(d, open(p, "w", encoding="utf-8"), ensure_ascii=False, indent=2)
          EOF
          if python3 /tmp/lp10-copy/test/validate.py 2>&1 | grep -q 'an open string is not a queue'; then
            echo "OK: validator FAILs — an open string accepts the name of a list nobody builds"
          else
            echo "ERROR: the queue accepted any string and nothing objected"
            exit 1
          fi
      - name: Negative self-test (the arming point loses the one this release moved to)
        run: |
          set -eu
          rm -rf /tmp/lp11-copy && cp -R . /tmp/lp11-copy
          python3 - <<'EOF'
          import json
          p = "/tmp/lp11-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.schema.json"
          d = json.load(open(p, encoding="utf-8"))
          a = d["definitions"]["run"]["properties"]["loop"]["properties"]["arm"]
          assert "after-decomposition" in a["enum"], "PLANT DID NOT LAND: lp11"
          a["enum"] = ["preflight"]
          json.dump(d, open(p, "w", encoding="utf-8"), ensure_ascii=False, indent=2)
          EOF
          if python3 /tmp/lp11-copy/test/validate.py 2>&1 | grep -q 'does not offer .after-decomposition'; then
            echo "OK: validator FAILs — the field cannot allow only the point the release moved away from"
          else
            echo "ERROR: arm kept only preflight and the field's existence was taken as proof"
            exit 1
          fi
      - name: Negative self-test (Part 1a stops stating its precondition)
        run: |
          set -eu
          rm -rf /tmp/lp12-copy && cp -R . /tmp/lp12-copy
          python3 - <<'EOF'
          p = "/tmp/lp12-copy/plugins/task-pipeline/skills/task-pipeline/references/continuity.md"
          t = open(p, encoding="utf-8").read()
          n = "**Where nothing is recorded, nothing arms.**"
          assert n in t, "PLANT DID NOT LAND: lp12"
          i = t.index(n); j = t.index("**What arming does NOT change", i)
          t = t[:i] + t[j:]
          t = t.replace("Where the mode\nis **recorded** and the queue", "Where the queue", 1)
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/lp12-copy/test/validate.py 2>&1 | grep -q 'arming is stated with no precondition'; then
            echo "OK: validator FAILs — unconditional arming overrides the default-off floor"
          else
            echo "ERROR: Part 1a made arming unconditional and the floor three surfaces away did not object"
            exit 1
          fi
      - name: Negative self-test (the floor leaves one section while a copy elsewhere keeps the guard green)
        run: |
          set -eu
          rm -rf /tmp/lp13-copy && cp -R . /tmp/lp13-copy
          python3 - <<'EOF'
          p = "/tmp/lp13-copy/plugins/task-pipeline/skills/task-pipeline/references/continuity.md"
          t = open(p, encoding="utf-8").read()
          # No regex: an escaped pattern in a YAML heredoc searched for a literal
          # backslash and the plant never landed. Plain indexing instead.
          head = "## Part 1a"
          i = t.index(head)
          j = t.index("\n## ", i + len(head))
          sec = t[i:j]
          n = "*a generic flag is not a specific authorization*, and arming a"
          assert n in sec, "PLANT DID NOT LAND: lp13 — Part 1a no longer states the floor"
          # Only Part 1a's copy: the file-wide test this guard replaced stayed green here,
          # answered by the bullet in "The limit" and a Rationalizations row.
          sec2 = sec.replace(n, "arming a", 1)
          open(p, "w", encoding="utf-8").write(t[:i] + sec2 + t[j:])
          EOF
          if python3 /tmp/lp13-copy/test/validate.py 2>&1 | grep -q "Part 1a.: the sentence that keeps arming"; then
            echo "OK: validator FAILs — a copy elsewhere in the file is not the copy this section's reader meets"
          else
            echo "ERROR: the floor left Part 1a and a Rationalizations row kept the guard green"
            exit 1
          fi
      - name: Negative self-test (the example reverts to its pre-v1.40 loop block)
        run: |
          set -eu
          rm -rf /tmp/lp14-copy && cp -R . /tmp/lp14-copy
          python3 - <<'EOF'
          import json
          p = "/tmp/lp14-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.example.json"
          d = json.load(open(p, encoding="utf-8"))
          assert "queue" in d["run"]["loop"], "PLANT DID NOT LAND: lp14"
          d["run"]["loop"] = {"mode": "off", "interval": "5m", "command": "/loop"}
          json.dump(d, open(p, "w", encoding="utf-8"), ensure_ascii=False, indent=2)
          EOF
          if python3 /tmp/lp14-copy/test/validate.py 2>&1 | grep -q 'run.loop has no .queue. — the schema offers it'; then
            echo "OK: validator FAILs — the example is what gets copied, so an absent field is a field nobody discovers"
          else
            echo "ERROR: the example lost both new fields and the contract passed"
            exit 1
          fi
      - name: Negative self-test (the dynamic rule is inverted while its words are kept)
        run: |
          set -eu
          rm -rf /tmp/lp15-copy && cp -R . /tmp/lp15-copy
          python3 - <<'EOF'
          p = "/tmp/lp15-copy/plugins/task-pipeline/skills/task-pipeline/references/continuity.md"
          t = open(p, encoding="utf-8").read()
          n = "prints **the delay it chose and why**"
          assert n in t, "PLANT DID NOT LAND: lp15"
          # The noun pair survives; the obligation is reversed. The first predicate
          # searched for the words and passed this.
          t = t.replace(n, "keeps the delay it chose internal, because a pacing line every turn is noise", 1)
          t = t.replace("A dynamic run that says nothing about its pacing is indistinguishable from a\nrun that quietly stopped.", "", 1)
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/lp15-copy/test/validate.py 2>&1 | grep -q 'never says a self-pacing run prints the delay'; then
            echo "OK: validator FAILs — the verb and the consequence are required, not the nouns"
          else
            echo "ERROR: the rule was inverted with its words intact and the guard passed"
            exit 1
          fi
      - name: Negative self-test (the run-stamp table loses its cap)
        run: |
          set -eu
          rm -rf /tmp/rs01-copy && cp -R . /tmp/rs01-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/rs01-copy/plugins/task-pipeline/skills/task-pipeline/references/retrospective.md"
          t = open(p, encoding="utf-8").read()
          # DERIVED: find the row the guard finds, then remove the cap from the segment
          # that names the stamps. The first version held the cell verbatim and would
          # have died red on a cosmetic reflow.
          out, done = [], False
          for l in t.splitlines(True):
              if not done and l.startswith("|") and "retro.md" in l.lower() and "run stamps" in l.lower():
                  i = l.lower().index("run stamps")
                  j = l.find("|", i)
                  seg = l[i:j if j > 0 else len(l)]
                  seg2 = re.sub(r"\(max[^)]*\)", "(one line each)", seg, count=1)
                  assert seg2 != seg, "PLANT DID NOT LAND: rs01 — the stamps segment states no cap"
                  l = l[:i] + seg2 + (l[j:] if j > 0 else "")
                  done = True
              out.append(l)
          assert done, "PLANT DID NOT LAND: rs01 — no row naming retro.md's run stamps"
          open(p, "w", encoding="utf-8").write("".join(out))
          EOF
          if python3 /tmp/rs01-copy/test/validate.py 2>&1 | grep -q "run stamps are given no cap"; then
            echo "OK: validator FAILs — one line per run is a slope, not a bound"
          else
            echo "ERROR: the stamp table lost its cap and the section read in full kept growing"
            exit 1
          fi
      - name: Negative self-test (the run-stamp table grows past its cap)
        run: |
          set -eu
          rm -rf /tmp/rs02-copy && cp -R . /tmp/rs02-copy
          python3 - <<'EOF'
          import re
          v = open("/tmp/rs02-copy/test/validate.py", encoding="utf-8").read()
          cap = int(re.search(r"^STAMP_CAP = (\d+)", v, re.M).group(1))
          p = "/tmp/rs02-copy/docs/superpowers/retro.md"
          t = open(p, encoding="utf-8").read()
          rows = re.findall(r"^[ \t]{0,3}\|\s*\d{4}-\d{2}-\d{2}\s*\|[^\n]*\n", t, re.M)
          assert len(rows) == cap, "PLANT DID NOT LAND: rs02 — %d stamps, cap is %d" % (len(rows), cap)
          # One more, in the shape the doctrine's own command appends — prose, not a row.
          # A guard that counts table rows under one heading cannot see this at all.
          i = t.index(rows[0])
          open(p, "w", encoding="utf-8").write(t[:i] + "2026-08-11 · `deadbee`\n" + t[i:])
          EOF
          if python3 /tmp/rs02-copy/test/validate.py 2>&1 | grep -q "run stamps in a file read"; then
            echo "OK: validator FAILs — the eleventh stamp must rotate, in every shape the doctrine writes them"
          else
            echo "ERROR: the file passed its cap inside a section read in full at stage 0"
            exit 1
          fi
      - name: Negative self-test (neighbour probe — the stamps lose their cap while the cell keeps another)
        run: |
          set -eu
          rm -rf /tmp/nb01-copy && cp -R . /tmp/nb01-copy
          python3 - <<'EOF'
          p = "/tmp/nb01-copy/plugins/task-pipeline/skills/task-pipeline/references/retrospective.md"
          t = open(p, encoding="utf-8").read()
          n = "**Standing instructions** (max **10**) · **Run stamps** (max **10**, oldest rotate out)"
          assert n in t, "PLANT DID NOT LAND: nb01"
          # Break the subject AND leave the neighbour's cap in the same cell — on the
          # other side of the separator, which is where the narrowed version fell.
          open(p, "w", encoding="utf-8").write(
              t.replace(n, "**Run stamps** (one line each) \u00b7 **Standing instructions** (max **10**)", 1))
          EOF
          if python3 /tmp/nb01-copy/test/validate.py 2>&1 | grep -q 'run stamps are given no cap'; then
            echo "OK: validator FAILs — the neighbour's cap does not answer for the stamps, on either side of the separator"
          else
            echo "ERROR: the stamps lost their cap and the standing instructions' own cap answered for them"
            exit 1
          fi
      - name: Negative self-test (neighbour probe — Part 1a loses the floor while the file keeps it)
        run: |
          set -eu
          rm -rf /tmp/nb02-copy && cp -R . /tmp/nb02-copy
          python3 - <<'EOF'
          p = "/tmp/nb02-copy/plugins/task-pipeline/skills/task-pipeline/references/continuity.md"
          t = open(p, encoding="utf-8").read()
          # This probe DELETES from Part 1a and relies on the copies next door. Assert
          # they exist, or a later edit silently demotes it to a delete-only probe.
          assert " ".join(t.split()).count("generic flag is not a specific authorization") >= 3, \
              "PLANT DID NOT LAND: nb02 — fewer neighbour copies than this probe assumes"
          head = "## Part 1a"
          i = t.index(head); j = t.index("\n## ", i + len(head))
          sec = t[i:j]
          n = "*a generic flag is not a specific authorization*, and arming a"
          assert n in sec, "PLANT DID NOT LAND: nb02"
          # Break the subject; the phrase survives twice elsewhere in the file — in the
          # section written twenty-nine releases earlier and in a Rationalizations row,
          # which is exactly what kept the first version of this guard green.
          open(p, "w", encoding="utf-8").write(t[:i] + sec.replace(n, "arming a", 1) + t[j:])
          EOF
          if python3 /tmp/nb02-copy/test/validate.py 2>&1 | grep -q 'Part 1a.: the sentence that keeps arming'; then
            echo "OK: validator FAILs — a copy elsewhere in the file is not the copy this section's reader meets"
          else
            echo "ERROR: Part 1a lost the floor and two copies elsewhere kept the guard green"
            exit 1
          fi
      - name: Negative self-test (neighbour probe — stage 2 loses the arming while keeping the word)
        run: |
          set -eu
          rm -rf /tmp/nb03-copy && cp -R . /tmp/nb03-copy
          python3 - <<'EOF'
          p = "/tmp/nb03-copy/plugins/task-pipeline/skills/task-pipeline/references/stages.md"
          t = open(p, encoding="utf-8").read()
          clause = " \u2014 **and the loop's arming state is printed**:\n  armed with its queue and pacing, or not armed with the reason"
          assert clause in t, "PLANT DID NOT LAND: nb03 gate clause"
          t = t.replace(clause, "", 1)
          # Plant the needle the predicate matches TODAY, next door. The first version
          # planted the needles of two RETIRED predicates, which proved the old guards
          # were neighbour-answerable and said nothing about this one — the doctrine's
          # own step 2 failing on its first use.
          needle = "A note for readers: the loop's arming state is printed by the harness, not by this stage."
          t = t.replace("\n## 3 \u2014 Spec", "\n" + needle + "\n\n## 3 \u2014 Spec", 1)
          assert needle in t, "PLANT DID NOT LAND: nb03 neighbour"
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/nb03-copy/test/validate.py 2>&1 | grep -q "the GATE does not require the loop's arming state"; then
            echo "OK: validator FAILs — a trailing note is not the gate's obligation"
          else
            echo "ERROR: the gate lost its clause and a note after the section answered for it"
            exit 1
          fi
      - name: Negative self-test (the hand-back loses the section a returning reader needs most)
        run: |
          set -eu
          rm -rf /tmp/hb01-copy && cp -R . /tmp/hb01-copy
          python3 - <<'EOF'
          p = "/tmp/hb01-copy/plugins/task-pipeline/skills/task-pipeline/references/progress.md"
          t = open(p, encoding="utf-8").read()
          n = "SURFACED    what came up that nobody asked for"
          assert n in t, "PLANT DID NOT LAND: hb01"
          open(p, "w", encoding="utf-8").write(t.replace(n, "EXTRAS      anything else worth a line", 1))
          EOF
          if python3 /tmp/hb01-copy/test/validate.py 2>&1 | grep -q 'the .SURFACED. section is gone'; then
            echo "OK: validator FAILs — what a run learned by accident is recoverable from no artefact"
          else
            echo "ERROR: the hand-back lost SURFACED and nothing objected"
            exit 1
          fi
      - name: Negative self-test (waiting decisions become a list instead of a question)
        run: |
          set -eu
          rm -rf /tmp/hb02-copy && cp -R . /tmp/hb02-copy
          python3 - <<'EOF'
          p = "/tmp/hb02-copy/plugins/task-pipeline/skills/task-pipeline/references/progress.md"
          t = open(p, encoding="utf-8").read()
          n = "**DECISIONS WAITING are asked, not listed.**"
          assert n in t, "PLANT DID NOT LAND: hb02 paragraph"
          tpl = "DECISIONS WAITING  <n>   each as a question with options, asked HERE"
          assert tpl in t, "PLANT DID NOT LAND: hb02 template line"
          # The rule is stated twice — in the template and in the paragraph that owns it.
          # Removing one leaves it stated, and the guard was right not to fire; this
          # probe's first version tested the paragraph alone and called that a defect.
          t = t.replace(n, "**DECISIONS WAITING are recorded for later.**", 1)
          t = t.replace(tpl, "DECISIONS WAITING  <n>   each with options, for the record", 1)
          t = t.replace("Ask it at the boundary, with options and a\nrecommendation, in the same breath as the report.",
                        "Record it with options and a recommendation.", 1)
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/hb02-copy/test/validate.py 2>&1 | grep -q 'DECISIONS WAITING line does not say the question is ASKED'; then
            echo "OK: validator FAILs — a question parked in a report is answered days later, if at all"
          else
            echo "ERROR: decisions became a list and the boundary stopped asking"
            exit 1
          fi
      - name: Negative self-test (the ambiguity list stops being computed)
        run: |
          set -eu
          rm -rf /tmp/hb03-copy && cp -R . /tmp/hb03-copy
          python3 - <<'EOF'
          p = "/tmp/hb03-copy/plugins/task-pipeline/skills/task-pipeline/references/progress.md"
          t = open(p, encoding="utf-8").read()
          i = t.index("### AMBIGUITIES are computed")
          j = t.index("\n## ", i)
          assert "none found" in t[i:j], "PLANT DID NOT LAND: hb03"
          # The shape it degrades into on its own: judgement with no register behind it.
          open(p, "w", encoding="utf-8").write(
              t[:i] + "### AMBIGUITIES\n\nList anything that still looks unclear to you.\n" + t[j:])
          EOF
          if python3 /tmp/hb03-copy/test/validate.py 2>&1 | grep -q 'no longer names these computed sources'; then
            echo "OK: validator FAILs — an unbounded 'is anything unclear?' is a ritual sentence within three runs"
          else
            echo "ERROR: the ambiguity list became judgement and nothing objected"
            exit 1
          fi
      - name: Negative self-test (stage 10's gate stops requiring the hand-back)
        run: |
          set -eu
          rm -rf /tmp/hb04-copy && cp -R . /tmp/hb04-copy
          python3 - <<'EOF'
          p = "/tmp/hb04-copy/plugins/task-pipeline/skills/task-pipeline/references/stages.md"
          t = open(p, encoding="utf-8").read()
          n = "**the hand-back is written**"
          assert n in t, "PLANT DID NOT LAND: hb04"
          i = t.index(n)
          j = t.index("a run\n  that cannot say what happened has not finished, it has stopped;", i)
          k = j + len("a run\n  that cannot say what happened has not finished, it has stopped;")
          open(p, "w", encoding="utf-8").write(t[:i] + t[k:])
          EOF
          if python3 /tmp/hb04-copy/test/validate.py 2>&1 | grep -q 'the GATE does not require the hand-back'; then
            echo "OK: validator FAILs — an instruction to report with no gate behind it is 'copy it, tick it'"
          else
            echo "ERROR: the gate dropped the hand-back and the doctrine kept it"
            exit 1
          fi
      - name: Negative self-test (neighbour probe — the gate loses the hand-back while the section keeps the word)
        run: |
          set -eu
          rm -rf /tmp/hb05-copy && cp -R . /tmp/hb05-copy
          python3 - <<'EOF'
          p = "/tmp/hb05-copy/plugins/task-pipeline/skills/task-pipeline/references/stages.md"
          t = open(p, encoding="utf-8").read()
          n = "**the hand-back is written**"
          i = t.index(n)
          j = t.index("a run\n  that cannot say what happened has not finished, it has stopped;", i)
          k = j + len("a run\n  that cannot say what happened has not finished, it has stopped;")
          t = t[:i] + t[k:]
          # Plant the CURRENT needle next door: same stage, not the gate bullet.
          needle = "- **The hand-back** is described in progress.md and printed by the harness."
          t = t.replace("- **GATE (manual):** the ladder walk ran", needle + "\n- **GATE (manual):** the ladder walk ran", 1)
          assert needle in t, "PLANT DID NOT LAND: hb05 neighbour"
          open(p, "w", encoding="utf-8").write(t)
          EOF
          if python3 /tmp/hb05-copy/test/validate.py 2>&1 | grep -q 'the GATE does not require the hand-back'; then
            echo "OK: validator FAILs — a sibling bullet naming the hand-back is not the gate requiring it"
          else
            echo "ERROR: a neighbouring bullet answered for the gate"
            exit 1
          fi
      - name: Negative self-test (the example's acceptance gate drops the hand-back)
        run: |
          set -eu
          rm -rf /tmp/hb06-copy && cp -R . /tmp/hb06-copy
          python3 - <<'EOF'
          import json
          p = "/tmp/hb06-copy/plugins/task-pipeline/skills/task-pipeline/pipeline.example.json"
          d = json.load(open(p, encoding="utf-8"))
          s = [x for x in d["stages"] if x.get("state") == "acceptance"][0]
          c = s["gate"]["check"]
          i = c.upper().index("THE HAND-BACK IS WRITTEN")
          assert i > 0, "PLANT DID NOT LAND: hb06"
          s["gate"]["check"] = c[:i].rstrip(". ") + "."
          json.dump(d, open(p, "w", encoding="utf-8"), ensure_ascii=False, indent=2)
          EOF
          if python3 /tmp/hb06-copy/test/validate.py 2>&1 | grep -q 'its gate does not require the hand-back'; then
            echo "OK: validator FAILs — the two surfaces that state one rule may not drift"
          else
            echo "ERROR: the machine-readable gate lost the hand-back while the prose kept it"
            exit 1
          fi
      - name: Negative self-test (the hand-back loses its trace in the ledger)
        run: |
          set -eu
          rm -rf /tmp/hb07-copy && cp -R . /tmp/hb07-copy
          python3 - <<'EOF'
          p = "/tmp/hb07-copy/plugins/task-pipeline/skills/task-pipeline/templates/run.md"
          t = open(p, encoding="utf-8").read()
          n = "hand:  <N|10>"
          assert n in t, "PLANT DID NOT LAND: hb07"
          i = t.index(n); j = t.index("\n", i)
          open(p, "w", encoding="utf-8").write(t[:i] + t[j+1:])
          EOF
          if python3 /tmp/hb07-copy/test/validate.py 2>&1 | grep -q 'no .hand:. line shape'; then
            echo "OK: validator FAILs — a gate criterion with no trace is 'copy it, tick it' one level up"
          else
            echo "ERROR: the ledger lost the hand: shape and the gate kept requiring the report"
            exit 1
          fi
      - name: Negative self-test (the doctrine stops naming where its trace lands)
        run: |
          set -eu
          rm -rf /tmp/hb08-copy && cp -R . /tmp/hb08-copy
          python3 - <<'EOF'
          p = "/tmp/hb08-copy/plugins/task-pipeline/skills/task-pipeline/references/progress.md"
          t = open(p, encoding="utf-8").read()
          n = "as a `hand:` line"
          assert n in t, "PLANT DID NOT LAND: hb08"
          # Every mention, not the first: the section names the shape twice, and removing
          # one left the guard reading the other.
          open(p, "w", encoding="utf-8").write(t.replace(n, "somewhere durable", -1).replace("`hand:`", "the trace", -1))
          EOF
          if python3 /tmp/hb08-copy/test/validate.py 2>&1 | grep -q 'never names the ledger line its trace lands on'; then
            echo "OK: validator FAILs — a narrative with no address is one no audit can find"
          else
            echo "ERROR: the doctrine lost the address and nothing objected"
            exit 1
          fi
      - name: Negative self-test (the ratchet-matcher section is deleted)
        run: |
          set -eu
          rm -rf /tmp/ri01-copy && cp -R . /tmp/ri01-copy
          python3 - <<'EOF'
          p = "/tmp/ri01-copy/plugins/task-pipeline/skills/task-pipeline/references/gates.md"
          t = open(p, encoding="utf-8").read()
          n = "## A ratchet's matcher is itself a check"
          assert n in t, "PLANT DID NOT LAND: ri01"
          _planted = t.replace(n, '## Matchers', 1)
          assert _planted != t, "PLANT DID NOT LAND: ri01"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/ri01-copy/test/validate.py 2>&1 | grep -q "no section on a ratchet's matcher"; then
            echo "OK: validator FAILs — a matcher looser than its subject shrinks the ratchet"
          else
            echo "ERROR: the rule went missing and nothing objected — a matcher looser than its subject shrinks the ratchet"
            exit 1
          fi
      - name: Negative self-test (the near-miss leaves the ratchet-matcher rule)
        run: |
          set -eu
          rm -rf /tmp/ri02-copy && cp -R . /tmp/ri02-copy
          python3 - <<'EOF'
          p = "/tmp/ri02-copy/plugins/task-pipeline/skills/task-pipeline/references/gates.md"
          t = open(p, encoding="utf-8").read()
          n = 'feed its matcher a near-miss it must reject'
          assert n in t, "PLANT DID NOT LAND: ri02"
          _planted = t.replace(n, 'check that the matcher is sensible', 1)
          assert _planted != t, "PLANT DID NOT LAND: ri02"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/ri02-copy/test/validate.py 2>&1 | grep -q 'the near-miss is gone'; then
            echo "OK: validator FAILs — reacting is not discriminating"
          else
            echo "ERROR: the rule went missing and nothing objected — reacting is not discriminating"
            exit 1
          fi
      - name: Negative self-test (the unlanded-plant section is deleted)
        run: |
          set -eu
          rm -rf /tmp/ri03-copy && cp -R . /tmp/ri03-copy
          python3 - <<'EOF'
          p = "/tmp/ri03-copy/plugins/task-pipeline/skills/task-pipeline/references/gates.md"
          t = open(p, encoding="utf-8").read()
          n = '## A green probe is evidence only if'
          assert n in t, "PLANT DID NOT LAND: ri03"
          _planted = t.replace(n, '## Probes and greens', 1)
          assert _planted != t, "PLANT DID NOT LAND: ri03"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/ri03-copy/test/validate.py 2>&1 | grep -q 'no section on a green probe'; then
            echo "OK: validator FAILs — a plant that missed reports the same green as a sound check"
          else
            echo "ERROR: the rule went missing and nothing objected — a plant that missed reports the same green as a sound check"
            exit 1
          fi
      - name: Negative self-test (the verified-by section is deleted)
        run: |
          set -eu
          rm -rf /tmp/ri04-copy && cp -R . /tmp/ri04-copy
          python3 - <<'EOF'
          p = "/tmp/ri04-copy/plugins/task-pipeline/skills/task-pipeline/references/acceptance.md"
          t = open(p, encoding="utf-8").read()
          n = '## A `verified by` name is a claim until it resolves'
          assert n in t, "PLANT DID NOT LAND: ri04"
          _planted = t.replace(n, '## Naming checks', 1)
          assert _planted != t, "PLANT DID NOT LAND: ri04"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/ri04-copy/test/validate.py 2>&1 | grep -q 'no section requiring a `verified by` name'; then
            echo "OK: validator FAILs — a cell can name a check that does not exist"
          else
            echo "ERROR: the rule went missing and nothing objected — a cell can name a check that does not exist"
            exit 1
          fi
      - name: Negative self-test (an unresolvable verified-by name keeps reading verified)
        run: |
          set -eu
          rm -rf /tmp/ri05-copy && cp -R . /tmp/ri05-copy
          python3 - <<'EOF'
          p = "/tmp/ri05-copy/plugins/task-pipeline/skills/task-pipeline/references/acceptance.md"
          t = open(p, encoding="utf-8").read()
          n = 'status **`unknown`**, never `verified`'
          assert n in t, "PLANT DID NOT LAND: ri05"
          _planted = t.replace(n, 'status noted in the row', 1)
          assert _planted != t, "PLANT DID NOT LAND: ri05"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/ri05-copy/test/validate.py 2>&1 | grep -q 'the status an unresolvable'; then
            echo "OK: validator FAILs — without `unknown` the row keeps reading verified"
          else
            echo "ERROR: the rule went missing and nothing objected — without `unknown` the row keeps reading verified"
            exit 1
          fi
      - name: Negative self-test (the seam section is deleted)
        run: |
          set -eu
          rm -rf /tmp/ri06-copy && cp -R . /tmp/ri06-copy
          python3 - <<'EOF'
          p = "/tmp/ri06-copy/plugins/task-pipeline/skills/task-pipeline/references/acceptance.md"
          t = open(p, encoding="utf-8").read()
          n = '## A seam is not a deliverable'
          assert n in t, "PLANT DID NOT LAND: ri06"
          _planted = t.replace(n, '## Boundaries', 1)
          assert _planted != t, "PLANT DID NOT LAND: ri06"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/ri06-copy/test/validate.py 2>&1 | grep -q 'no section on the seam'; then
            echo "OK: validator FAILs — two green halves reporting a working whole"
          else
            echo "ERROR: the rule went missing and nothing objected — two green halves reporting a working whole"
            exit 1
          fi
      - name: Negative self-test (the seam loses its unlooked fallback)
        run: |
          set -eu
          rm -rf /tmp/ri07-copy && cp -R . /tmp/ri07-copy
          python3 - <<'EOF'
          p = "/tmp/ri07-copy/plugins/task-pipeline/skills/task-pipeline/references/acceptance.md"
          t = open(p, encoding="utf-8").read()
          n = 'the table says so under `unlooked`'
          assert n in t, "PLANT DID NOT LAND: ri07"
          _planted = t.replace(n, 'the table notes it', 1)
          assert _planted != t, "PLANT DID NOT LAND: ri07"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/ri07-copy/test/validate.py 2>&1 | grep -q 'the `unlooked` fallback'; then
            echo "OK: validator FAILs — where no check spans the seam, two green rows are a false whole"
          else
            echo "ERROR: the rule went missing and nothing objected — where no check spans the seam, two green rows are a false whole"
            exit 1
          fi
      - name: Negative self-test (the consumption section is deleted)
        run: |
          set -eu
          rm -rf /tmp/ri08-copy && cp -R . /tmp/ri08-copy
          python3 - <<'EOF'
          p = "/tmp/ri08-copy/plugins/task-pipeline/skills/task-pipeline/references/tdd.md"
          t = open(p, encoding="utf-8").read()
          n = '## What a case consumes'
          assert n in t, "PLANT DID NOT LAND: ri08"
          _planted = t.replace(n, '## Suite hygiene', 1)
          assert _planted != t, "PLANT DID NOT LAND: ri08"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/ri08-copy/test/validate.py 2>&1 | grep -q 'no section on what a case consumes'; then
            echo "OK: validator FAILs — a suite that exhausts a production limit is measuring the limit"
          else
            echo "ERROR: the rule went missing and nothing objected — a suite that exhausts a production limit is measuring the limit"
            exit 1
          fi
      - name: Negative self-test (a timeout becomes a slow result again)
        run: |
          set -eu
          rm -rf /tmp/ri09-copy && cp -R . /tmp/ri09-copy
          python3 - <<'EOF'
          p = "/tmp/ri09-copy/plugins/task-pipeline/skills/task-pipeline/references/tdd.md"
          t = open(p, encoding="utf-8").read()
          n = 'is an unclassified result, not a slow one'
          assert n in t, "PLANT DID NOT LAND: ri09"
          _planted = t.replace(n, 'is a slow result', 1)
          assert _planted != t, "PLANT DID NOT LAND: ri09"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/ri09-copy/test/validate.py 2>&1 | grep -q 'UNCLASSIFIED result'; then
            echo "OK: validator FAILs — read as slow, it sends a run to investigate the wrong thing"
          else
            echo "ERROR: the rule went missing and nothing objected — read as slow, it sends a run to investigate the wrong thing"
            exit 1
          fi
      - name: Negative self-test (the publish disclosure section is deleted)
        run: |
          set -eu
          rm -rf /tmp/ri10-copy && cp -R . /tmp/ri10-copy
          python3 - <<'EOF'
          p = "/tmp/ri10-copy/plugins/task-pipeline/skills/task-pipeline/references/retrospective.md"
          t = open(p, encoding="utf-8").read()
          n = '## `publish:` is a line in the verdict'
          assert n in t, "PLANT DID NOT LAND: ri10"
          _planted = t.replace(n, '## Publishing notes', 1)
          assert _planted != t, "PLANT DID NOT LAND: ri10"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/ri10-copy/test/validate.py 2>&1 | grep -q 'no section giving publication'; then
            echo "OK: validator FAILs — an unarmed mechanism and one with nothing to say are indistinguishable"
          else
            echo "ERROR: the rule went missing and nothing objected — an unarmed mechanism and one with nothing to say are indistinguishable"
            exit 1
          fi
      - name: Negative self-test (the publish line loses its not-configured form)
        run: |
          set -eu
          rm -rf /tmp/ri11-copy && cp -R . /tmp/ri11-copy
          python3 - <<'EOF'
          p = "/tmp/ri11-copy/plugins/task-pipeline/skills/task-pipeline/references/retrospective.md"
          t = open(p, encoding="utf-8").read()
          n = 'not configured (N insight-grade entries stayed local)'
          assert n in t, "PLANT DID NOT LAND: ri11"
          _planted = t.replace(n, 'nothing to report', 1)
          assert _planted != t, "PLANT DID NOT LAND: ri11"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/ri11-copy/test/validate.py 2>&1 | grep -q 'the `not configured` form'; then
            echo "OK: validator FAILs — a blank where configuration is absent is the silence this ends"
          else
            echo "ERROR: the rule went missing and nothing objected — a blank where configuration is absent is the silence this ends"
            exit 1
          fi
      - name: Negative self-test (stage 10's verdict drops the publish line)
        run: |
          set -eu
          rm -rf /tmp/ri12-copy && cp -R . /tmp/ri12-copy
          python3 - <<'EOF'
          p = "/tmp/ri12-copy/plugins/task-pipeline/skills/task-pipeline/references/stages.md"
          t = open(p, encoding="utf-8").read()
          n = 'Either way the verdict carries a `publish:`'
          assert n in t, "PLANT DID NOT LAND: ri12"
          _planted = t.replace(n, 'Publication is optional and', 1)
          assert _planted != t, "PLANT DID NOT LAND: ri12"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/ri12-copy/test/validate.py 2>&1 | grep -q 'carries no `publish:` line'; then
            echo "OK: validator FAILs — disclosed at the only moment anyone is reading"
          else
            echo "ERROR: the rule went missing and nothing objected — disclosed at the only moment anyone is reading"
            exit 1
          fi
      - name: Negative self-test (a mutating probe ships with no plant-landed assertion)
        run: |
          set -eu
          rm -rf /tmp/ri13-copy && cp -R . /tmp/ri13-copy
          python3 - <<'EOF'
          p = "/tmp/ri13-copy/.github/workflows/validate.yml"
          t = open(p, encoding="utf-8").read()
          # A probe that reads a file and writes it back, with the phrase only in a
          # COMMENT — all three of these shapes were watched passing a first version of
          # the guard: the phrase in a comment, in a bash echo, and asserted about a
          # different needle.
          step = (
              "      - name: Negative self-test (planted, unasserted)\n"
              "        run: |\n"
              "          set -eu\n"
              "          rm -rf /tmp/zz9-copy && cp -R . /tmp/zz9-copy\n"
              "          python3 - <<'XEOF'\n"
              "          p = \"/tmp/zz9-copy/README.md\"\n"
              "          t = open(p, encoding=\"utf-8\").read()\n"
              "          # the plant would not land if the needle moved; checked by eye\n"
              "          open(p, \"w\", encoding=\"utf-8\").write(t.replace(\"NEEDLE\", \"x\"))\n"
              "          XEOF\n"
              "          echo ok\n")
          _planted = t.rstrip("\n") + "\n" + step
          assert _planted != t, "PLANT DID NOT LAND: ri13"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/ri13-copy/test/validate.py 2>&1 | grep -q "never assert the plant landed"; then
            echo "OK: validator FAILs — the phrase in a comment is not an assertion"
          else
            echo "ERROR: a mutating probe shipped unasserted and the guard that exists for it stayed quiet"
            exit 1
          fi
      - name: Negative self-test (the green-probe section keeps its heading and loses its rule)
        run: |
          set -eu
          rm -rf /tmp/ri03b-copy && cp -R . /tmp/ri03b-copy
          python3 - <<'EOF'
          p = "/tmp/ri03b-copy/plugins/task-pipeline/skills/task-pipeline/references/gates.md"
          t = open(p, encoding="utf-8").read()
          n = "asserts its plant landed"
          assert n in t, "PLANT DID NOT LAND: ri03b"
          # The heading stays. Only the obligation goes — the branch ri03 cannot reach,
          # because it renames the heading and stops at the first fail().
          _planted = t.replace(n, "is worth writing carefully", 1)
          assert _planted != t, "PLANT DID NOT LAND: ri03b"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/ri03b-copy/test/validate.py 2>&1 | grep -q "the obligation is gone"; then
            echo "OK: validator FAILs — a section emptied to a bare heading keeps none of its rule"
          else
            echo "ERROR: the heading survived, the rule did not, and nothing objected"
            exit 1
          fi
      - name: Negative self-test (the teardown keeps its first half and loses the one that protects other agents)
        run: |
          set -eu
          rm -rf /tmp/res1-copy && cp -R . /tmp/res1-copy
          python3 - <<'EOF'
          p = "/tmp/res1-copy/plugins/task-pipeline/skills/task-pipeline/references/residue.md"
          t = open(p, encoding="utf-8").read()
          n = '**Tear down what this run started. Report what it did not.**'
          assert n in t, "PLANT DID NOT LAND: res1"
          _planted = t.replace(n, '**Tear down what this run started.**', 1)
          assert _planted != t, "PLANT DID NOT LAND: res1"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/res1-copy/test/validate.py 2>&1 | grep -q 'the half that kills another agent'; then
            echo "OK: validator FAILs — 'end what you started' without 'report what you did not' is rejected"
          else
            echo "ERROR: the asymmetry vanished and nothing objected — a run may now end another session's work"
            exit 1
          fi
      - name: Negative self-test (the holds disclosure is given a direction)
        run: |
          set -eu
          rm -rf /tmp/res2-copy && cp -R . /tmp/res2-copy
          python3 - <<'EOF'
          p = "/tmp/res2-copy/plugins/task-pipeline/skills/task-pipeline/references/residue.md"
          t = open(p, encoding="utf-8").read()
          n = 'is never a target'
          assert n in t, "PLANT DID NOT LAND: res2"
          _planted = t.replace(n, 'is the goal', 1)
          assert _planted != t, "PLANT DID NOT LAND: res2"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/res2-copy/test/validate.py 2>&1 | grep -q 'no longer declared a non-target'; then
            echo "OK: validator FAILs — a disclosure that acquires a direction is rejected"
          else
            echo "ERROR: holds: became a target and nothing objected"
            exit 1
          fi
      - name: Negative self-test (stage 10's criterion 13 loses the report-do-not-end half)
        run: |
          set -eu
          rm -rf /tmp/res3-copy && cp -R . /tmp/res3-copy
          python3 - <<'EOF'
          p = "/tmp/res3-copy/plugins/task-pipeline/skills/task-pipeline/references/acceptance.md"
          t = open(p, encoding="utf-8").read()
          n = '**reported, never ended**'
          assert n in t, "PLANT DID NOT LAND: res3"
          _planted = t.replace(n, 'cleaned up as well', 1)
          assert _planted != t, "PLANT DID NOT LAND: res3"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/res3-copy/test/validate.py 2>&1 | grep -q 'lost one of its halves'; then
            echo "OK: validator FAILs — criterion 13 must carry both halves"
          else
            echo "ERROR: the give-back criterion kept one half and nothing objected"
            exit 1
          fi
      - name: Negative self-test (NEIGHBOUR PROBE — criterion 13 is broken and its needle planted just outside the item)
        run: |
          set -eu
          rm -rf /tmp/res4-copy && cp -R . /tmp/res4-copy
          python3 - <<'EOF'
          p = "/tmp/res4-copy/plugins/task-pipeline/skills/task-pipeline/references/acceptance.md"
          t = open(p, encoding="utf-8").read()
          n = '**reported, never ended**'
          assert n in t, "PLANT DID NOT LAND: res4"
          _planted = t.replace(n, 'cleaned up as well', 1)
          _planted = _planted.replace('## When the answer is "something\'s missing"',
              "<!-- eight classes enumerated \u00b7 reported, never ended -->\n\n"
              '## When the answer is "something\'s missing"', 1)
          assert _planted != t, "PLANT DID NOT LAND: res4"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/res4-copy/test/validate.py 2>&1 | grep -q 'lost one of its halves'; then
            echo "OK: validator FAILs — the guard reads the item, not the region before the next heading"
          else
            echo "ERROR: the guard was answered by text parked outside criterion 13 — positional narrowing is not scoping"
            exit 1
          fi
      - name: Negative self-test (INVERSION — criterion 13 keeps both needles and gains an exception clause)
        run: |
          set -eu
          rm -rf /tmp/res5-copy && cp -R . /tmp/res5-copy
          python3 - <<'EOF'
          p = "/tmp/res5-copy/plugins/task-pipeline/skills/task-pipeline/references/acceptance.md"
          t = open(p, encoding="utf-8").read()
          n = 'does not own** is **reported, never ended** — the inventory is machine-wide and\n   the authority is not.'
          assert n in t, "PLANT DID NOT LAND: res5"
          _planted = t.replace(n, 'does not own** is **reported, never ended** — unless it looks stale, in which\n   case end it and note that you did.', 1)
          assert _planted != t, "PLANT DID NOT LAND: res5"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/res5-copy/test/validate.py 2>&1 | grep -q 'admits none'; then
            echo "OK: validator FAILs — an exception marker inside a rule that admits none is the defect"
          else
            echo "ERROR: the rule was inverted beside its own needle and every keyword check stayed green"
            exit 1
          fi
      - name: Negative self-test (INVERSION — the foreign lease loses its absolute form)
        run: |
          set -eu
          rm -rf /tmp/res6-copy && cp -R . /tmp/res6-copy
          python3 - <<'EOF'
          p = "/tmp/res6-copy/plugins/task-pipeline/skills/task-pipeline/references/residue.md"
          t = open(p, encoding="utf-8").read()
          n = '- **A lease held by another agent is never released by this run**'
          assert n in t, "PLANT DID NOT LAND: res6"
          _planted = t.replace(n, '- **A lease held by another agent may be released once it looks stale**', 1)
          assert _planted != t, "PLANT DID NOT LAND: res6"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/res6-copy/test/validate.py 2>&1 | grep -q 'lost its absolute form'; then
            echo "OK: validator FAILs — 'never released' is guarded as a sentence, not as a topic"
          else
            echo "ERROR: another agent's lease became releasable and the topic-level check saw no change"
            exit 1
          fi
      - name: Negative self-test (INVERSION — the holds count acquires a direction while 'never a target' still stands)
        run: |
          set -eu
          rm -rf /tmp/res7-copy && cp -R . /tmp/res7-copy
          python3 - <<'EOF'
          p = "/tmp/res7-copy/plugins/task-pipeline/skills/task-pipeline/references/residue.md"
          t = open(p, encoding="utf-8").read()
          n = '**Only stage 10 requires the count to reach zero**'
          assert n in t, "PLANT DID NOT LAND: res7"
          _planted = t.replace(n, '**Every stage should drive the count toward zero**, and stage 10 requires it', 1)
          assert _planted != t, "PLANT DID NOT LAND: res7"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/res7-copy/test/validate.py 2>&1 | grep -q 'acquired a direction'; then
            echo "OK: validator FAILs — a disclosure given a direction is rejected even with its non-target line intact"
          else
            echo "ERROR: holds: got a direction three paragraphs below the sentence forbidding one"
            exit 1
          fi
      - name: Negative self-test (the workflow itself stops being valid YAML)
        run: |
          set -eu
          rm -rf /tmp/res8-copy && cp -R . /tmp/res8-copy
          python3 - <<'EOF'
          p = "/tmp/res8-copy/.github/workflows/validate.yml"
          t = open(p, encoding="utf-8").read()
          n = "- name: Negative self-test (the holds disclosure is given a direction)"
          assert n in t, "PLANT DID NOT LAND: res8"
          # A colon-space inside an unquoted step name. Every count in the validator
          # reads this file with a regex and cannot tell; only a parse can.
          _planted = t.replace(n, "- name: Negative self-test (the holds: disclosure is given a direction)", 1)
          assert _planted != t, "PLANT DID NOT LAND: res8"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          _out=$(python3 /tmp/res8-copy/test/validate.py 2>&1 || true)
          if printf '%s' "$_out" | grep -q "not valid YAML"; then
            echo "OK: validator FAILs — the workflow is parsed, not only pattern-matched"
          elif printf '%s' "$_out" | grep -q "PyYAML not installed"; then
            echo "SKIP: PyYAML is absent, so the parse guard could not run — it said so"
            echo "      rather than passing quietly, which is the behaviour under test here"
          else
            echo "ERROR: the workflow stopped being loadable, the parse guard neither fired"
            echo "       nor declared itself unable to look, and every regex count stayed happy"
            exit 1
          fi
      - name: Negative self-test (the third owner state loses its proof requirement)
        run: |
          set -eu
          rm -rf /tmp/res9-copy && cp -R . /tmp/res9-copy
          python3 - <<'EOF'
          p = "/tmp/res9-copy/plugins/task-pipeline/skills/task-pipeline/references/residue.md"
          t = open(p, encoding="utf-8").read()
          n = 'end it **when it is provably spent**'
          assert n in t, "PLANT DID NOT LAND: res9"
          _planted = t.replace(n, 'end it **when it looks abandoned**', 1)
          assert _planted != t, "PLANT DID NOT LAND: res9"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/res9-copy/test/validate.py 2>&1 | grep -q 'lost `provably spent`'; then
            echo "OK: validator FAILs — 'provably spent' is a fact and 'looks abandoned' is a guess"
          else
            echo "ERROR: an earlier run's residue became deletable on a judgement"
            exit 1
          fi
      - name: Negative self-test (the third owner state stops being bounded to this project)
        run: |
          set -eu
          rm -rf /tmp/res10-copy && cp -R . /tmp/res10-copy
          python3 - <<'EOF'
          p = "/tmp/res10-copy/plugins/task-pipeline/skills/task-pipeline/references/residue.md"
          t = open(p, encoding="utf-8").read()
          n = '**A foreign item never becomes spent.**'
          assert n in t, "PLANT DID NOT LAND: res10"
          _planted = t.replace(n, '**A foreign item becomes spent eventually.**', 1)
          assert _planted != t, "PLANT DID NOT LAND: res10"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/res10-copy/test/validate.py 2>&1 | grep -q 'outside the project'; then
            echo "OK: validator FAILs — widening what a run may clean inside its project must not widen it outside"
          else
            echo "ERROR: uptime became permission to stop another project's container"
            exit 1
          fi
      - name: Negative self-test (the routing surface and the doctrine disagree on the ledger field's name)
        run: |
          set -eu
          rm -rf /tmp/res11-copy && cp -R . /tmp/res11-copy
          python3 - <<'EOF'
          p = "/tmp/res11-copy/plugins/task-pipeline/skills/task-pipeline/SKILL.md"
          t = open(p, encoding="utf-8").read()
          n = 'prints `holds: N`'
          assert n in t, "PLANT DID NOT LAND: res11"
          _planted = t.replace(n, 'prints `residue: N`', 1)
          assert _planted != t, "PLANT DID NOT LAND: res11"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/res11-copy/test/validate.py 2>&1 | grep -q 'must agree on a'; then
            echo "OK: validator FAILs — SKILL.md and residue.md must name the same field"
          else
            echo "ERROR: SKILL.md told runs to print a field the doctrine does not define, and it shipped that way once"
            exit 1
          fi
      - name: Negative self-test (the cross-cutting rules stop naming the field every gate prints)
        run: |
          set -eu
          rm -rf /tmp/res12-copy && cp -R . /tmp/res12-copy
          python3 - <<'EOF'
          p = "/tmp/res12-copy/plugins/task-pipeline/skills/task-pipeline/SKILL.md"
          t = open(p, encoding="utf-8").read()
          n = 'every gate\n   prints `holds: N`'
          assert n in t, "PLANT DID NOT LAND: res12"
          _planted = t.replace(n, 'every gate\n   says nothing about what', 1)
          assert _planted != t, "PLANT DID NOT LAND: res12"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/res12-copy/test/validate.py 2>&1 | grep -q 'no longer name the field'; then
            echo "OK: validator FAILs — a gate disclosure nobody is told to print does not exist"
          else
            echo "ERROR: the obligation to print holds: vanished and only the doctrine file remembered it"
            exit 1
          fi
      - name: Negative self-test (the residue doctrine file is deleted outright)
        run: |
          set -eu
          rm -rf /tmp/res13-copy && cp -R . /tmp/res13-copy
          python3 - <<'EOF'
          import os, re
          p = "/tmp/res13-copy/plugins/task-pipeline/skills/task-pipeline/references/residue.md"
          assert os.path.isfile(p), "PLANT DID NOT LAND: res13"
          os.remove(p)
          assert not os.path.exists(p), "PLANT DID NOT LAND: res13"
          EOF
          if python3 /tmp/res13-copy/test/validate.py 2>&1 | grep -q 'references/residue.md is gone'; then
            echo "OK: validator FAILs — every gate prints holds: and the doctrine behind it must exist"
          else
            echo "ERROR: the file defining eight classes and the teardown vanished and the suite stayed green"
            exit 1
          fi
      - name: Negative self-test (the not-torn-down section is deleted, not merely reworded)
        run: |
          set -eu
          rm -rf /tmp/res14-copy && cp -R . /tmp/res14-copy
          python3 - <<'EOF'
          import os, re
          p = "/tmp/res14-copy/plugins/task-pipeline/skills/task-pipeline/references/residue.md"
          t = open(p, encoding="utf-8").read()
          m = re.search(r"^## What must \*\*not\*\* be torn down.*?(?=^## )", t, re.S | re.M)
          assert m, "PLANT DID NOT LAND: res14"
          _planted = t[:m.start()] + t[m.end():]
          assert _planted != t, "PLANT DID NOT LAND: res14"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/res14-copy/test/validate.py 2>&1 | grep -q 'no section on what must not be torn down'; then
            echo "OK: validator FAILs — the section is required to exist, not merely to be quoted elsewhere"
          else
            echo "ERROR: the asymmetry section was removed wholesale and only its citations remained"
            exit 1
          fi
      - name: Negative self-test (stage 10 loses criterion 13 entirely)
        run: |
          set -eu
          rm -rf /tmp/res15-copy && cp -R . /tmp/res15-copy
          python3 - <<'EOF'
          import os, re
          p = "/tmp/res15-copy/plugins/task-pipeline/skills/task-pipeline/references/acceptance.md"
          t = open(p, encoding="utf-8").read()
          m = re.search(r"^13\. .*?(?=^\S)", t, re.S | re.M)
          assert m, "PLANT DID NOT LAND: res15"
          _planted = t[:m.start()] + t[m.end():]
          assert _planted != t, "PLANT DID NOT LAND: res15"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/res15-copy/test/validate.py 2>&1 | grep -q 'has no criterion 13'; then
            echo "OK: validator FAILs — a give-back written as good intention runs only on the runs that did not need it"
          else
            echo "ERROR: the gate criterion was deleted and every text-level check still passed"
            exit 1
          fi
      - name: Negative self-test (the owner table loses its earlier-run row)
        run: |
          set -eu
          rm -rf /tmp/res16-copy && cp -R . /tmp/res16-copy
          python3 - <<'EOF'
          import os, re
          p = "/tmp/res16-copy/plugins/task-pipeline/skills/task-pipeline/references/residue.md"
          t = open(p, encoding="utf-8").read()
          m = re.search(r"^\|\s*\*\*an earlier run of this project\*\*.*$", t, re.M)
          assert m, "PLANT DID NOT LAND: res16"
          _planted = t[:m.start()] + t[m.end():]
          assert _planted != t, "PLANT DID NOT LAND: res16"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/res16-copy/test/validate.py 2>&1 | grep -q 'lost its `an earlier run'; then
            echo "OK: validator FAILs — the state that says which debris a run may clear must exist"
          else
            echo "ERROR: the middle owner state disappeared and the two remaining ones read as complete"
            exit 1
          fi
      - name: Negative self-test (INVERSION — the foreign lease keeps its sentence and gains a carve-out)
        run: |
          set -eu
          rm -rf /tmp/res17-copy && cp -R . /tmp/res17-copy
          python3 - <<'EOF'
          p = "/tmp/res17-copy/plugins/task-pipeline/skills/task-pipeline/references/residue.md"
          t = open(p, encoding="utf-8").read()
          n = '**A lease held by another agent is never released by this run**'
          assert n in t, "PLANT DID NOT LAND: res17"
          _planted = t.replace(n, '**A lease held by another agent is never released by this run**, unless it has clearly expired', 1)
          assert _planted != t, "PLANT DID NOT LAND: res17"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/res17-copy/test/validate.py 2>&1 | grep -q "exception marker"; then
            echo "OK: validator FAILs — an absolute rule with a carve-out is the inversion, substring intact"
          else
            echo "ERROR: another agent's lease became releasable by appending four words, and every substring check agreed"
            exit 1
          fi
      - name: Negative self-test (INVERSION — the foreign-item boundary keeps its sentence and gains a carve-out)
        run: |
          set -eu
          rm -rf /tmp/res18-copy && cp -R . /tmp/res18-copy
          python3 - <<'EOF'
          p = "/tmp/res18-copy/plugins/task-pipeline/skills/task-pipeline/references/residue.md"
          t = open(p, encoding="utf-8").read()
          n = '**A foreign item never becomes spent.**'
          assert n in t, "PLANT DID NOT LAND: res18"
          _planted = t.replace(n, '**A foreign item never becomes spent**, except where nobody claims it.', 1)
          assert _planted != t, "PLANT DID NOT LAND: res18"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/res18-copy/test/validate.py 2>&1 | grep -q "exception marker"; then
            echo "OK: validator FAILs — the boundary of the third owner state admits no exception"
          else
            echo "ERROR: the third owner state reached outside the project through an appended clause"
            exit 1
          fi
      - name: Negative self-test (INVERSION — the carve-out is a separate sentence, not a clause)
        run: |
          set -eu
          rm -rf /tmp/res19-copy && cp -R . /tmp/res19-copy
          python3 - <<'EOF'
          p = "/tmp/res19-copy/plugins/task-pipeline/skills/task-pipeline/references/residue.md"
          t = open(p, encoding="utf-8").read()
          n = "**A foreign item never becomes spent.**"
          assert n in t, "PLANT DID NOT LAND: res19"
          # After the period, so a sentence-scoped guard cannot see it. The first
          # version of that guard was sentence-scoped and this walked straight past.
          _planted = t.replace(n, n + " Except when a container has been idle for more than a week.", 1)
          assert _planted != t, "PLANT DID NOT LAND: res19"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/res19-copy/test/validate.py 2>&1 | grep -q "exception marker"; then
            echo "OK: validator FAILs — the guard reads the whole span, not one sentence"
          else
            echo "ERROR: a carve-out one period away from the rule was invisible"
            exit 1
          fi
      - name: Property check (a legitimate exception two rules away does not trip the carve-out guard)
        run: |
          set -eu
          rm -rf /tmp/carve-ctl-copy && cp -R . /tmp/carve-ctl-copy
          python3 - <<'EOF'
          p = "/tmp/carve-ctl-copy/plugins/task-pipeline/skills/task-pipeline/references/residue.md"
          t = open(p, encoding="utf-8").read()
          # An `unless` that belongs to the ambiguous-ownership bullet, two rules away
          # from the lease rule and in a different bullet from the foreign-item rule.
          # Paragraph-wide scoping tripped on exactly this; a guard that fires on
          # honest text is worse than one that sleeps, because it teaches evasion.
          n = "than deleted, which is irreversible."
          assert n in t, "PLANT DID NOT LAND: carve-ctl"
          _planted = t.replace(n, "than deleted, which is irreversible unless the operator says otherwise.", 1)
          assert _planted != t, "PLANT DID NOT LAND: carve-ctl"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/carve-ctl-copy/test/validate.py >/dev/null 2>&1; then
            echo "OK: the guard reads the rule's own item, so a neighbour's exception is not its business"
          else
            echo "ERROR: a legitimate exception in an unrelated rule failed the suite — false positive"
            exit 1
          fi
      - name: Negative self-test (the SURFACED zero stops being checked against what the run filed)
        run: |
          set -eu
          rm -rf /tmp/sf1-copy && cp -R . /tmp/sf1-copy
          python3 - <<'EOF'
          p = "/tmp/sf1-copy/plugins/task-pipeline/skills/task-pipeline/references/progress.md"
          t = open(p, encoding="utf-8").read()
          n = 'is checked against what the run filed'
          assert n in t, "PLANT DID NOT LAND: sf1"
          _planted = t.replace(n, 'is worth writing carefully', 1)
          assert _planted != t, "PLANT DID NOT LAND: sf1"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/sf1-copy/test/validate.py 2>&1 | grep -q 'no longer checked against the artefacts'; then
            echo "OK: validator FAILs — the one hand-back field with no lower bound needs the check DEC-0001 decided"
          else
            echo "ERROR: SURFACED went back to a zero nobody can disagree with"
            exit 1
          fi
      - name: Negative self-test (the SURFACED check loses the residual that keeps it honest)
        run: |
          set -eu
          rm -rf /tmp/sf2-copy && cp -R . /tmp/sf2-copy
          python3 - <<'EOF'
          p = "/tmp/sf2-copy/plugins/task-pipeline/skills/task-pipeline/references/progress.md"
          t = open(p, encoding="utf-8").read()
          n = 'nothing will notice'
          assert n in t, "PLANT DID NOT LAND: sf2"
          _planted = t.replace(n, 'the run is done', 1)
          assert _planted != t, "PLANT DID NOT LAND: sf2"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/sf2-copy/test/validate.py 2>&1 | grep -q 'lost its residual'; then
            echo "OK: validator FAILs — a check sold as more than it is buys false confidence"
          else
            echo "ERROR: the check now reads as coverage it does not have"
            exit 1
          fi
      - name: Negative self-test (the behaviour-or-reporting field loses one of its two values)
        run: |
          set -eu
          rm -rf /tmp/r6a-copy && cp -R . /tmp/r6a-copy
          python3 - <<'EOF'
          p = "/tmp/r6a-copy/plugins/task-pipeline/skills/task-pipeline/references/acceptance.md"
          t = open(p, encoding="utf-8").read()
          n = 'One of two words per finding, `behaviour` or `reporting`,'
          assert n in t, "PLANT DID NOT LAND: r6a"
          _planted = t.replace(n, 'One word per finding, `behaviour`,', 1)
          assert _planted != t, "PLANT DID NOT LAND: r6a"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/r6a-copy/test/validate.py 2>&1 | grep -q 'both values as a pair'; then
            echo "OK: validator FAILs — one value cannot distinguish a fix from a note"
          else
            echo "ERROR: R-006 got a field that cannot answer the question it exists for"
            exit 1
          fi
      - name: Negative self-test (criterion 12a is deleted outright)
        run: |
          set -eu
          rm -rf /tmp/r6b-copy && cp -R . /tmp/r6b-copy
          python3 - <<'EOF'
          p = "/tmp/r6b-copy/plugins/task-pipeline/skills/task-pipeline/references/acceptance.md"
          t = open(p, encoding="utf-8").read()
          n = '12a. **Every finding closed in this run records which changed'
          assert n in t, "PLANT DID NOT LAND: r6b"
          _planted = t.replace(n, '12z. **Something else entirely', 1)
          assert _planted != t, "PLANT DID NOT LAND: r6b"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/r6b-copy/test/validate.py 2>&1 | grep -q 'no longer records, per finding'; then
            echo "OK: validator FAILs — R-006 has been in force for four releases with nothing able to read it"
          else
            echo "ERROR: the distinction between fixing and reporting vanished and every close-out still passed"
            exit 1
          fi
      - name: Negative self-test (the release path stops running the suite it advertises)
        run: |
          set -eu
          rm -rf /tmp/rel1-copy && cp -R . /tmp/rel1-copy
          python3 - <<'EOF'
          p = "/tmp/rel1-copy/.github/workflows/release.yml"
          t = open(p, encoding="utf-8").read()
          n = 'npm run test:all'
          assert n in t, "PLANT DID NOT LAND: rel1"
          _planted = t.replace(n, 'npm test', 1)
          assert _planted != t, "PLANT DID NOT LAND: rel1"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/rel1-copy/test/validate.py 2>&1 | grep -q 'publishes without running'; then
            echo "OK: validator FAILs — a tag is not evidence"
          else
            echo "ERROR: the release path can ship a red suite again, which it has done twice"
            exit 1
          fi
      - name: Negative self-test (a version number already spoken for by a tag)
        run: |
          set -eu
          rm -rf /tmp/vt1-copy && cp -R . /tmp/vt1-copy
          cd /tmp/vt1-copy
          # A collision is DIVERGENCE. Every tag in this repo is an ancestor of main,
          # and carrying a released version forward is legitimate — so a real collision
          # has to be built: a commit with no parent, tagged, and then claimed.
          # Identity inline: a CI runner has none configured and commit-tree exits 128.
          # Green locally, red in CI — the environment dependence B-066 is about, mine.
          SIDE=$(git -c user.email=t@t -c user.name=t commit-tree \
                   "$(git hash-object -t tree /dev/null)" -m "another lineage")
          # -f, because a copy of a git WORKTREE carries .git as a pointer and this
          # tag lands in the real repository. Verified once, the hard way.
          git tag -f v99.99.99 "$SIDE" >/dev/null
          python3 - <<'EOF'
          import json, re
          for f, fn in (("package.json", lambda d: d.__setitem__("version", "99.99.99")),
                        (".claude-plugin/marketplace.json", lambda d: d["plugins"][0].__setitem__("version", "99.99.99")),
                        ("plugins/task-pipeline/.claude-plugin/plugin.json", lambda d: d.__setitem__("version", "99.99.99"))):
              d = json.load(open(f, encoding="utf-8")); fn(d)
              json.dump(d, open(f, "w", encoding="utf-8"), indent=2, ensure_ascii=False)
              open(f, "a", encoding="utf-8").write("\n")
          for f, pat, rep in (("SKILL-CARD.md", r"(\| \*\*Version\*\* \| )[\d.]+", r"\g<1>99.99.99"),
                              ("CHANGELOG.md", r"^## v[\d.]+", "## v99.99.99")):
              t = open(f, encoding="utf-8").read()
              _p = re.sub(pat, rep, t, count=1, flags=re.M)
              assert _p != t, "PLANT DID NOT LAND: vt1 " + f
              open(f, "w", encoding="utf-8").write(_p)
          EOF
          cd - >/dev/null
          _out=$(python3 /tmp/vt1-copy/test/validate.py 2>&1 || true)
          if printf '%s' "$_out" | grep -q "is spoken"; then
            echo "OK: validator FAILs — the number is taken by another lineage, and the merge is not the place to find out"
          elif printf '%s' "$_out" | grep -q "shallow clone, ancestry unknowable"; then
            echo "SKIP: shallow checkout — ancestry cannot be answered, so the collision cannot be staged"
          else
            echo "ERROR: a branch may claim a version another lineage already ships under"
            exit 1
          fi
      - name: Negative self-test (the design skill is allowed to run before the file it should follow)
        run: |
          set -eu
          rm -rf /tmp/fig1-copy && cp -R . /tmp/fig1-copy
          python3 - <<'EOF'
          p = "/tmp/fig1-copy/plugins/task-pipeline/skills/task-pipeline/references/build.md"
          t = open(p, encoding="utf-8").read()
          n = 'runs **after** the file, never instead of it'
          assert n in t, "PLANT DID NOT LAND: fig1"
          _planted = t.replace(n, 'is also worth consulting', 1)
          assert _planted != t, "PLANT DID NOT LAND: fig1"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/fig1-copy/test/validate.py 2>&1 | grep -q 'order of authority is gone'; then
            echo "OK: validator FAILs — sheleg-design after the frame, never instead of it"
          else
            echo "ERROR: the visual skill may now invent what the frame already decided"
            exit 1
          fi
      - name: Negative self-test (the frame becomes a contract without a width)
        run: |
          set -eu
          rm -rf /tmp/fig2-copy && cp -R . /tmp/fig2-copy
          python3 - <<'EOF'
          p = "/tmp/fig2-copy/plugins/task-pipeline/skills/task-pipeline/references/build.md"
          t = open(p, encoding="utf-8").read()
          n = 'contract at its own width'
          assert n in t, "PLANT DID NOT LAND: fig2"
          _planted = t.replace(n, 'contract', 1)
          assert _planted != t, "PLANT DID NOT LAND: fig2"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/fig2-copy/test/validate.py 2>&1 | grep -q 'without its'; then
            echo "OK: validator FAILs — a frame is one width and said nothing about the others"
          else
            echo "ERROR: a rule that cannot be followed at any other breakpoint shipped as if it could"
            exit 1
          fi
      - name: Negative self-test (the missing-frame branch loses the offer to draw)
        run: |
          set -eu
          rm -rf /tmp/fig3-copy && cp -R . /tmp/fig3-copy
          python3 - <<'EOF'
          p = "/tmp/fig3-copy/plugins/task-pipeline/skills/task-pipeline/references/build.md"
          t = open(p, encoding="utf-8").read()
          n = 'offers to draw'
          assert n in t, "PLANT DID NOT LAND: fig3"
          _planted = t.replace(n, 'mentions', 1)
          assert _planted != t, "PLANT DID NOT LAND: fig3"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/fig3-copy/test/validate.py 2>&1 | grep -q 'lost a half'; then
            echo "OK: validator FAILs — building and naming without offering leaves the file drifting"
          else
            echo "ERROR: the design file drifts from the product with nobody proposing to close it"
            exit 1
          fi
      - name: Negative self-test (generated frames stop being marked as generated)
        run: |
          set -eu
          rm -rf /tmp/fig4-copy && cp -R . /tmp/fig4-copy
          python3 - <<'EOF'
          p = "/tmp/fig4-copy/plugins/task-pipeline/skills/task-pipeline/references/build.md"
          t = open(p, encoding="utf-8").read()
          n = 'marked as coming from implementation'
          assert n in t, "PLANT DID NOT LAND: fig4"
          _planted = t.replace(n, 'added', 1)
          assert _planted != t, "PLANT DID NOT LAND: fig4"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/fig4-copy/test/validate.py 2>&1 | grep -q 'lost a half'; then
            echo "OK: validator FAILs — a designer must tell a decision from a generation"
          else
            echo "ERROR: an unmarked generated frame reads as a design decision, which is the same false confidence as an unproven green"
            exit 1
          fi
      - name: Negative self-test (the screen-is-the-frame section is deleted outright)
        run: |
          set -eu
          rm -rf /tmp/fig5-copy && cp -R . /tmp/fig5-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/fig5-copy/plugins/task-pipeline/skills/task-pipeline/references/build.md"
          t = open(p, encoding="utf-8").read()
          # fig1-fig4 mutate a phrase inside the section, so none of them reaches the
          # branch that fires when the heading itself is gone. A fail() path with no
          # probe is a path nobody has watched.
          m = re.search(r"^## 4a\. A screen is the frame, implemented.*?(?=^## )", t, re.S | re.M)
          assert m, "PLANT DID NOT LAND: fig5"
          _planted = t[:m.start()] + t[m.end():]
          assert _planted != t, "PLANT DID NOT LAND: fig5"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/fig5-copy/test/validate.py 2>&1 | grep -q "no section making a screen"; then
            echo "OK: validator FAILs — without the section Figma is an address and a link, which is what it was"
          else
            echo "ERROR: the whole rule was removed and only its citations objected"
            exit 1
          fi
      - name: Negative self-test (the worktree-per-agent rule is removed)
        run: |
          set -eu
          rm -rf /tmp/ag1-copy && cp -R . /tmp/ag1-copy
          python3 - <<'EOF'
          p = "/tmp/ag1-copy/plugins/task-pipeline/skills/task-pipeline/references/build.md"
          t = open(p, encoding="utf-8").read()
          n = '**A worktree per agent, always.**'
          assert n in t, "PLANT DID NOT LAND: ag1"
          _planted = t.replace(n, '**Try to avoid overlapping.**', 1)
          assert _planted != t, "PLANT DID NOT LAND: ag1"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/ag1-copy/test/validate.py 2>&1 | grep -q 'worktree rule is gone'; then
            echo "OK: validator FAILs — sharing a checkout is what corrupts two independent changes into one state"
          else
            echo "ERROR: two agents may share a working tree again, which cost four version collisions in one session"
            exit 1
          fi
      - name: Negative self-test (the lease rule is removed)
        run: |
          set -eu
          rm -rf /tmp/ag2-copy && cp -R . /tmp/ag2-copy
          python3 - <<'EOF'
          p = "/tmp/ag2-copy/plugins/task-pipeline/skills/task-pipeline/references/build.md"
          t = open(p, encoding="utf-8").read()
          n = '**A lease before any shared register.**'
          assert n in t, "PLANT DID NOT LAND: ag2"
          _planted = t.replace(n, '**Be careful with shared files.**', 1)
          assert _planted != t, "PLANT DID NOT LAND: ag2"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/ag2-copy/test/validate.py 2>&1 | grep -q 'lease rule is gone'; then
            echo "OK: validator FAILs — a worktree separates files and says nothing about who may edit the board"
          else
            echo "ERROR: the shared registers lost the only rule that stops two agents overwriting each other"
            exit 1
          fi
      - name: Negative self-test (what to do on finding the other agent mid-run is removed)
        run: |
          set -eu
          rm -rf /tmp/ag3-copy && cp -R . /tmp/ag3-copy
          python3 - <<'EOF'
          p = "/tmp/ag3-copy/plugins/task-pipeline/skills/task-pipeline/references/build.md"
          t = open(p, encoding="utf-8").read()
          n = 'leave their work alone'
          assert n in t, "PLANT DID NOT LAND: ag3"
          _planted = t.replace(n, 'proceed as you see fit', 1)
          assert _planted != t, "PLANT DID NOT LAND: ag3"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/ag3-copy/test/validate.py 2>&1 | grep -q 'mid-run is'; then
            echo "OK: validator FAILs — ending someone else's work to unblock your own is the asymmetry residue.md refuses"
          else
            echo "ERROR: a run may now stage, revert or stash another agent's uncommitted work"
            exit 1
          fi
      - name: Negative self-test (the another-agent section is deleted outright)
        run: |
          set -eu
          rm -rf /tmp/ag4-copy && cp -R . /tmp/ag4-copy
          python3 - <<'EOF'
          import re
          p = "/tmp/ag4-copy/plugins/task-pipeline/skills/task-pipeline/references/build.md"
          t = open(p, encoding="utf-8").read()
          m = re.search(r"^## 1a\. Another agent may be in this repository right now.*?(?=^## )", t, re.S | re.M)
          assert m, "PLANT DID NOT LAND: ag4"
          _planted = t[:m.start()] + t[m.end():]
          assert _planted != t, "PLANT DID NOT LAND: ag4"
          open(p, "w", encoding="utf-8").write(_planted)
          EOF
          if python3 /tmp/ag4-copy/test/validate.py 2>&1 | grep -q "nothing tells a run that another agent"; then
            echo "OK: validator FAILs — ag1-ag3 mutate inside the section and never reach this branch"
          else
            echo "ERROR: the whole rule was removed and only its phrases were guarded"
            exit 1
          fi
